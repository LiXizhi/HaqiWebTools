# HaqiCombatSim 总体计划

## 2026-09-23：全岛任务可以推进

427条未废除任务里，14条教学任务仍走原章节。其余413条可以接取、累计进度并交付。击败、掉落、交谈、空目标交付，以及强化、镶嵌、喂养、换卡包和身上的物品、等级、攻击、防御都会计入。剧情对白脚本和变身不执行。红蘑菇对战、充值、人气和原服徽章仍会标明无法推进。冒险等级上限是10，更高的原版等级下限会降到这个上限。详见任务目录。

## 2026-09-23：会员到期日自动兑换魔豆

Keepwork 会员剩余日历天数会在登录后自动换成魔豆，每天 10 颗。账号第一次从今天算到到期日，并记下该日期；之后只兑换更晚的新增天数。战斗中延后到战斗结束。详见宠物与商店文档。

## 2026-09-22：符文目录与抓宠符文

45项RuneList已导出，含商店、任务和原版掉落途径。普通/高级/顶级抓宠符文按kids公式结算：权重乘以损失生命比例，再加上等级差/80；强制概率覆盖该结果。成功或失败都消耗一张，已拥有、普通怪物和死亡目标不消耗。安卓婆婆可用10/200仙豆出售前两张；顶级符文兑换编号为0，仍不出售。2026-09-23起，捕获晶球退出商城和战斗按钮，未使用的晶球在读档或战斗结束时换成普通抓宠符文；已经掷过晶球的旧战斗仍按原记录重演。272项未列入RuneList的符文物品只计数，不开放。

## 2026-09-22：怪物美术已接入战斗

23张原版Boss WebP已上传并校验Keepwork CDN，607项模板/模型外观覆盖当前全部副本怪物，地图与战斗统一渲染。普通怪物复用25种已有宠物；419项测试、构建和桌面/手机CDN及离线预览通过。

## 2026-09-22：全岛任务手记可浏览

底部任务窗口沿用现有布局，展示684条原任务，支持地区/状态筛选、名称编号搜索、20项分页及前置跳转。约472KB精简目录按需加载，失败可重试。14个教学任务保留真实进度与追踪，其余只读并标明尚未开放/已废除。桌面、390px及无图兜底验收通过，416项测试与完整构建通过。详见[任务目录](quest-catalog.md)。

## 2026-09-22：全岛任务目录与怪物美术准备

原版kids全部684个任务已导出，保留对白、条件、奖励、废除与重复标志及全部辅助表；实际任务目标按原Lua使用Excel表。607个怪物模板、611个定义与540个法阵实例完整归档。359种宠物中整理530个怪物外观候选；23个剧情首领保留原模型、58个待选图。19张原版64px缩略图保留为对照；随后已启动本机Paracraft，完成23张原模型高清透明WebP本地归档，最大44,354字节，已完成CDN发布和运行时绑定（见当日后续验证记录）。新增目录不自动开放剧情，既有14个教学任务运行范围保持。详情及原始缺口见[全岛任务与怪物目录](quest-catalog.md)。


## 2026-09-22：海上捕鱼

点击岛屿海岸外的海面即可撒网。普通网要罩住鱼影才结算；必中网和捕鱼器点中海面即结算。成功结算才按原版 ExtendedCost 扣一张网，并在六个或七个兑换分支中等概率取一条，分支内 `p/1000` 逐项判定。翻倍网数量为2，皇冠网只有闪闪皇冠鱼，普通/精良捕鱼器含“鱼儿跑掉了”分支。精力前提为10，上限按非会员100；中、大药剂按兑换回复并封顶。小药剂仍因未知前提关闭。排行标记只记在角色上，不进背包，也没有全服榜。渔夫收购价在物品表中为0，本轮不出售。

## 2026-09-22：六岛NPC与商人数据

已从原版收录292条居民实例、1764条商店行、8位导师目录、1699份兑换记录，保留完整来源与未落地NPC报告。六岛地图接入原居民，交谈打开商品/课程；导师学习和已支持普通货币符文兑换按原表校验并原子保存。新增居民为道路旁二维布局，缺图使用通用标记。装备/坐骑/原服直购、付费限购、特殊条件、洗点与回售仅保留目录并明确禁用；不宣称所有交易和剧情完成。374项全量测试、4项定向回归、完整构建及桌面/390px隔离交互通过。详见adventure.md和当日日志。


## 2026-09-21：反射、隐身、群攻仇恨与会员经验

新遭遇增加reflectionRulesVersion=1、stealthRulesVersion=1，threatRulesVersion升级5；旧检查点保留既有规则。真实反射按普通吸收后的整次伤害计算，消耗反射容量，经过攻击者盾/抗性与最终权重，不递归、不致死，kids上限4500；DOT仅消耗盾不反射。隐身阻挡非area/arena/singleheal卡名的目标，回合到期解除，使用原版眩晕效果仇恨；死亡清空新增状态。角色状态栏显示反射量和隐身回合。

版本5补齐冰系普通AreaAttack的2倍仇恨、AreaDOTAttack逐目标延迟仇恨。会员战斗经验按服务端表对应总倍率155%至200%，向上取整，冻结于新遭遇；非会员100%，旧遭遇不追补，任务经验不变。

验证：368项全量、24项战斗定向、9项会员、4项状态UI通过；浏览器仅核验状态标签模块输出，未完成本轮完整战斗动画及多尺寸视觉验收。完整npm run build再次确认被camp.json快照过期阻断，未修改地图。

仍未全部完成：口袋十项奖品依赖捕鱼、幸运抽奖、精力、材料用途及世界聊天，当前本地没有这些对应系统。原版翻倍网/皇冠网走1605/1606兑换，精力药剂读stats[51]走ExtendedCost，不能猜测服务端奖励或擅自销毁物品。需要迁移对应玩法并取得兑换配置，或明确采用Web改编规则。普通HoT/DoT条目为表现/内部类型，本轮未为它们猜测独立原版施法分支；完整卡型覆盖审计仍不能宣称完成。剧情、变身、原服账号及真实存档继续不动。

## 2026-09-21：原服角色有限导入

已按用户授权接入 Haqi.html：仅读取穿戴、装备、已学牌与符文，复用背包/卡包进行内存预览，两者均查看后显式确认才创建独立角色。转换核对原版 Lua 与现有数值库，不支持项显示差异；独立测试页保持兼容。35项定向测试、隔离浏览器预览/取消/身份变化与390px布局、Vite打包通过。完整构建仍受已有camp地图过期门禁阻断；本轮未再次用真实账号落盘。范围详见 [player-import.md](player-import.md)。

## 2026-09-21：图腾操作与套装详情

图腾首次学习免费，转换消耗50魔豆（984）并保留经验（50359），依据原版TotemLearnPage.html。四种kids图腾学习/转换和经验道具使用已接入背包；经验按原版物品70/71属性和等级差折减，不新增免费获取途径。动作通过persistReward先保存后替换角色，战斗期间禁止修改。

套装详情显示实际穿戴件数、各门槛激活状态和属性；原版禁用的双重攻击保持不启用。图腾显示经验、等级与成长属性。351项全量测试、Vite独立打包和编辑器诊断通过；隔离浏览器核验真实火舞五件套详情及1280/390px图腾布局，未修改真实存档。

剩余：口袋奖品实际使用、完整仇恨的隐身/未覆盖卡型/特殊群攻参数、反射盾真实反射、魔法星经验加成。图腾道具来源未新增，原服付费及服务器兑换未接入。世界剧情和变身继续排除。完整npm构建此前被camp地图快照门禁阻断，本轮仅验证Vite打包。

## 2026-09-21：延迟仇恨与倍率十轮

1. SingleHealWithHOT与DOTAttackWithHOT在施法时预计算HOT仇恨。
2. DOTAttack按施法时暴击/护符计算绝对值直接与溅射延迟仇恨。
3. AreaHealWithHOT逐人逐跳两次取整后累计。
4. SingleAttackWithDOT使用带符号的预计算队列及溅射。
5. AreaAttackWithDOT仅给各受击怪物注册，不继承本地伤害旁目标0.83系数。
6. 防御/嘲讽姿态即时仇恨分别乘3/5，延迟队列及最高仇恨嘲讽不乘。
7. 成功眩晕产生500目标仇恨，被眩晕盾吸收不产生；移除盾、偷盾补齐。
8. 群攻直接仇恨不再产生旁观溅射，计算位于最终伤害权重前。
9. 延迟队列遵循原版写入顺序、尾部出队，并在怪物UseCard入口推进，不在轮首推进。
10. 对称盾、反射盾及按受益人数计数的群体能量、群体净化仇恨接入。

新遭遇仇恨版本4；旧版本0–3保留原行为。340项全量、18项仇恨定向通过，包括真实火系DOT三轮事件/队列/RNG重演。未操作真实存档或主APP接线。

历史更正：后续已在TotemLearnPage.html找到首次免费、转换50魔豆并保留经验的明确规则；图腾操作与套装详情进展见顶部记录。口袋奖品使用仍未实现。完整仇恨仍有隐身、未覆盖卡型及特殊冰系群攻参数等差异；反射盾仍为既有吸收近似。原版ProcessPendingThreatPerTurn死亡分支仅改局部变量，本地保留既有清表行为，不宣称1:1。世界剧情和变身继续排除。

## 2026-09-21：效果与群疗仇恨续接

新遭遇threatRulesVersion=3，旧版本不追补。已接入Global/Global2、MiniAura、RemovePositiveCharm/RemoveNegativeCharm/StealCharm、Charms/Wards、AreaCharm/AreaWard、Absorb共11类效果；群疗即时部分按惩罚前数值逐人累计后乘0.2取整，附带吸收逐人追加400再参与汇总。全部数值走BalanceParams，失误不产生效果仇恨。

327项全量测试通过，覆盖参数覆盖、死亡过滤、怪物施法排除、新旧版本、真实陷阱卡完整回合重演。未修改主APP接线或注入页面测试DOM。仍未完成持续伤害/治疗延迟仇恨、其余效果和生成权重、图腾学习/经验获取、套装详情及口袋奖品使用；本批不是全部非剧情系统完成。

## 2026-09-21：成长属性续接

本批完成绝对攻击151–158、绝对防御159–166、全系闪避188、暴击倍率376；成长规则版本3隔离旧战斗，普通装备与商城映射不变。真实伤害方向、特殊属性效果、单位换算、完整配置覆盖、旧档兼容及完整回合重演共10项定向测试通过。此前全量321项通过，随后新增的回合重演单独通过；Vite独立打包通过。

原版card_server.lua的TryDoubleAttack对kids/teen直接返回false，故256明确不启用，而非等待猜测实现。全部83套/7类图腾导出属性除该禁用项均有映射。

仍未完成：图腾学习/经验获取入口、完整PvE群疗与持续效果仇恨、套装专用详情、口袋奖品使用。未改世界剧情、变身、UI、玩家存档或发布；本批不等于全部系统完成。完整地图构建门禁及浏览器验收未运行。

## 2026-09-21：仇恨十轮继续迭代

1. 单体嘲讽卡接入最高仇恨加增量。
2. 群体嘲讽逐个敌方怪物计算。
3. 引擎拒绝无效仇恨版本。
4. 仇恨目标排除死亡和显式未激活单位。
5. 嘲讽排除未激活目标，验证不改生命值。
6. 单体治疗即时部分在治疗惩罚前产生ceil(heal*0.3)仇恨，满血也生效。
7. 新遭遇使用仇恨版本2，旧版本1不追补治疗仇恨。
8. 真实组队回合验证怪物攻击高仇恨队友。
9. 治疗决定重演验证仇恨、事件及随机状态一致。
10. 新版自伤不误走敌方伤害溅射仇恨通道。

316项全量测试、仇恨定向4项、商城图标测试和Vite独立打包通过，编辑器无错误。未改世界剧情、变身、UI或玩家存档，未提交发布。完整地图构建门禁未重跑，未做浏览器战斗美术验收。

未完成：群疗、DOT/HOT预计算、效果仇恨权重、隐身机制、图腾学习/成长、套装特殊属性151/159/376及图腾188/256、套装专用详情、礼物使用玩法。此次完成十轮，不代表所有非剧情系统已完成；上述内容仍需逐项核对原版后实施。

## 2026-09-21：套装成员映射已补齐

从原版database/globalstore.db.mem读取template第31项itemsetid（Python索引30），按原版1001–8999范围筛选并关联效果表：391件装备覆盖83套。export_progression.mjs调用受限Lua字面量解析导出器，记录缓存SHA-256，重导出不会清空成员。此前“成员缺失”状态已解决。

成长规则版本2启用真实套装，旧版本1战斗仍不计算套装以保持重演。火舞套装真实五件验证2件生命+20、3件全系攻击+5%、5件强力魔力概率+3%，卸装取消门槛效果。映射完整不表示所有特殊属性已映射，也未新增套装专用详情UI。

验证：314项全量测试、4项成长定向测试、Vite独立打包和编辑器诊断通过。未操作玩家存档、提交或发布；完整地图build门禁未重跑。

## 2026-09-21：魔法口袋领取闭环

会员页免费领取接入十项原版奖池，领取前刷新会员身份，复用persistReward先保存再替换角色。每周星级+1次，库存和次数一并保存；角色重载及云端快照保留记录，保存失败或旧标签页不发奖。普通仙豆/法杖领取不会覆盖口袋次数。礼物使用玩法仍未开放。

Web改编：按角色seed、周、领取序号确定性加权抽取，采用XML权重，不复刻旧客户端GetGift区间遗漏问题，也不调用原版exid=660后端。它是本地单人奖励，不是服务器防作弊抽奖；旧存档回滚仍可能回滚奖励记录。

验证：313项全量测试、13项奖励定向测试、Vite独立打包通过。390px隔离内存角色DOM领取验证次数2→1且无横向溢出，非真实会员或触屏验收。完整地图构建门禁未重跑。套装成员、图腾学习和完整仇恨仍待接入。

## 2026-09-21：第三组十轮，开始接入长期系统

1. 魔法星等级表转换为实际战斗属性。
2. 魔法星HP独立乘算，不混入kids装备242的3.14系数。
3. 会员适配器状态进入新遭遇快照，旧档不追补；验证实际HP与恢复。
4. 套装件数门槛累计计算。
5. 图腾职业、经验物品及最高已达档位选择。
6. 魔法口袋每周星级加一次数、跨周及回拨保护。
7. 可复现导出83套效果、7个图腾职业、10项口袋奖池。
8. 套装和已有图腾物品的已支持属性进入角色计算，旧战斗保留旧规则。
9. 仇恨表、最高/最低卡位排序、嘲讽、延迟队列与死亡清除纯规则。
10. 新PvE启用吸收前直接伤害、5%溅射仇恨及目标选择；旧战斗仍走旧行为。

验证：310项自动测试通过，Vite独立生产打包、编辑器诊断及diff检查通过。未做真实账号会员战斗或本轮浏览器交互验收；完整build地图门禁未重跑。未提交发布，未操作玩家存档，仍排除剧情、地图和变身。

实际边界：魔法星六项战斗加成已接通，经验加成未接。套装效果表已导出但原物品itemsetid成员映射缺失，components为空，因此生产套装还不会触发。图腾尚无学习/成长入口，且特殊属性未全部映射，不自动发职业和经验。魔法口袋只有奖池与额度规则，没有领取、扣次数、持久化或UI入口。完整PvE仇恨未完成：治疗、DOT/HOT预计算、各效果权重、隐身/激活过滤、嘲讽卡处理仍待接通；不能将当前直接伤害实现称为完整迁移。

## 2026-09-21：第二组非剧情十轮（不做变身）

范围：继续排除世界剧情，按用户要求不实现变身。以下是十次小步迭代，不是十个大系统迁移。

1. 装备汇总补齐本系韧性、穿透与绝对攻防字段。
2. 汇总补齐治疗、受治疗与全局战斗修正字段。
3. 按原版接入182/183治疗百分比属性映射。
4. 遭遇增加装备规则版本，旧进行中战斗保留旧治疗装备规则。
5. 装备及通用详情统一正负数显示。
6. 换装比较按属性键匹配，验证全系与本系叠加、预览无副作用。
7. 未映射的非零装备属性显示未接入提示。
8. 换装对比显示其他学系的攻击、防御、命中、暴击、韧性和穿透。
9. 当前属性保留主要字段，仅显示非零次要字段。
10. 实际治疗卡验证加成生效、宝石沿用同一映射；保持既有商城准入范围，纯治疗装备待资源准备后再上架。

验证：303项全量测试、16项装备测试通过，Vite独立打包通过。手机隔离DOM详情无横向溢出；真实指针、动画和桌面详情验收未完成。完整build此前被既有地图快照门禁阻断，本轮未重跑。未修改玩家存档、剧情或地图，未提交、发布。

仍未实现：套装属性、完整PvE仇恨、龙图腾、魔法星战斗加成和魔法口袋。变身不在后续实施范围，优先于下方历史计划。

## 2026-09-21：非剧情十轮迭代

1. 统一符文物品识别、法术映射和未支持原因查询。
2. 旅行背包符文详情显示真实法术，取消错误收藏提示。
3. 通用物品预览支持符文状态和卡面。
4. 任务物品奖励可查看详情，不改变任务内容、流程或奖励。
5. 跨战斗重置符文栏，覆盖耗尽拒绝与旧检查点兼容。
6. 符文消耗全部校验后再写库存，拒绝不同遭遇的结果。
7. 引擎决定计数防重复记录，支持重演后继续记录。
8. 符文分页：桌面八种、窄屏四种；任务预览传入卡面渲染器。
9. 拒绝畸形符文检查点和无效决定结构。
10. 符文记录要求引擎结果、消耗不能倒退；验收修正过时35%说明与实际40%卡面的冲突。

验证：293项全量测试通过，Vite打包通过。隔离浏览器DOM事件验证任务奖励详情、背包符文卡面及22种符文分页；390px关闭动画后静态布局无横向溢出。集成浏览器实际指针点击持续超时，完整鼠标/触屏及动画验收未完成，不视为通过。未修改世界剧情、地图、付费或真实账号，不提交或发布。

仍待后续：完整符文导出与获取途径、套装属性、PvE仇恨、龙图腾、变身、魔法星战斗加成和魔法口袋。本轮十次小步迭代不代表这些系统已经迁移。

## 2026-09-21：符文消耗卡与迁移复核

- [x] 已有18类/2子类符文通过实物GSID减1000映射法术，战斗独立入口，不占普通卡包。
- [x] 成功施法扣一张，失误不扣；数量、目标、触屏、重载重演、撤退保留消耗结果。
- [x] 载入时校验符文库存与战斗重演一致，兼容旧检查点。
- [x] 完整RuneList目录与获取途径、专属抓宠符文。捕获晶球已从商城和战斗按钮移除，抓宠改用这三张符文。
- [ ] 四岛原版专属NPC、怪物、完整任务剧情；开放地图不等于剧情迁移。
- [ ] 套装属性、龙图腾、变身；强化和宝石已有实现，但仍需全目录覆盖审计。
- [ ] 原版账号导入、异步PVP、真实账号跨设备验收，需要协议与服务端契约。
- [ ] 宠物据点/繁殖、多语言范围待确认，不把现有养成和魔法星界面视作完整迁移。

验证：287项测试通过，Vite独立打包通过；完整build被既有地图快照过期检查拦截。浏览器隔离符文点击施法数量2→1通过，未写玩家存档；完整美术、多尺寸验收待补充。

> 状态：**Phase 0~6 已实施完成**（2026-09-16）；其后冒险/发布阶段见文中各节。**2026-09-18 真实用户数据、异步 PVP、帕鲁式宠物/VIP 口粮、卡牌多语言仅为计划需求，未实现。** 实施进度以本文件 checkbox、[qa-report.md](qa-report.md) 和 `devlog/` 为准。与原计划的偏差在各阶段末以「实际」标注。

## 目标

1. 在浏览器中以 2D 网页形式复现魔法哈奇 PvP 战斗（寒冰 / 烈火 / 风暴 / 生命 / 死亡，1v1 / 2v2 / 3v3 / 4v4），支持人机对战。
2. 战斗引擎脱离 UI，可在 Web Worker / Node 中以每秒数百场的速度批量模拟，输出五系胜率矩阵。
3. 数值参数（HP 曲线、卡牌伤害、能量球概率、暴击/命中常量等）可在页面中修改并即时重跑。
4. 提供数值调整建议：内置自动调参器 + 大模型自然语言建议。
5. 引擎公式与 `script/apps/Aries/Combat/ServerObject/*.lua` 逐行对照，结论可回移到线上配置。

## 非目标（第一阶段）

- 宠物、符文、龙图腾、装备实物属性（用装备分数区间预设 + 手动滑杆代替）。
- PvP 排位分 / ELO / 奖励结算。
- 真实多人联机（多人 = 本地 Bot 队友 / 对手）。
- PvE 怪物 AI 的 Genes_Attacker。
- 从线上魔法哈奇拉取真实玩家卡包 / 装备 / VIP（2026-09-18 起列为计划需求，见下方；第一阶段仍不包含）。
- 异步（非实时）玩家对战。
- 帕鲁式宠物劳动/据点，以及把治疗、口粮做成 VIP 门槛（现有捕获/四卡位/饱食已交付，会员锁未做）。
- 卡牌与 UI 多语言（卡面说明已程序生成，locale 切换未做）。

## 研究结论摘要

- 权威数值全部在服务端 Lua：伤害 / 治疗 / 暴击 / 闪避 / 命中在 `card_server.lua`；HP 曲线与能量球在 `player_server.lua`；回合状态机在 `arena_server.lua`。详见 [lua-mapping.md](lua-mapping.md)。
- `combat_server.lua` 与 GSL gridnode、`PowerItemManager` 强耦合，无法直接 headless 复用，因此选择 JS 1:1 移植而非桥接。
- 1v1~4v4 在引擎层没有区别，只是每边单位数（上限 4）；差别来自大厅 key 和 arena XML 的人数限制。
- 五系之间**没有相克表**（`boost_table` 已废弃），平衡完全由 HP 曲线、卡牌数值、charm/ward、装备百分比决定。
- 卡牌数据在本机 `config/Aries/`（gitignored）：kids 811 张、teen 1374 张；`deck_attacker_ai/Aggressive{School}.csv` 直接以 cardkey 给出官方 AI 卡组与情境权重，可作为 Bot 策略与预设卡组来源，无需 gsid 映射。

## 阶段与任务

### Phase 0 — 项目骨架
- [x] `HaqiCombatSim.html`（**实际**：自带 `css/style.css`，不引 Tailwind CDN，保证离线可用）
- [x] `package.json`（`type: module`；scripts：`export`、`test`、`sim`；**实际**：零依赖，XML 用自写 `scripts/lib/xml_lite.mjs`）
- [x] `.gitignore`（`node_modules/`；`data/kids/` 与 `data/teen/` 入库）
- [x] `js/app.js`、`state.js`、`utils.js`（**实际**：`config`/`i18n` 并入 `state.js` 与 `combat_presets_core.js` 的 `SCHOOL_NAMES`，未单列）
- [x] `data/sample/` 手写极小数据集（52 卡）：每系 6~8 张卡（单攻 / 群攻 / 治疗 / blade / shield / DOT）、1 个 stats 区间、1 份 AI 权重

### Phase 1 — 数据导出器（`scripts/export_data.mjs`）
- [x] 解析 `CardList*.xml` → 逐个单卡 XML → `cards.json`（字段与 `Card:CreateCardTemplate` 一致，`params` 全部数值化）
- [x] 解析 `CharmWardList*.xml` → `charms.json`（charm / ward / miniaura / globalaura）
- [ ] 解析 `CombatThreatConfig*.xml` → `threat.json`（**实际**：PvP 不用威胁值，暂未导出）
- [x] 解析 `MobStatsByGearScore*.xml` → `stats_by_gear.json`
- [x] 解析 `deck_attacker_ai/*.csv` → `ai_decks.json`（列名 → 条件键，权重数值化）
- [x] 解析 `MobAIDeckByGearScore*.xml` + gsid 尽力映射 → `ai_deck_by_gear.json`，未映射写 `manifest.unmapped`
- [x] 解析 `HP/HP_level_mapping.xml` → `hp_table.json`
- [x] 生成 `manifest.json`（version、卡数、type 分布、已支持 / 未支持 type 列表、导出时间）
- [x] 对真实 `config/Aries` 跑通 kids + teen，记录未支持 type 占比到 [data-export.md](data-export.md)

### Phase 2 — 无 UI 战斗引擎（`js/combat_*_core.js`、`rng_core.js`）
- [x] `rng_core.js`：mulberry32，接口 `int(min,max)`、`float()`、`pick(arr)`，与 Lua `math.random(a,b)` 闭区间语义一致
- [x] `combat_params_core.js`：`DEFAULT_PARAMS`（按 version）、`resolveParams`、`diffParams`、`serialize/parse`
- [x] `combat_formulas_core.js`：`aboveMinBoost`、`damageExpression`、`healExpression`、`tryCriticalStrike`、`tryDodge`、`rollAccuracy`、`maxHpFor(school, level, version)`、`powerPipChance(level, version, bonus)`、`generatePip(unit)` —— 每个函数注释 Lua 行号
- [x] `combat_unit_core.js`：单位结构、各系 stat 查询（damage% / resist% / accuracy% / crit / resilience / hit / dodge / penetration / absolute）、charm / ward / absorb / DOT / HOT / stun / miniaura、手牌（上限 8）与卡组抽取、pips（上限 7 kids / 14 teen，power pip 对本系卡计 2）
- [x] `combat_cards_core.js`：效果处理器注册表，第一批 type：`Pass`、`SingleAttack`、`SingleAttackWithDOT`、`SingleAttackWithStun`、`SingleAttackWithSelfStun`、`SingleAttackWithLifeTap`、`SingleAttackWithPercent`、`SingleAttackWithStandingWards`、`SingleAttackWithTrap`、`AreaAttack`、`AreaAttackWithDOT`、`AreaAttackWithStun`、`ArenaAttack`、`DOTAttack`、`DOTAttackWithHOT`、`SingleHeal`、`SingleHealWithHOT`、`SingleHealWithCleanse`、`AreaHeal`、`AreaHealWithHOT`、`AreaHealWithAbsorb`、charm / ward / trap 类（`Blade` / `Trap` / `Shield` / `Weakness` 等经 CharmWardList）、`Global`、`MiniAura`、`AreaPowerPipBoost`、`SingleCleanse`、`AreaCleanse`、`RemovePositiveCharm` / `RemoveNegativeCharm` / `StealCharm` 及 ward 对应项、`Stance`、`Random`；其余登记为 `unsupported`
- [x] `combat_arena_core.js`：`createArena(teams, params, rng)`、`advanceTurn`（生成 pip → 所有单位选牌 → 按顺序结算 → DOT/HOT/aura 递减 → 死亡判定）、`isFinished`（一侧全灭或超 `maxRounds` 判平）、`CombatEvent[]` 事件流
- [x] `combat_policy_core.js`：`Policy` 接口；`DeckAttackerBot`（移植 CSV 情境权重：base_weight + 命中条件列累加，<0 剔除，加权随机）；`SimpleBot`（低血治疗 / 有 blade 出大招 / 否则最高可用伤害打最低 HP 敌人）；`HumanPolicy`（返回 Promise，由 UI resolve）
- [x] `combat_presets_core.js`：`makeUnit({school, level, gearBand, version, deckSource})`；HP 走公式，装备百分比取 `stats_by_gear` 区间，卡组取 `ai_decks` 并按 `require_level` / pipcost 过滤
- [x] 测试（**实际**合并为三个文件）：`formulas.test.mjs`（公式 / HP 曲线 / pips / rng）、`engine.test.mjs`（确定性、1v1~4v4 冒烟、状态机、参数覆盖、DOT 溅射 / X 费、批量聚合、调参器、LLM 解析）、`xml_lite.test.mjs`

### Phase 3 — 批量模拟（`js/sim_*.js`）
- [x] `sim_batch_core.js`：`buildMatrix({schools, modes, levels, gearBand, teamComposition})`、`runOne(matchup, params, seed)`、`aggregate(results)`（胜率 + Wilson 置信区间、平均回合、场均伤害 / 治疗、fizzle 率、卡牌使用与贡献伤害、unsupported 计数）
- [x] `sim_worker.js`：接收 `{version, dataset, params, matchups, gamesPer, seed}`，每 N 场 postMessage 进度
- [x] `sim_pool.js`：按 `navigator.hardwareConcurrency` 建池，按 matchup 分片，合并结果
- [x] 性能目标：sample 数据 1v1 ≥ 500 场/秒/worker；真实数据 4v4 ≥ 50 场/秒/worker（实测 Node 单线程 teen 1v1 ≈ 5800 场/s，4v4 ≈ 1250 场/s）

### Phase 4 — 页面（`js/view_*.js`、`css/`）
- [x] 顶栏：kids / teen 切换（重新加载数据集）、数据集状态（manifest 摘要 / 回退 sample 提示）
- [x] 对战页 `view_battle.js`：模式选择、双方阵容配置（系 / 等级 / 装备段 / 策略）、2D 战场（两侧各 4 槽：头像色块、HP 条、pips、charm/ward 图标、DOT 计时、stun）、手牌区（8 张，可用高亮，显示 pipcost / 伤害区间 / 命中）、点卡 → 选目标 → 结算动画（CSS）、战斗日志、结束面板
- [x] 批量页 `view_batch.js`：矩阵配置（系 × 系、模式、等级段、场次、seed）、进度条、5×5 胜率热力图、按模式对比、卡牌统计表、导出 CSV / JSON
- [x] 数值面板 `view_params.js`：全局常量、按系系数滑杆、单卡表格（筛选 / 编辑 damage_min/max / pipcost / accuracy）、与基线 diff 高亮、导入 / 导出 JSON、"重跑矩阵"
- [x] AI 建议页 `view_advisor.js`：见 Phase 5
- [x] 移动端适配（≥ 375px 宽可用）

### Phase 5 — 数值建议
- [x] `sim_tuner_core.js`：目标函数 Σ(winrate − 0.5)² 覆盖所选矩阵；对 `perSchool.{hp,damage,heal,accuracy,powerPip}` 乘子做坐标下降（步长自适应，迭代与每次评估场次可配，固定 seed 降噪）；输出参数改动清单 + 前后矩阵对比；可一键写回 BalanceParams
- [x] `llm_advisor.js`：`buildPrompt(stats, params, manifest)`（中文，附带每系代表卡与当前系数）、`requestAdvice({endpoint, apiKey, model, prompt})`（OpenAI 兼容 chat/completions，凭据存 localStorage）、解析返回中的 ```json 参数块供一键应用
- [x] 建议页展示：当前矩阵摘要、调参器运行 / 停止 / 应用、LLM 设置与结果 Markdown

### Phase 6 — 验收与文档
- [x] `npm test` 全绿；确定性测试覆盖引擎 + 批量聚合
- [x] 用真实 kids / teen 数据各跑一次 1v1 五系 × 1000 场，把矩阵与未支持卡占比记入 [qa-report.md](qa-report.md)
- [x] 填写 `architecture.md` 的"意图 vs 现实"小节
- [x] 仓库 wiki 收口：新增 `docs/aries/haqi-combat-sim.md`，并在 `docs/CODEMAP.md`、`docs/TOPIC-INDEX.md` 各加一行（按根 AGENTS.md 要求）

### Phase 7 — 卡包 / 配卡 / 决斗圆盘演示（2026-09-17 追加需求）
- [x] `BalanceParams.global.deckCapacity` / `deckEachCapacity` / `deckPresetCopies`（Lua 卡包道具 stats[167]/[170]；默认 40 / 6 / 3，初始卡包 kids 14/3、teen 18/5 可手动改回）
- [x] `clampDeck` 裁剪 + teen 同名技能共享上限；预设卡组改为轮询填充，随容量自动缩放
- [x] 弃牌与出牌解耦（可弃牌 + 跳过）；无手牌跳过区分 `no_cards` / `deck_empty`；双方打空提前判平
- [x] 批量统计：无牌跳过率、单位打空卡包比例、双方打空判平比例；批量页可复用对战页配卡
- [x] 对战页配卡面板（搜索 / 份数 / 预设 / 保存 per version+school）
- [x] SVG 决斗圆盘：站位点、头像 / HP / 魔力点（普通蓝 / 超级金 + 图例）/ 卡包剩余 / 状态徽标；中央卡牌面板 + 施法者→目标能量束 + 飘字；状态明细板
- [x] 事件流分步动画（慢 / 正常 / 快 / 无），跳过动画，跨页切换接回对局
- [x] 测试：`clampDeck`、容量裁剪、打空 → `deck_empty` → 提前平局、弃牌生效时机

## 关键设计决策

1. **纯 JS 移植而非桥接 NPL**：`combat_server.lua` 依赖 GSL 与 PowerItemManager，桩掉成本高且不利于浏览器内批量模拟；移植时以行号对照保证可审计。
2. **无构建、无框架**：与 MagicHaqi 一致，`*_core.js` 可在 Node 直接测试。
3. **随机数集中**：Lua 端 `math.random(0,100)`、`(0,1000)`、`(1,10000)` 等阈值语义在 `rng_core.int(min,max)` 中保持闭区间一致。
4. **参数覆盖层**：数据集只读；所有可调项在 BalanceParams，便于 diff、导入导出、调参器与 LLM 共用。
5. **Bot 策略复用官方 CSV**：`Aggressive{School}.csv` 是线上 Autobot 的真实策略，用它可让胜率矩阵更接近真实玩家 / 机器人对局；`SimpleBot` 作为无数据时的后备。
6. **未支持类型透明化**：manifest 与批量报告都展示未支持卡的数量与出现频次，避免结论被"静默 Pass"污染。

## 风险与对策

| 风险 | 对策 |
|------|------|
| 卡牌 type 长尾多（60+），第一批覆盖不全导致某系胜率失真 | 报告中标注每系"未支持卡占卡组比例"；优先补占比最高的 type |
| 真实装备属性缺失 | 用 `MobStatsByGearScore` 区间 + 手动滑杆；在报告里声明"装备段" |
| Lua 中 kids / teen 分支差异多，移植遗漏 | `lua-mapping.md` 表逐项打勾；测试用例按 version 成对 |
| 批量模拟噪声掩盖小幅参数变化 | 固定 seed、Wilson 区间显示、调参器每次评估场次可调 |
| LLM 建议不可控 | 仅作为建议展示，应用前经 diff 确认；调参器结果为主 |

## Phase 8 — Haqi.html 初心之旅（2026-09-17）

- [x] 独立入口，Canvas 2D世界/法阵与HTML界面，保留现有模拟器
- [x] 原63000–63013、五系、15居民、7种遭遇、本地原版美术、新绘制精灵
- [x] 纯规则模块：任务/装备强化/配卡/成长/宠物、碰撞寻路与交互
- [x] 单独PvE回合控制，原AI序列与HP基因，可重现的决定重演
- [x] 独立版本化存档、JSON导入导出、奖励去重、失败/撤退安全点
- [x] 限制Lua字面量导出、完整CDN条目准备、本地解码与引用验证
- [x] 五系完整章节自动验证，浏览器验证与修复记录见qa-report

范围与明确改编见 [adventure.md](adventure.md)，不将此章节表述为完整MMO移植。

## Phase 9 — WebP、Keepwork CDN 与云端旅途（2026-09-17）

- [x] 90张图片无损WebP，保留alpha/像素/尺寸，Git保留本地副本
- [x] 91项内容哈希命名永久CDN文件；全部实际GET、CORS、字节/哈希核验
- [x] localhost默认local，线上默认Keepwork CDN，可显式测试两种模式
- [x] 按需SDK、中文登录、本地自动存档、手动云端检查点、预览后恢复、替换前备份
- [x] 独立workspace与唯一文件，远端写后读验证，账号变化/本地更新/失败写入保护
- [x] 69个自动测试，实际CDN浏览器解码，桌面/手机菜单、战斗云端重演与本地重载
- [ ] 真正登录Keepwork账号后的服务端保存/另一设备读取验收（无测试账号；真实SDK已验证加载、中文登录及取消，读写流程使用隔离模拟SDK）

未部署网站；资源上传不等同于网站上线。没有引入额外前端库或改变PvP/PvE数值。


## 2026-09-18：全卡美术替换

- [x] 225个kids基础技能/701条定义共享主体，18张常规图集与6张专属九帧图集生成并上传永久CDN。
- [x] 新图集≤100KB，背景<24KB；接入冒险卡包/手牌/演出、特效工坊、全卡检索预览及kids模拟器手牌。
- [x] 全量引用、渲染命令、帧裁剪与远端资源验证；92项工作区测试通过。
- [ ] 当前任务浏览器连接失败，页面视觉/动画连贯性仍待人工验收。

## 2026-09-18：抱抱龙、全宠物与50级商店

- [x] 359项来源宠物、三色初始抱抱龙、四阶段与独立等级卡包
- [x] 四卡位、同位附卡/跟随、独立宠物AI、捕获及战斗重演
- [x] 全局商店、等级科技树、1008件装备、补给与1–50级试炼
- [x] HP持久化、在线饥饿/自动进食、离线仅回血、旧存档迁移
- [x] 家园正式入口与版本化联动接口（双向同步不在本期）
- [x] 359张原CDN WebP本地归档、CORS/哈希/体积核验

具体规则及验收范围见[pets-and-shop.md](pets-and-shop.md)及QA报告。帕鲁式深化与 VIP 口粮/治疗见计划需求 [pets-vip-innovation.md](pets-vip-innovation.md)，**尚未实现**。

## 2026-09-18：Vite发布流程

- [x] 参考Maisi/MagicHaqi加入四入口Vite构建，保留原生源码运行。
- [x] 内容哈希发布计划、复用Maisi七牛上传器、逐文件CDN哈希与CORS核验。
- [x] 打包批量Worker支持跨域HTML/CDN部署；新增发布回归测试。

操作见[部署说明](deployment.md)，实际首次发布结果见qa-report。

## 计划需求（2026-09-18，李西峙）

四项产品需求已写入文档，**尚未实现**。调查范围为本仓库 `HaqiCombatSim/` 与兄弟目录 `HaqiCombatEmulator/`；不编造生产 API。

### 拉取魔法哈奇真实用户数据

- 2026-09-18 本轮用户明确身份目标为原版《魔法哈奇2009》MMORPG；先核查装备、镶嵌宝石、强化是否完整进入新系统。核查结论：**尚未完整接入**，详见 [player-import.md](player-import.md#装备导入前置核查2026-09-18)。
- [x] 对照原版装备实例与服务端属性汇总，核查现有导出及战斗入口
- [ ] 补全装备实例模型、宝石、全装备强化、套装配置与属性换算，并逐来源验证
- [ ] 核验原版账号鉴权和背包接口的浏览器接入条件，再实现登录与角色导入
- [x] Keepwork 每账号最多5个独立主角；本地角色与备份按账号隔离、旧存档迁为访客角色，网页存档导入共用名额
- [x] 登录默认继续最近成功进入的角色；支持切换/新建，角色项显示名称、外观、当前等级和系别
- [x] 新建流程拆为起名字、选择抱抱龙、选择系别；抱抱龙页提供小字入口“导入魔法哈奇角色”，明确提示尚未接入，不占名额
- [x] 系别页提供每系3个代表技能动画，可切换、暂停、重播；无角色直接新建、单角色直接继续，移除冒险文件导入/导出并修复小屏裁切
- [ ] 真实Keepwork账号跨设备验收（目前云端读写、冲突、账号切换通过模拟SDK测试）
- 目标：卡包、装备、VIP 及相关成长字段进入模拟器单位规格（`unitSpec` / `playerSpec`）。
- 已有：静态 kids/teen 快照、gsid 尽力映射、装备 stat 表、冒险本地成长与装备面板、Keepwork 云检查点登录（与哈奇角色无关）。
- 没有：原服/家园玩家库接口、本引擎 VIP 属性、默认卡组中的会员卡。
- 鉴权、接口、线上字段契约、玩家缓存：代码无法回答，见 [player-import.md](player-import.md)。
- [x] 身份归属：Keepwork 网页账号下管理角色；原版2009账号用于数据导入
- [ ] 核验原版导入授权协议与字段白名单
- [ ] 只读快照契约（无凭据入库）并对接到 `unitSpec` / `playerSpec`
- [ ] 对战页「从快照开战」，失败不阻断本地 Bot 对战

### 异步 PVP（非实时对战）

- 目标：双方不必同时在线；复用现有 `free_pvp` 与 1v1–4v4 人数，不新开伤害公式。
- 已有：本地 1v1–4v4 人机、半回合状态机、事件流动画、PvE 决定重演；兄弟项目有 PvP `exportReplay`（本目录未共用）。
- 没有：匹配队列、选牌/整场过期时钟、本目录的 PvP 战报导出、网页排行榜。
- 匹配规则、过期判负/托管、回放存哪、排行榜是否独立于 Lua 排位分：见 [async-pvp.md](async-pvp.md)。
- 运营备注：网页包继续作为上线前试验新技能 / BalanceParams 的场所（对战/批量/数值面板已有）；如何把结论搬回原服未定。
- [ ] 1v1 异步行动形状对齐 `playTurn`
- [ ] seed + 开场规格 + 逐步 picks 的可导入检查点
- [ ] 匹配与超时作为 IO 层；4v4 多人提交放在 1v1 之后

### 帕鲁式宠物创新与 VIP 口粮 / 治疗

- 目标：在现有冒险宠物上朝《帕鲁世界》方向创新；治疗或口粮可以 VIP 化。
- 已有：359 种捕获/四卡位/饱食/营养餐/非战斗回血、奇豆商店、空的「升级会员」按钮；章节口粮 17172 只喂咕噜噜。
- 没有：会员档位、口粮/回血门槛、据点劳动、繁殖、同种多只、VIP 商店接口。
- 做到哪一层帕鲁化、锁哪些资源、会员身份与导入 VIP 是否同一套：见 [pets-vip-innovation.md](pets-vip-innovation.md)。
- [ ] 产品确认帕鲁化范围（加深现有养成 vs 新开劳动/据点）
- [ ] 产品确认 VIP 锁的是口粮、治疗还是两者，以及身份源
- [ ] 身份字段进存档后再改 `buy` / `pet-feed` / `tickCare`；无会员时本地养成仍可玩

### 卡牌多语言文案与海外运营

- 目标：卡面标题/说明可出多语言，海外不必等国内版号、也不必重绘卡图。
- 已有：`CardRenderer` 程序绘制标题与 `cardDescription`；kids `card_names.json` 661 条中文名；图集不含印刷字。
- 已有界面层：冒险固定句子可切换语言，词典在 `data/adventure/locale/`，见 [locale.md](locale.md)。对战页不加载这套词典。
- 没有：英文卡名表、teen 卡名、charm/ward 译文、带数字的卡面说明译文、对战页中文名（仍用 key 残段）。
- 第一语言、翻译权威、是否含 UI/对白：见 [card-i18n.md](card-i18n.md)。本项不做完整 i18n 实现。
- [ ] 产品确认第一语言与范围（仅卡面 vs 含 UI）
- [ ] 生成说明模板与 `SCHOOL_NAMES` 可抽 locale；kids 卡名表可并列语言文件
- [ ] 对战页显示名与冒险卡包对齐；不引入运行时翻译服务

## 2026-09-19：强化任务与原版配置
- 已增加背包强化标签、任务63007直达法杖详情、任意已收录且有原配置装备的强化。
- 已接入468个原配置GSID、六种强化属性、按配置限制等级和材料、成功操作触发79016；保留GSID存档限制，高阶未收录材料明确提示。核对来源见lua-mapping.md。


## 2026-09-19：世界地图传送

- [x] 右上角六岛地图、选岛详情、等级锁与当前位置
- [x] 免费跨岛传送、回程传送门、岛屿地表、存档和任务追踪兼容
- [x] 4项传送专项测试、既有217项回归及生产构建通过
- [ ] 浏览器桌面/手机视觉验收（工具连接超时）
- [ ] 远方四岛专属原版NPC、怪物和任务（当前仅开放探索）

## 2026-09-20：大型岛屿探索

- [x] 哈奇岛5600×4400空间骨架、八区导览、桥梁/河流碰撞、既有居民/遭遇重新分布。
- [x] 有界地面分块缓存、局部对象查询、长距离道路寻路与旧坐标迁移。
- [x] 五主岛原版俯视地图校验归档，整理分区美术/剧情可复用流程，见 island-exploration.md。
- [ ] 哈奇岛原版专属地标精细美术、完整城镇/游乐设施及新剧情。
- [x] 火鸟、寒冰、沙漠、幽暗岛按原图关系分别扩建，六岛源文件/运行时 JSON 拆分，共用地形/森林/道路/天气生成配置。
- [ ] 四岛原版专属居民、怪物和完整剧情接入。


## 2026-09-22：副本小世界与自动云存档

从原版世界表、竞技场出生点及怪物模板导出92个kids/通用实例世界、315组出生点、372份怪物模板，保留源文件哈希和原XML。61个世界可进入二维投影地图，35个世界全部组可挑战，共244组当前可开始并重演；其余按组显示缺失模板、卡牌、目标或怪物先手/场景脚本原因。31个无可用出生点或缺配置的实例仅收录目录。地图支持多怪组、原卡位、清怪进度、继续/重开、出口与返回原岛位置，清完显示通关；地图投影与通用学系外观为Web适配，不宣称原三维地图、机关、过场、难度、门票、宝箱/服务器奖励完成。

右上角“副本”替代云存档，设置保留云端恢复入口。升级、战斗结算、任务/重要操作、进入退出副本请求后台保存；相邻操作合并，30秒重试间隔，普通游玩10分钟兜底。保留访客本地存档、账号隔离、远端核验和冲突保护；不自动弹登录、不覆盖远端冲突。

制作入口见[dungeon-authoring.md](dungeon-authoring.md)，可编辑config/dungeons/custom.json增加原创世界，再运行npm run export:dungeons；自动生成[dungeon-catalog.md](dungeon-catalog.md)逐组来源与迁移状态。隔离预览tests/fixtures/dungeons.html不读写玩家存档。


### 2026-09-22：副本改为整包按需预加载

按用户澄清，保留一个完整dungeons.json，不拆成每副本小文件。启动只带轻量目录；首次进入任意副本整包加载并缓存，恢复正在副本中的存档也先预加载。现有Vite构建同时生成主站与独立副本JSON，无需额外打包命令；完整副本JSON排除出主adventure.json。原始配置和原创入口不变，详见dungeon-authoring.md。
