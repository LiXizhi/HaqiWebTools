# 2026-09-18 开发日志

## 计划需求文档：真实用户数据与异步 PVP

产品负责人李西峙当日经钉钉告知 Lily 两项后续需求。本仓库仅补充文档，不实现功能、不编造生产接口：

- `docs/player-import.md`：对照现有导出器、装备 stat、冒险存档、Keepwork 登录与兄弟项目 VIP 手工计算；列出卡包/装备/VIP 已有字段与缺失项；鉴权、接口、线上契约、玩家缓存标为开放问题。
- `docs/async-pvp.md`：对照 `free_pvp` 1v1–4v4 人机、Lua 30s 选牌时限（引擎未实现）、`maxRounds` 平局、PvE 决定重演与 Emulator 战报；匹配与异步过期规则标为未知。
- README / plan.md / design.md / AGENTS.md 增加入口。核对范围：`HaqiCombatSim/` 与 `HaqiCombatEmulator/`。

## 计划需求文档：帕鲁式宠物 / VIP 口粮 与 卡牌多语言

同一钉钉后续补充两项，仍只入库文档、不写运行时代码、不编造 VIP 或翻译接口：

- `docs/pets-vip-innovation.md`：对照已交付的 359 种捕获/四卡位/饱食/营养餐（`FOOD_ID=990001`）与章节口粮 17172、空的「升级会员」按钮；帕鲁式劳动/繁殖/同种多只以及口粮/回血 VIP 门槛标为开放问题。
- `docs/card-i18n.md`：卡面标题与 `cardDescription` 由 `CardRenderer` 程序绘制；kids `card_names.json` 661 条中文、约 40 张系统卡无库名；teen 无卡名表；locale 切换未做。记录模板分表 / 卡名分文件等选项。
- `docs/async-pvp.md` 增补运营备注：排行榜（与 Lua 排位分不必同一套）；网页包继续当上线前试验新技能与 BalanceParams 的场所（`#params` / `#batch` 已有）。
- README / plan.md / design.md / architecture.md / AGENTS.md / pets-and-shop.md / card-study.md / adventure.md 增加入口。

核对范围仍为 `HaqiCombatSim/` 与 `HaqiCombatEmulator/`；无密钥、无新 API。


## 冒险界面仙豆与右上角功能区

仙豆移入左上角角色信息卡，存档提示随角色信息显示。移除常驻小地图和控制器中的定时绘制；地图仍可通过右上角按钮打开。右上角提供葫芦签到预留入口、地图、云存档、设置；签到明确显示未开放并禁用，未增加奖励或存档规则。底部保留任务、卡包、背包、宠物。窄屏采用两列功能按钮，任务追踪区下移避让。

验证：JS语法检查通过，npm test 82项通过。浏览器连接返回 nodeRepl.fetch request failed，未完成浏览器视觉验收。未改动已有未跟踪文件 data/adventure/skill-art-plan.json。

## 角色装备与背包系统

- 增加角色装备入口（头像/R），背包入口保持 B/I；共用原纸张色、绿色按钮、角色图和17件章节装备图标。
- 新装备视图支持五部位、搜索、筛选、穿戴/替换/卸下、属性变化、原附加卡牌、来源任务、强化成本/上限/不足提示；手机属性区折叠，操作后保留滚动位置。
- 核心新增可说明原因的条件检查和卸下动作；库存与强化等级不丢失，卡包缩小时仅裁剪配卡，战斗中拒绝变更，保持 v1 存档兼容。
- 新增6项装备测试，覆盖五槽位/存档/实际战斗属性、预览无副作用、卡包裁剪、非法操作原子性、强化累计和战斗重演。
- 工作区并行存在未完成的技能美术改动，未纳入本次提交。该工作区 npm test 为88通过、1失败（缺少 skill-art.json）；check:adventure 校验92项资源通过。使用 HEAD 基线叠加本次改动的隔离副本执行 npm test，88/88通过，避免把其他任务的未完成资源纳入装备验证。

浏览器补充验收：完整隔离游戏在手机375px通过新建角色、头像入口、真实存档导入、卸下翡翠口袋、14/14配卡跳转；电脑检查原图、属性对比、法杖穿戴/强化/卸下。详情见qa-report。早期工作区因并行美术清单缺失无法启动，后续结果见收尾复验。

收尾复验：并行技能美术工作已补齐 skill-art.json，当前整个工作区 npm test 92/92通过，主工作区 Haqi.html 已在浏览器确认恢复角色创建页面。先前缺失清单的阻塞已解除；该美术工作仍不纳入本次装备提交。

## 全卡主体与高费专属动作

内置imagegen参照原卡面完成18张共享主体图集及6张专属九帧图集，覆盖kids225个基础技能和701条卡牌定义。所有新WebP严格≤100,000字节，背景保持<24,000字节；总计2,185,706字节，均上传永久Keepwork CDN并核验远端哈希、CORS、透明边缘和裁剪。最终提示词与来源在 skill-art-prompts.json、skill-art-plan.json 和 skill-art.json 中保留。

共享渲染模块替换冒险卡包/手牌及演出，特效工坊按需加载，全卡页支持筛选/搜索/分页，模拟器kids手牌异步补图。六套专属为海狮冰剑、火魔掷石、旋风爆击、重生、蝠王吸魂、梦龙极光符文。数值/文字独立绘制，变体共享美术，不更改数值机制。

工作区92项测试通过；生成图/压缩图已查看。当前任务浏览器工具连接失败，页面视觉和连贯性尚待人工验收。并行装备改动单独保留，不纳入本次美术提交。

## 图集粒子层、合成音效与增量美术流程

用户反馈主体图集占比过高、缺少原版丰富粒子。移除成功绘制图集后的提前返回，保留图集并继续语义攻击/辅助/secondary演出，补五系不同形状的蓄力粒子。Web Audio按学系和技能类型合成短音效，接入两个预览和冒险，独立开关、手势解锁、暂停/定位/后台停止及24声源上限，不新增图片或音频依赖。

新增docs/art-pipeline.md与制作单模板，覆盖原图/数据参考、提示词、共享主体/专属九帧、100KB压缩、实际CDN目录、粒子/声音配置和验收。prepare_skill_art.py增加--reuse-existing，未来只需提供新图源，既有图集按来源/布局/哈希复用。

96项测试和701条效果覆盖审计通过；空源目录增量复用验证无元数据变化。浏览器连接失败，实际画面和听感尚未验收；不把模拟Canvas/Web Audio测试视为实机验收。

## 2026-09-18：技能命中受击抖动恢复

实战根据当前cast后实际正伤害事件，在普通/召唤技能命中节点触发220ms受击后仰与抖动，不再等整段施法结束。已播放过的受击在后续扣血展示中去重；未命中、零伤害、治疗不触发，周期伤害独立播放，致死仍进入死亡动画。HaqiCards补充受击靶标，按同一命中节点预览抖动；减少动态模式不位移。

完整测试97项通过；随后补充周期伤害边界与多目标测试，actor_animation专项3项通过，JS语法检查通过。未改伤害公式和真实结算时序。本轮未完成浏览器视觉验收。

## 2026-09-18：召唤主体固定法阵中央

召唤类型及专属主体固定中央，不再随攻击滑向敌人；主体脚下法阵同样锚定中央。为巨岩之怒、蛇女雷暴、火狼咆哮等24个角色/生物主体补充subjectPlacement=center，保留原攻击类型与命中时序。群体攻击只绘制一个中央主体，粒子继续分别命中目标；减少动态模式同样居中，普通护盾等仍在目标位置。美术流程文档补充定位字段。

npm test 99/99通过，新增正反方向、不同阶段、简化模式、群体单主体/多目标命中及普通护盾定位回归。未改战斗公式。本轮未完成浏览器视觉验收。

## 2026-09-18：统一卡牌渲染与恢复四角图形

新增CardRenderer类统一卡面布局、学系图标、费用/冷却圆章、类型徽标、说明与品质边框；移除学系首字和攻/愈/辅文字替代，复用早期底图样张的Canvas图形。skill_art资源层委托渲染类，旧对照样张也接入；冒险手牌/卡包/装备附卡不再通过DOM重复覆盖角标，保留完整无障碍说明及悬停说明。

既有99项测试通过，新增四角图形、X费用、冷却底座、纯背景和状态平衡回归1项通过，JS语法检查通过。本轮未完成浏览器视觉验收；并行宠物/商店改动不纳入此提交。

## 2026-09-18：攻击徽标改为拳头

CardRenderer右侧攻击徽标由斜剑改为握拳轮廓，包含指节、收拢拇指与腕部，沿用圆章和配色；所有共用卡面同步生效。卡面回归1项及JS语法检查通过。


## 设置中的属性编辑器

新增设置调试入口与适配PC/手机的分类搜索表单。支持等级/经验联动、货币、物品数量、法杖强化、卡牌份数及原章节宠物经验；改动预览、撤销草稿、自动备份和恢复。下降等级或删除装备后自动修正穿戴与配卡，整份结果通过既有存档校验后才保存。没有修改随机种子、任务账本或战斗数值公式。

新增7项测试通过；工作区115项全绿，隔离本次改动的版本107项全绿。浏览器完成等级/货币修改、重载保持、负数阻止、备份恢复与375px手机无溢出验收。保留并行宠物/商店工作，不纳入本次提交。

## 抱抱龙、全宠物与50级商店

实现359项MagicHaqi宠物导入，21,368,922字节原WebP全部通过200KB上限与CORS核验。创建角色选三色抱抱龙；四阶段、独立卡包、四卡位/同位附卡、自动战斗、捕获、商店和1–50级试炼接入。1008件装备可购买，301件排除原因入报告。加入新版存档迁移、HP恢复与自动喂食、家园协议和新规则测试。保留并兼容同期设置调试编辑器改动。


### 技能预览宠物受击可见性

- 预览受击动作延长至360毫秒，命中时将宠物绘制到特效上层，避免光效遮住闪白、后仰和回弹；普通/召唤技能沿用各自命中时点，友方技能不触发。
- 验证：actor_animation测试4项通过，spell_preview语法检查通过；未完成浏览器视觉核验。

## 商店装备图标与宠物详情弹窗

- 为 1008 件商店装备补齐原版图标映射，复用到背包与穿戴栏。845 张原图按 manifest MD5 校验后转换为 63 张透明无损 WebP 图集，每张低于 200KB；懒加载并在图片就绪后重绘。28 件缺失专属原图的装备使用原版同部位示意图，清单记录替代来源，商店明确标注。
- 新增 shop-icons.json，保留原始清单条目、原图 SHA-256、图集 SHA-256、裁剪坐标、本地路径和已验证的永久 CDN URL。prepare_shop_icons.py 可重新生成并核验 CDN 的字节和 CORS。
- 商店和宠物养成的“四阶段与卡片”统一打开原生独立 dialog；桌面四列、手机两列，正文独立滚动，关闭/Esc 返回原视图，保留商店状态与焦点。
- 验证：现有 119 项测试通过，新增装备图集完整性测试通过；1280px 与 375px 浏览器核验装备图标、宠物四阶段、关闭返回、无水平溢出，浏览器无错误。63 张 CDN 图集 SHA-256/CORS 全部通过。
- 本次修改依赖工作区尚未提交的宠物/商店扩展，保留该扩展的其他修改，不将其未经完成的工作合并提交。
最终工作区 npm test：122 项全部通过（含同期新增测试）。

### 左上角状态栏排版

- 隐藏正常状态的“已存档”，保存失败仍显示导出备份提示。
- 生命值改为独立绿红血条：绿色为剩余生命，红色为损失生命，条内显示当前值 / 最大值；随角色恢复同步更新。
- 奇豆独立排在血条下方，头像不被压缩，避免窄屏文字挤出面板。
- 验证：npm test 全部 122 项通过；浏览器 375px 宽度下状态栏无横向溢出，血条和奇豆均在面板内部，正常存档提示隐藏。

## 商店顶部分类标签

按截图将分类下拉框替换为标题栏“装备 / 宠物 / 补给”按钮，当前分类高亮。桌面与标题同行，手机在标题下方三等分排列，列表滚动时仍可切换。仅装备显示部位筛选，补给隐藏学系；切换分类清空不适用的筛选并回到首页。浏览器验证三分类切换、装备部位筛选后切回宠物、375px无横向溢出；模块语法检查通过。改动保留在尚未提交的宠物商店扩展内。

### 左上角状态栏进一步精简

- 去掉状态栏角色形象和奇豆数量，保留学系、等级、经验及绿红血条。
- 名字旁加入“升级会员”按钮；名字仍可打开角色装备。当前尚未接入会员状态与付费入口，点击按钮明确提示“会员升级暂未开放”。
- 验证：JS 语法检查、git diff --check 通过；浏览器桌面及 375px 窄屏核验，头像和货币元素已移除，标题行无溢出，会员按钮反馈正常。

## 宠物扩展最终验收

补充捕获优先于队友AI、捕获结果/消耗显示、BalanceParams捕获参数随检查点保存、奖励阵容校验、饥饿补给提示。完成123项自动测试、原资源检查与桌面/390px手机四卡位实战、配卡、喂食及HP持续恢复验收。完整结果见qa-report。同期商店图标、四阶段弹窗与标题栏分类已整合，不影响规则模块隔离。

### 定时签到奇豆奖励

- 启用签到入口与奖励弹窗：首次可领取，默认每 5 分钟 100 奇豆，离线计时，最多保留一份，领取后重新计时。
- 倒计时每秒更新，到时启用领取按钮；领取走纯规则 action，立即保存，阻止提前领取和连续重复领取。
- 参数集中在 BalanceParams.checkin；存档保存 checkin.nextAt，兼容缺失该字段的旧存档，导入时校验时间戳。
- 验证：npm test 全部 126 项通过，包含边界时间、重复点击、时钟回退、存档往返、离线不累积及参数覆盖；浏览器实测 0 → 100 奇豆、倒计时 05:00、等待按钮禁用及窄屏弹窗。

### 按原版改为五个米酒葫芦

- 对照原版 Lua/MCML，重做为五个可直接点击的葫芦，清楚区分可领取、剩余时间、已领取。
- 采用每日累计在线 1/15/30/60/90 分钟，默认各 100 奇豆，每档独立领取；BalanceParams 集中配置，v2 存档兼容旧计时器，不撤回既有货币。
- 原生 SVG 绘制葫芦，不增加图片网络依赖。桌面五列、手机三列换行。
- npm test 126/126 通过，覆盖严格阈值、跨档领取、重复领取、刷新、离线不计时、日切、时钟回退与旧存档迁移。

## 随行宠物独立移动

- 新增 `adventure_companion_core.js`：宠物维护独立的临时坐标，超过100像素开始跟随，靠近至46像素停下；远距离加速追赶，复用地图寻路与碰撞。
- 闲时以独立种子随机选择主角周围34–76像素的可行走位置，走走停停；不消耗战斗随机数、不写入存档。切换地图、随行宠物或读取存档时重新初始化，主角瞬移后重新靠近。
- 地图渲染按宠物真实位置参与深度排序，随水平移动左右镜像，并增加落地阴影和轻微跳动；减少动态效果设置下取消跳动。
- 验证：原有 `npm test` 126项通过，新增3项测试覆盖游走确定性、双向朝向、追赶与停步、绕障和瞬移；`git diff --check` 通过。浏览器工具返回 `nodeRepl.fetch request failed`，未完成实际画面验收。

## 2026-09-18：战斗站位、重叠手牌与收发牌动画

- 法阵站位符文、角色脚底和点击目标共用椭圆坐标；高度不足时整体等比缩放，宽屏和手机保持对齐。
- 默认展开一张可用手牌；窄屏采用重叠排列，完整预留选中牌与末张牌宽度，八张牌无需横向滚动。可查看魔力不足或冷却中的牌，但不能施放；弃牌后自动选择下一张。
- 首次发牌依次滑入，出牌复制已绘制卡面飞向法阵；演出期间保留剩余手牌，结束后仅新补的牌播放入手动画。系统减少动态效果偏好会关闭卡牌动画。
- 根据底部按钮实际换行高度避让手牌及法阵；低高度横屏采用左法阵、右手牌。战斗数值、随机数和存档规则未因本次显示改动而改变。
- 浏览器使用独立端口和测试存档检查 320×568、390×844、844×390、1440×900；八张手牌、默认选中、出牌期间七张/禁用输入、次轮八张且仅一张入手已核验。横屏页面尺寸与视口完全一致；最小手机手牌与按钮间距12px。
- 最终验证：npm test 129/129通过；三个修改的JS文件语法检查通过。工作区同时存在签到等其他改动，本次未提交或推送混合变更。

### 签到时间只显示分钟

- 签到入口和葫芦剩余时间改为整分钟显示（向上取整），累计在线显示已满分钟（向下取整），去掉秒数跳动。
- 到时仍自动切换为可领取；未改领取计时规则。JS 语法检查通过。

### 放慢宠物移动跳动

- 宠物移动时的跳动相位系数由0.14降为0.10，同速下跳动频率降低约29%，移动速度和跳动高度保持原值。
- 验证：宠物移动专项测试3项通过。

### 选牌切换动画补充

- 切换选中手牌时记录各牌当前屏幕位置，再对新布局播放300–340ms过渡：旧牌回落、新牌抬起抽出、邻牌平滑让位。连续点选从当前动画位置接续，不重播发牌、不改变卡牌顺序或战斗状态。
- 保留减少动态效果偏好。浏览器隔离存档实测487×844下由末张切换至中间牌，旧牌归位且新牌唯一展开；JS语法检查通过。

## Vite构建与CDN发布

- 按用户新要求参考Maisi/MagicHaqi接入Vite 7.3.6，四个HTML入口共享打包模块，保留运行时JSON和本地WebP/Ogg；源码依旧支持普通HTTP静态服务。
- 新增dev/build/preview/plan:release/upload/verify:release命令；全产物内容哈希目录、复用Maisi既有七牛凭据流程、GET字节哈希及CORS全量核验，验证后才输出带base的正式入口。
- 生产Worker通过Vite内联Blob打包，避免Keepwork托管HTML与CDN脚本跨域时Worker构造失败；调用方自定义workerUrl保留。
- 基线129项测试和新增发布专项测试通过；本地四入口加载正常，1v1完成、批量7500场完成且无Worker回退告警；npm审计零漏洞。CDN结果另记qa-report。

### 发布资源范围修正

- 用户明确网站发布不得重复上传美术；最终上传清单改为HTML/JS/CSS/JSON，52文件共5,595,921字节。WebP/Ogg只保留本地dist预览，上传器仅接收白名单暂存目录。
- 发布哈希只涵盖代码和配置；美术清单已有的永久CDN地址维持不变。专项测试确认本地美术副本不进入清单、不改变版本哈希。
- 用户澄清前首次783文件完整上传已完成；后续以仅代码/配置版本为正式入口，未擅自删除旧CDN目录。

### 最终默认CDN与无美术构建

- 用户进一步要求dist也不保留美术；Vite现在仅复制data JSON，dist只有HTML/JS/CSS/JSON，WebP与Ogg数量均为0。
- 资源策略在所有域名（包括localhost/loopback）统一默认CDN；assetUrl和美术清单校验默认同样为CDN。源码显式离线选项保留，构建发布不依赖归档图片。
- 最终版本0382d8b8190ed7eb：52文件，逐项HTTP/CORS/SHA-256验证通过；不带资源参数的本地构建冒险、全卡库、特效工坊加载成功。

## Vite运行时美术JSON精简

- 新增scripts/package_runtime_data.mjs：构建时对美术JSON执行显式字段白名单投影，所有JSON紧凑序列化；保留运行时校验、CDN、图集裁剪、九帧动作、旧卡对照与缺图提示字段。
- assets/media/skill-art/card-frames/shop-icons/pets.art删除未读元数据；原data清单完整保留。四份未被运行时读取的开发计划/报告不再进入dist。
- dist从52文件5,595,868字节降至48文件3,298,097字节（减少41.1%）。skill-art 203,125→84,048；shop-icons 498,332→69,473；media 69,423→44,655字节。
- npm run build成功；npm test 132/132。新增全部技能裁剪/动画帧、CDN地址、原卡对照、宠物非美术数据、清单校验、输出压缩及源码不变检查。此轮未重新上传CDN。

## 发布后同步Maisi入口

- 用户要求发布成功后检测本机lxzsrc/maisi仓库，将四个Haqi*_v1.html同步到maisi/maisi/webgames/MagicHaqi/release。
- 增加共享Maisi路径发现，支持MAISI_ROOT和祖先目录下的maisi；仅CDN核验成功、正式HTML生成后复制，逐字节验证，不提交/推送Maisi仓库。
- 回归测试验证仓库缺失跳过、未核验不复制、同名覆盖、只复制指定四入口、保留MagicHaqi原文件及显式路径。node --test tests/release.test.mjs两项通过；本轮没有触发实际上传或复制旧发布入口。

## Vite发布JSON合包

- 发布时将精简后的32个JSON合为5包：datasets（仅manifest索引）、adventure、kids、teen、sample。源码data目录保留原结构。
- 所有浏览器JSON请求统一到runtime_data.js，构建开关只在Vite生产构建启用合包；dev与普通源码HTTP服务仍逐文件读取。data_core只保留注入式加载和纯数据处理。
- 按需下载、并发去重、失败清除缓存允许重试；每次返回独立副本，避免规范化/扩展修改污染缓存。索引独立避免模拟器发现数据集时下载全部版本。
- npm run build与发布dry-run通过：21文件/3,299,345字节，只有5个JSON，无WebP/Ogg。npm test 136/136；涵盖所有数据集与源文件结果一致、真实冒险加载函数只请求两包（图片IO模拟）、CDN图片策略、缓存隔离和失败重试。本轮未上传CDN。
