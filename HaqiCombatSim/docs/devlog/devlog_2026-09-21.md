
## 战斗左下角文字避让

### 魔法口袋领取闭环

- 十项奖池物品定义随导出器生成，保留已有定义；道具使用玩法仍未开放。
- 会员免费领取页增加次数和按钮，复用会员刷新与persistReward保存通道；同角色同周同次序确定性抽奖，失败重试不换奖。
- 口袋记录嵌入magicStarClaims，普通奖励不覆盖；保存失败、旧标签页不发奖，云端快照保留次数。
- 313项全量、13项奖励测试及Vite通过；手机隔离DOM领取2→1，无横向溢出。非真实账号/触屏验收，未改玩家存档或发布。

### 第三组十轮：长期系统首批接入

- 魔法星六项战斗属性进入新遭遇，HP独立倍率、等级快照及旧档兼容；会员页更新状态。
- 套装累计门槛、图腾最高经验档位进入角色计算，导出83套效果和7职业；套装成员缺失、图腾学习及特殊属性仍待补。
- 魔法口袋10项奖池及周额度规则已实现，领取闭环未实现。
- PvE仇恨表与直接伤害/溅射目标选择接入；治疗、持续效果、特殊权重和嘲讽卡尚未接通，不称完整仇恨。
- 310项测试和Vite打包通过，未验真实账号及浏览器交互；未改地图剧情变身、玩家存档或提交发布。

### 第二组非剧情十轮：装备与治疗

- 补齐装备主副系及全局属性汇总、按键换装比较、正负号和未支持提示；隐藏零值次要属性。
- 原版182/183接入治疗与受治疗百分比，宝石共用映射；旧战斗保留旧装备规则，新遭遇启用新版本。
- 真实治疗卡与装备测试16项通过，全量303项通过，Vite独立打包通过。修复映射扩大商城范围导致缺图，纯治疗装备暂不上架。
- 手机隔离DOM详情无溢出；桌面详情、真实指针/动画及完整地图构建门禁未完成。未改存档、地图、剧情，未提交发布。变身按用户要求排除；套装、仇恨、龙图腾、VIP战斗加成仍待迁移。

- 卡包剩余数量改为对齐底部控制栏顶部，避免与独立定位的战斗记录入口重叠；不改变操作按钮和日志位置。
- 验证：1107px桌面数量文字与日志入口垂直间隔49px；390px手机沿用隐藏数量规则，日志入口可见。CSS诊断无错误，Vite打包通过；未重新运行已知被地图快照阻断的完整build。

## 战斗手牌紧凑居中

- 手牌网格卡位最大步长改为卡宽加12px，整组居中，不再将少量卡牌拉满可用宽度；窄屏保留压缩叠牌。
- 浏览器验证：1107px桌面四张牌总宽548px、间距12px且居中；390px手机左右边界18px/372px，无横向溢出。编辑器无错误，Vite打包通过；完整build仍被既有camp地图快照过期检查阻断，未改动地图。

## 符文消耗卡

### 非剧情十轮补齐

- 统一符文状态，背包和通用预览显示实际卡面及支持状态，任务奖励接入详情；不更改世界剧情。
- 补齐跨战斗栏状态、耗尽与旧档兼容、消耗原子性、场次校验、决定计数防重复、畸形检查点拒绝及消耗禁止倒退。
- 桌面八种/手机四种符文分页；移除原物品说明35%与实际法术40%并列的冲突。
- 293/293全量测试通过，Vite打包通过；隔离DOM事件验证详情和22种符文分页，390px禁用动画后无横向溢出。真实指针工具持续超时，动画与真实触屏验收仍待补齐。没有操作用户角色存档、提交或发布。

- 对照arena_server.lua的RUNE_SPELL_OFFSET=1000、player_server.lua L5385-5395成功施法扣除，接入独立库存、目标/魔力校验、触屏、即时持久化及重演一致性验证。
- 287项自动测试通过，浏览器隔离点击数量2→1，Vite打包通过。完整build的地图快照检查报camp.json过期，未修改无关地图。未发布，完整美术及多尺寸视觉验收待补充。

## 底部主菜单移除大背景

- 隐藏游戏菜单整组木质背景框，保留任务、卡包、背包、宠物、商店各自图标与文字底牌，布局和事件不变。
- 验证：CSS诊断无错误，构建通过；浏览器1280px与390px宽度下背景伪元素均为none，五个按钮均可见且未超出视口，已截图核对底栏效果。

## 右上角快捷入口圆形样式

- 签到、世界地图、云存档、设置复用底部导航的金边绿色圆环与木质文字底牌，移除整组黄色底框。
- 保留签到倒计时和领取提示，手机双列及横屏布局调整间距，避免任务追踪面板遮挡。
- 验证：npm run build 通过；git diff --check 通过。浏览器工具连接失败（nodeRepl.fetch request failed），未完成桌面及390px实际截图验收。仅修改视图标签和CSS，无引擎改动。
# 开发日志 2026-09-21

## 默认口袋容量下调

- 后续修复：新增 `defaultPocketVersion`，旧存档先按14/3校验再迁移到10/2；未结束战斗保留原快照规则，结算或撤退后迁移，避免启动报“超过拥有数量或单卡上限”。新增旧档加载、非法卡组拒绝、战斗快照保留和撤退迁移测试，冒险定向测试24/24通过。
- 未装备口袋时由总容量14张、同卡3张下调为总容量10张、同卡2张，避免默认口袋强于麻布口袋；装备道具属性不变。
- 配卡校验、推荐卡组、战斗规格和卸下预览共用 `deckLimits`；已有卡组沿用同步逻辑裁剪到新上限。
- 验证：`node --test tests/adventure.test.mjs` 23/23通过，包含五系完整章节、10张边界、单卡2张边界、旧卡组裁剪及装备口袋属性保持；未进行浏览器视觉验收。

## 背包默认打开角色装备

- 背包入口默认选择“角色装备”，不再直接选择“旅行背包”；保留手动切换旅行背包，再次打开时回到角色装备。
- 验证：执行实际 openPanel 控制器函数，背包、装备入口及再次打开均选择 gear；编辑器无错误，npm run build 通过。未进行浏览器点击验收。

## 关闭常驻环境光尘

- 地图默认 `motes` 不再绘制，移除旧地图独立的漂浮光点；避免光尘贴在镜头上、跟随角色移动的观感。
- 保留显式天气预览，以及降雪、沙尘、余烬、薄雾、灰烬；未新增森林局部萤光。
- 验证：天气定向测试7/7、全量测试270/270、六岛配置检查和构建通过。内存副本移除拦截后恢复光尘绘制，确认回归断言可识别旧行为。未进行浏览器截图验收。

## 商城内测道具过滤

- 新增按ID维护的 `data/adventure/item-flags.json`，1122、1126、2025–2030八件测试道具标记 `isInternalTest`（是否内测）；导出器将属性写入候选目录，保留可重复导出能力。
- 扩展层把属性传入物品与商品，商城在搜索/分类/分页前隐藏；购买逻辑拒绝内测道具且不扣款。保留物品与历史商品记录，兼容已有购买记录和背包。
- 回归测试覆盖名单与候选一致、截图三件道具禁止购买且状态不变、旧购买存档可加载、普通商品仍可购买。`npm test` 264/264通过，`npm run build`通过。
- 未提交、未推送。

## 卡包手势与紧凑布局

- 训练点移到分页行，牌库顶部取消独占行；整张牌库卡片点击放入，去掉右上角加号。
- 手机长按查看详情，电脑右键或悬停查看详情；触摸滚动/长按抑制随后的点击，防止误学卡或误放入。
- 左侧拖动显示随指针移动的卡牌副本，放置在页面根节点避开窗口滤镜和滚动裁切；拖出移除一张，区内释放/取消保留并清理副本。
- 验证：269项测试通过，构建通过。测试覆盖指针坐标更新、释放及取消清理、长按/右键不删除、滚动不学习、点击加入及草稿扣点。
- 浏览器连接请求失败，未完成本轮实际浏览器视觉验收。

## PC卡包窗口固定高度

- 桌面（宽度大于650px）窗口采用680px固定目标高度，小屏按视口高度减44px收缩；不再由卡牌数量或筛选结果撑高。
- 工具行与保存按钮固定，牌库填满剩余空间并内部滚动，左侧配卡区域可独立滚动；保留手机纵向布局。
- 验证：npm run build通过。浏览器连接此前持续失败，本轮未完成实际视觉验收。

## 旅行背包空状态

- 旅行背包及其分类为空时只显示空物品格，移除覆盖格子的空状态提示。
- 验证：npm run build、git diff --check 通过；未进行浏览器视觉验收。

### 姓名旁 VIP 星章
- 参考原版 `MagicStarArea.DoClick` → `CombatMagicStarPage.html`，已确认会员在姓名右侧显示金色星形 VIP 标记。
- 点击展示徽章、账号状态及商城权益卡片，支持刷新状态和前往商城；手机纵向布局，复用统一关闭按钮。
- 会员资格仍由 Keepwork 适配器提供，不增加存档字段或原版能量/等级。
- 验证：`npm test` 269/269 通过，`npm run build` 通过。隔离会员身份页面实测姓名旁星章、点击详情、商城跳转、390×844 手机纵向布局，以及普通账号无 VIP 星章；未使用真实付费账号验收。

### 会员入口文案调整
- 姓名右侧的会员入口改为纯星形图标，非会员入口及商城入口统一“会员权益”。详情及购买失败提示不再显示“Keepwork VIP”，账号会员判定保持原有接入。

### 会员入口改用文字按钮
- 按最新反馈取消姓名右侧星形标记，改为浅金色文字小按钮：非会员“升级会员”，会员“会员权益”；商城入口同步条件文案，点击仍打开会员详情。

### 魔法星复刻与奖励领取
- 查阅原版 `CombatMagicStarPage.html/.lua`、`VIP/main.lua` 与 `MagicMoneyBox.lua`；导入本地 JSON 的 0–10 级属性预览、五件法杖门槛和每周仙豆展示数量。
- 按用户确认规则将剩余日历月数映射星级，封顶 10；会员可以实际领取法杖及每周仙豆，记录写入角色存档。领取前刷新会员状态，防重复、到期和异步角色切换。
- “成为VIP”始终可见，接 SDK `showProfileWindow`；详情关闭后刷新状态。会员付费开通和原版战斗加成未新增。
- 浏览器隔离身份验收桌面及390×844手机、普通用户完整预览、法杖/周奖励领取状态和个人资料入口回调；真实付费账号与 SDK 资料窗口未做实账号操作。
- 针对测试29/29通过；全量276项中272通过，4项失败位于 adventure_debug、adventure_decks、adventure_equipment、adventure_pet_cards 的卡包数量/单卡上限用例，未修改这些并行工作的规则或断言。构建通过。

### 米酒葫芦会员差异
- 通过新增脚本 `scripts/export_adventure_checkin.py` 从原版加密静态数据库解码并导出1801–1806配置，带来源哈希；所有奖励读取本地JSON。
- 普通奖励改为仙豆和原版道具，会员可逐葫芦追加星级仙豆，会员领取前刷新资格。普通与会员标记独立，可当天升级后补领。
- 兼容旧版本2在线时长/已领取记录，不清除旧奇豆，不重发已领普通奖励；保存并验证额外领取记录。午夜按北京时间重置，离线不累计。
- 原版随机抽奖缺少本地完整奖池、独立日历签到及部分奖励道具使用仍未开放，界面已注明。
- 验证：全量 `npm test` 280/280通过，`npm run build`通过。浏览器隔离会员先领100仙豆及道具，再领300额外仙豆，余额400、双标记1/5；非会员额外奖励跳转魔法星，390×844两列且无横向溢出。使用测试身份，未操作真实会员账户。

### 魔法星奖励详情与通用弹窗
- 奖励图标/名称可点击查看原图、穿戴条件、基础属性、附加法术及领取条件，未解锁和已领取同样可查看。
- 新增通用 DetailDialog、ItemDetails，背包装备详情迁移共用弹窗生命周期，保留原对比/穿戴逻辑。
- 已浏览器验证未解锁暴云魔杖的两张附加卡、关闭/Esc焦点恢复且魔法星父窗口保留。
- 验证：`npm test` 280/280通过，构建通过；浏览器实测未解锁及已领取奖励详情、附加法术、390×844手机布局、Esc只关闭子窗并恢复焦点；背包共用弹窗仍显示装备对比与穿上入口。

### 修复刷新后重复领取仙豆
- 根因：`checkedProgress` 以 `createAdventure` 的初始字段做白名单，漏掉首次领取/计时才产生的 `magicStarClaims`、`checkin`。角色缓存重新打开和云端验证时记录丢失，但库存保留。
- 将经 `parseSave` 校验的两项可选记录明确加入白名单，仍过滤未知字段。魔法星每角色每周一次、签到每角色每日一次的周期不变。
- 新增 `persistReward`：克隆暂存奖励，写入角色本地缓存成功后才替换当前状态；存储失败、多标签页版本冲突均不发奖且不消耗领取机会。
- 回归284/284、构建通过。新增真实角色存储重开、云端往返、每日普通/VIP标记、存储失败重试、旧标签页冲突测试。浏览器隔离缓存实际领取1200仙豆后刷新，仍为“本周已领取”，余额保持1200。
