# 2026-09-25

- en.txt 语言文件补全：为 `data/adventure/locale/en.txt` 全部缺失字段填写英文翻译（最终 2195 键），保留用户添加的 `# 文件名` 分组注释。保留 2 个有意为空的键（英文 AI 配对 prompt、SVG 福袋图）——lookup 空值时返回源串，行为正确。验证：`node --test tests/locale_core.test.mjs tests/locale_scan.test.mjs` 8/8 通过。
- 第二批新增内容覆盖：npc-catalog.json（NPC/物件名）、quest-journal.json（任务标题+描述，含秘境 1-100 层系列、咕噜噜/铁壳怪/胖雪人泪水副本任务）、chapter.json（青龙新手教学对白）、view_adventure.js 的 {name} 模板。用户追加内容时按 `# 数据文件/源码文件` 分组追加，翻译需逐节处理。
- 批量填充方法：写临时 node 脚本（key→value Map + splitLocaleLine 解析，只填空 value），跑完删除。注意：键必须逐字节匹配（源文有标点变体：'双倍奖章奖励' vs '双倍的奖章奖励'、'第13守卫' 缺'层'、半角/全角引号混用）；脚本需报告 remaining empty 与 unused entries 以定位漏项。
- 术语约定（用户 2026-09-25 指定）：哈奇=Haqi，魔法哈奇=Magic Haqi，哈奇小镇=Haqi Town，哈奇岛=Haqi Island，哈奇商城=Haqi Mall；奇豆=Qi beans，仙豆=fairy beans，魔法星=Magic Star，咕噜噜=Gululu，抱抱龙=Cuddle Dragon，出奇蛋=Surprise Egg，米酒葫芦=Rice Wine Gourd，安卓婆婆=Granny Andro，青龙=Azure Dragon，镶嵌符=socketing talisman，魔力点=pips。
- locale 格式要点（见 js/locale_core.js）：`key||value`，最长 `|` 串为分隔符；空 value = 已知键（返回源串）；换行存为字面 `\n`；行首 `#` 且不含 `|` 为注释。
- 角色名五连击调试入口 + 货币一键充值：功能已在工作区（js/view_adventure.js renderHud heroClicks、js/view_adventure_debug.js debug-topup、css/adventure.css .debug-topup），本次完成浏览器端到端验收 9/9（Chrome headless + playwright-core 脚本在 managed node workspace，隔离端口 8917，默认 CDN 模式可加载但首次较慢），QA 报告与 devlog 已追加。验收脚本保留在 node workspace/verify-debug-panel.mjs 可复用；playwright-cli CLI 的 daemon 在 bash 命令间不存活，须用单脚本跑完整流程。
- 充值按钮立即生效修复：js/view_adventure_debug.js 的一键充值原先只填表单（需再点「保存并生效」），玩家因此以为没生效；改为目标值高于余额时直接 cb.applyDebug。复现脚本 verify-debug-topup-apply.mjs（先手动设 152/1960/460 再逐个点击）8/8 通过。教训：调试面板的“一键”操作应直接落盘，不要套用表单预览两步流程。
- 装备槽位（我的背包·角色装备）已装备时不再显示部位名：js/view_adventure_equipment.js 的 renderEquipment 只在 `!item` 时输出 `.equipment-slot-name`（空槽保留部位名 + ＋），css/adventure.css 的 `.equipment-modal .equipment-slot` 加 `justify-content:center` 让纯图标的格子居中。aria-label 不变。原因：英文部位名（炫彩头饰→Rainbow headwear）在 66px 格子里换行把图标压到下一行。
- 新隔离夹具 tests/fixtures/equipment-slots.html：真实 renderPanel('equipment') + 内存角色，参数 `?assets=local&lang=en&empty=<slot>`（empty 用于留空槽位），只渲染不写存档。
- 新项目技能 .cursor/skills/browser-fixture-check/（shot.mjs 截图 / probe.mjs 量测表达式）：跑仓库内 `ws` 走 CDP，**不需要 Playwright**；流程＝python -m http.server 静态服务（后台）+ chrome --headless=new --remote-debugging-port=9222（后台）+ 脚本从技能目录运行（node_modules 解析 ws）。夹具在渲染完成后置 `document.documentElement.dataset.fixtureReady=1`，失败置 fixtureError。
- 沙箱注意：后台服务/浏览器必须用后台模式启动否则随命令结束被杀；`npm run build` 首次可能因 safe-delete 清理 dist 报 ETIMEDOUT，重试即过；tests/release.test.mjs 在沙箱内 spawnSync 报 EBUSY（环境，非回归）。当前全量 528 项中 525 通过。
