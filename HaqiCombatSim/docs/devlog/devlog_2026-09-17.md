# 2026-09-17 · 卡包 / 配卡 / 决斗圆盘演示

## 需求（用户）

> 有个很重要的概念是卡包大小，配卡。并且每轮会随机从中抽 N 张，支持扔掉卡片。卡片用完就无法出卡了。
> 动画演示：可以在 2 队中间画个圆盘，上面几个点，中间可以显示下卡片，以及谁对谁使用的。各种 buff/debuff 效果显示清晰。

## 调研

- 抽牌 / 弃牌 / 打空引擎里已有（`ShuffleDeck` / `PrepareCard` / `DiscardCard` 1:1），但缺**卡包容量**：Lua 由卡包道具 `stats[167]`（总容量）`[170]`（单卡上限）`[171-177]`（分系单卡上限）在 `Arena.OnReponse_TryEnterCombat`（arena_server.lua L8011-8166）校验；teen 同 `spell_name` 共享单卡上限（L8119）。道具表（GlobalStore）不在本地。
- 客户端初始卡包：kids 14 / 单卡 3（CombatCardDeckSubPage.lua L38-40），teen 18 / 单卡 5（CombatCardManager.teen.lua L41-43）。
- 旧预设卡组固定「每卡 2 份、最多 40 张」，与容量无关。

## 实现

- 引擎：`global.deckCapacity` / `deckEachCapacity`（默认 40 / 6，用户确认正常玩家约 40 张、单卡 6）/ `deckPresetCopies`（官方卡组每卡份数，默认 3，用户强调带满不一定最好）；`clampDeck`（含 teen 同名共享、同 key 合并、`deckTrimmed`）；`deckCounts` / `isDeckExhausted` / `restoreDiscardedCard`；弃牌与出牌解耦；`pass.reason` 增加 `no_cards` / `deck_empty`；`totals.noCardPasses`；双方打空提前判平（`decksExhausted`）；`summarizeUnit` 增加 DOT/HOT 段值、冷却、卡包计数。
- 预设：`presetDeck` 改为轮询展开（A,B,C,A,B,C…），由 `createUnit` 按容量裁剪 → 小卡包仍保持卡种多样，容量改大自动填满。
- 批量：`noCardPassRate` / `deckExhaustedRate` / `deckDraws`；批量页可勾选「用对战页配卡」。
- UI：`utils.svg()`；对战页重写 —— 配卡面板、SVG 决斗圆盘（站位点 / 头像 / HP / 魔力点：普通蓝、超级金 + `n+m` 数字 + 图例；teen 无超级魔力点 / 卡包剩余 / 状态徽标）、中央卡牌面板 + 能量束 + 飘字、事件流分步动画（慢 / 正常 / 快 / 无 + 跳过）、弃牌模式与右键弃牌、卡包计数条、状态明细板；跨页切换接回对局（`game.ui`）。
- 测试 +4（34 例）。

## 数据观察

见 [qa-report.md](../qa-report.md) 2026-09-17 表：teen 18 张卡包下生命系 1v1 胜率 2%、平局 45%；不限容量时 27%。卡包容量与 HP / 伤害同级重要，结论前须确定目标玩家的真实卡包容量。

## 待办

- [ ] 从 GlobalStore 导出卡包道具 `stats[167]/[170]/[171-177]`，按等级段给 `deckCapacity` 默认值，并支持分系单卡上限
- [ ] 配卡面板显示卡牌中文名（依赖卡名解码）
- [ ] 圆盘：DOT 结算时目标高亮；死亡时倒下动画

## Haqi.html · 可玩的魔法哈奇开篇

用户要求在 `HaqiCombatSim` 独立入口实现kids二维单人游戏，并复用原任务、居民、奖励、美术及战斗规则。实现已放入独立web仓库的dev分支；父仓库已有改动及同时进行的HaqiCombatEmulator改动保持不动。

### 交付

- `Haqi.html`：Canvas 2D营地/小镇与圆形战斗场；HTML中文角色创建、任务、对话、背包/强化、宠物、卡包、存档设置。桌面WASD/箭头、点击寻路、移动端方向按钮与点选。
- `adventure_core.js` / `adventure_world_core.js` / `adventure_content_core.js`：纯规则，14原任务、五学系、源奖励/装备、宠物经验、可走地图、版本化存档及严格引用检查。
- `combat_pve_core.js`：单独PvE接口，复用公共卡效果/公式，原怪物起始魔力、额外/普通行动、18回合考核脚本、HP基因、可重复怪物卡池；事件流驱动法术动画。公共模块仅增加PvE分支，原PvP保持独立。
- 可重复Python导出器：XML及限制为字面量的Lua数据解析，读取原任务/NPC/怪物/场地/商店/强化/宠物定义；独立提交的章节数据含原ID、原文、来源哈希及逐条改编说明。
- 完整清单CDN地址准备89项本地资源，校验长度/MD5/PNG CRC及解码。原NPC立绘、卡图、道具图、地图参考、音乐加两张新生成透明图集；生成方式和提示词概述见[美术说明](../../assets/adventure/README.md)。运行时无需构建、npm install、父仓库或网络。
- 自动存档与JSON导入/导出；战斗保存种子和决定，重演恢复；任务/遭遇奖励去重；失败或撤退保留已得物品及进度。

### 验证与修复

56 / 56测试通过（原34 + 新22），五系完整章节自动通关；实际浏览器烈火角色完成全部14任务、毕业并到达小镇。手机尺寸、虚拟方向键、NPC点击交谈、存档/战斗恢复、失败检查点、导出文件、无效导入、音乐404及必需JSON404均检查。原模拟器1v1及5000场矩阵正常。内容导出重复生成三个字节完全一致的JSON快照，89项本地资源全数通过校验。详情见[QA记录](../qa-report.md)。

实际游玩修复了A*仅验证节点导致树角卡住的问题：现在验证整条边、非网格中心起点的连接以及分段移动；新增覆盖地图全部交互点的实走回归。翡翠口袋课程必须在装备后重新保存卡包，不能由早期配卡记录提前完成。生成角色图集按实际留白切行，避免混入上一行建筑。

### 范围与启动

毕业后小镇为明确改编（原下一任务指向火鸟岛）；原奖励数值保留，等级阈值、基础牌学习及孵蛋结果为已记录的章节安排。捕捉、战宠技能、会员兑换、消耗符文/变身系统、其他岛屿和在线服务未开放，相关物品保留为收藏。未部署或修改线上服务。

运行 `python3 -m http.server 8791 --bind 127.0.0.1` 后访问 `/Haqi.html`。完整说明见[adventure.md](../adventure.md)。

## 后续开发约定：Maisi 技能、WebP 与 Keepwork CDN

根据用户要求，在项目 `AGENTS.md` 记录了 Maisi 的美术生成/WebP/CDN 技能路径、Keepwork SDK 指引和本地源码位置。允许 Git 保留 WebP 美术副本；线上资源必须使用永久 Keepwork CDN。第三方库只允许已在 Keepwork CDN 托管的版本，列出 SDK、Three.js、Tailwind 的技能目录地址，不允许其他 CDN 或本地打包绕过。云存档以后优先通过独立 workspace 的 PersonalPageStore 接入，保留访客本地存档和重演校验。

本次仅更新开发约定，检查文档差异和引用路径；未转换现有图片、上传 CDN 或修改游戏运行逻辑。

## 下一轮：WebP / 永久CDN / Keepwork云端旅途

沿用Maisi美术与Keepwork SDK指引，将90张PNG无损转换为WebP并移除工作树中的旧PNG；两张图集尺寸、alpha、裁剪坐标不变。91项资源共8.33MB，减少23.1%；用现有七牛上传技能以内容SHA-256文件名发布到 `keepwork/haqi-adventure/v1/`。远端GET/CORS/长度/哈希全部通过后记录实际URL，保留原 `assets_manifest.txt` 完整来源。资源生成、发布计划、远端验证均可重复执行，凭据不入库。

新增纯资源模式模块、纯云端封装模块、SDK IO适配器及独立云端菜单。线上默认CDN，loopback默认Git中的WebP；SDK core按需从已有Keepwork CDN加载。通过PersonalPageStore的HaqiAdventure workspace保存唯一检查点，不覆盖另一设备记录；先预览再确认恢复、备份旧本地进度、校验账号和预览后的本地更新。源码检查发现store读取即便forceRemote也可能回退本地，因此写后使用SDK原始文件接口关闭缓存、比对远端JSON，避免假成功。SDK返回格式可能重排缩进，核验解析后的JSON而非空白。

69测试通过，91资源校验通过，浏览器本地/CDN场景、手机云端菜单、模拟SDK写入/失败/恢复、战斗第2回合云端重演与刷新继续通过。真实SDK中文登录和取消通过，实际账号存储往返未做（未提供测试账号）。没有网站部署，没有引入其他CDN库或修改战斗数值。用户原存档保持，浏览器写入测试放在独立8793 origin。可选digitalhuman-resource镜像未经明确选择不复制，web仓库保留WebP。

最终SDK检查补充：createFile硬编码启用后台server pageCache写入，可能先清除pending而让后续syncToGit(false)成为空操作。适配器使用同一workspace的savePageData(path, content, text, false, false)暂存无缓存写入后再同步；新增回归，防止未来重新使用缓存写入快捷接口。

## 卡面直接承载文字

根据用户截图调整战斗手牌和卡包：去掉米色外层卡框，原版卡图填满151×230比例的卡面；保留图内卡名，效果摘要叠加到下方描述区，魔力点叠加到右上角原圆标。坐标参考kids `pe_item.DrawCardMask`，不使用teen左上角成本布局。桌面手牌宽度最高142px，手机116px并支持横滑，弃牌按钮独立位于卡图下方；选牌发光及弃牌灰化保持。

69测试通过；浏览器1280×720、390×844实看卡面、魔力点、选择高亮、弃牌/撤销与施法，卡包内增益描述同样可读。使用独立8794 origin导入测试战斗，未替换用户的角色、任务或配卡。原素材和CDN URL未改，只变更DOM/CSS绘制。
