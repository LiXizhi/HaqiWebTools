# 2026-09-26 开发日志

## 背包改名字和基础头部形象

- 角色装备页名字旁增加不显眼的 ✎ 铅笔按钮（无边框）；费用与图腾转换相同：改名字 50 魔豆，改基础头部形象 50 魔豆，只对有改动的部分收费。价格只在编辑弹窗里显示，不出现在背包列表。
- 编辑窗口复用创建角色的头部轮播和名字输入。魔豆不足、没有改动或战斗中不能确认。

## 本地美术独立包

- H5 的 `dist/` 与 CDN 上传保持原样。`npm run build:app` 另写 `app-dist/`，把运行时 JSON 引用的 WebP/Ogg 打进目录，并把资源模式烘焙为本地。本次共 1181 个文件、70003050 字节。
- Electron 从回环地址打开 `Haqi.html`，Windows 已打出 NSIS 与 `win-unpacked`，并暂存到 `steam-depot/windows`。macOS dmg 需在 Mac 上执行同一条 `build:desktop`。
- Capacitor 工程在 `android/` 与 `ios/`，入口 `index.html` 跳到 `Haqi.html`。iOS 归档仍要在 Mac 上签名。
- 浏览器验收：对 `app-dist` 做 HTTP 服务后打开 `Haqi.html`，图片请求留在该源，不请求 `cdn.keepwork.com` 上的 WebP。

## 角色创建向导英文混排修复（步骤条与云端错误）

- 用户截图反馈：英文界面创建角色第一步步骤条「01 起名字 / 02 选择抱抱龙 / 03 选择系别」与底部红色云端错误「云端操作未完成。请检查网络或重新登录；本地进度仍然保留。」仍是中文，其余正文均为英文。
- 根因一：`view_adventure.js` renderEntry 步骤条把序号拼进模板字符串（`01 起名字`）再交给 `tr()`，而 en.txt 的键只有裸标签「起名字」，带序号整串查不到词典（扫描器把该模板归为动态句，扫描也不报缺）。
- 根因二：`adventure_cloud.js` 约 40 条用户可见 CloudError 文案从未进 en.txt，且该文件不在 `.cursor/skills/locale-scan/scan-paths.json` 扫描清单内，缺词无守护。
- 修复：序号与标签作为两个子节点传入 `el()`（各自走 tr，视觉输出不变，中文模式仍是「01 起名字」）；en.txt 新增 `# js/adventure_cloud.js` 小节共 40 条（动态拼接的「已登录，但云端角色校验失败：${error.message}…」不进词典）；登录窗口标题改为 `tr('登录 Keepwork，继续魔法旅程')` 使词条实际生效；`js/adventure_cloud.js` 加入扫描清单防回退。
- 扫描器修复：`scan_locale.mjs` 把正则结尾转义斜杠 `\//`（如 `/^checkpoints\//`）误判为行注释起点，吞掉同行后续字符串（`云端记录路径无效` 既不进 missing 也不进 statics、进词典反而成 stale）；行注释判定增加 `text[i-1] !== '\\'` 前置条件。
- 验证：`node scripts/scan_locale.mjs` 中 adventure_cloud.js 全部静态文案命中词典（无 missing/stale），总缺词回到既有 85 条（npc 副标题、词义映射 UI、语言学习设置等既有缺口，本轮未动）；locale 相关 21 项测试全过；`npm test` 645/648，3 项失败均为用户 WIP 既有问题（adventure_creation_preview 新增 hero ensure await 后测试 `pending.shift()` 时序错位、`data/mount-demo/mounts.json` 未配置 ×2），与本改动无关。未提交。

## 战斗指针回位与淡色符文样式

- 每轮结束追加200ms指针回位，再恢复选牌；默认己方按卡位选首位存活行动者，支持敌方先手，战斗已结束则不追加回位。下一轮从待机朝向开始，移除上一轮最后施法者造成的跳向。
- 金色实体箭头改为更短的半透明青绿色轮廓，带渐隐细线、菱形符文和断环；取消金属填充和强烈金色光晕。
- 7项指针/角色动画测试通过，npm run build通过。浏览器只读预览确认淡色轮廓；预览队列已加入回位和选牌等待。未修改战斗数值、存档、提交或发布。

## 魔法指针尺寸与淡金线调整

保留青绿色与55%透明度，长度由法阵半径39%增至49%，箭头半宽12增至20，主轮廓1.5增至3像素，符文和断环同步放大加粗。箭尖内侧、轴线局部与中心断环加入少量淡金细线。浏览器只读预览已检查，npm run build通过；仅调整绘制样式，不改行动及回位时序。

## 魔法星法杖领取条件英文翻译修复

- 物品详情弹窗"领取条件：魔法星 10 级 · 角色 1 级"在英文界面未翻译。根因：`view_adventure_membership.js` L51 用模板字符串直接把数字插入中文，生成"成品中文"后 `slot()` 在 en.txt 查不到精确键（en.txt L3087 的键是带 `{star}`/`{level}` 占位符的模板）。
- 修复：改用已导入的 `fill('魔法星 {star} 级 · 角色 {level} 级', …).text` 先翻译模板再传参，复用 en.txt L3087 既有翻译（Magic Star level {star} · hero level {level}）。en.txt 无改动。
- `npm test` 632/639 通过；7 项失败（renderer createActor、document 未定义、quest-catalog 哈希漂移等）为既有问题，栈中均不涉及 membership 模块。未提交。

## 语言陪聊入口改为随行宠物头顶气泡

- 原固定右上角"抱抱龙 · 和我聊聊"横幅按钮（`.learning-launch`，top:110px）与任务追踪器（top:126px）重叠，按用户要求移除；`view_language_adventure.js` 删除按钮及 `visibility`/`invite` 接口，`css/language_adventure.css` 与测试夹具同步清理。
- 改为场景内气泡：`adventure_renderer.js` 新增 `drawSpeechBubble` 画师，在随行宠物头顶绘制奶油色对话气泡（#fffbe9 底、#7a6335 描边、向下小尾巴、正弦浮动，遵循 reduced motion），并暴露 `bubbleTarget()` 返回世界坐标命中矩形；`language_adventure.js` 控制器以 `bubble` getter 输出邀请文案（`${伙伴名} · 想和你聊两句` / `和我聊聊`）。
- `adventure_app.js` 帧循环在 `stage==='world' && 无面板 && 无对话框 && !languageAdventure.active && !fishing` 时传入 `companionBubble`；`clickWorld()` 在钓鱼判定前先检测 `bubbleTarget()` 命中，命中即 `openFreeTalk()` 打开聊天 UI。鼠标与触摸路径均走既有世界坐标点击管线。
- 验证：语言冒险 28/28 测试通过；`npm test` 630/639（9 项失败为用户未提交 WIP 的既有问题：测试 mock 缺 `assets.hero`、缺 `data/mount-demo/mounts.json`、packed_startup 缺 document）；`npm run build` 通过。浏览器核验四项全过：无 `.learning-launch` 残留、宠物头顶有气泡像素（#fffbe9 扫描约413像素）、点击气泡打开营地聊天、任务追踪器区域无遮挡。
- 发现既有问题（未修）：`adventure_checkin_core.js` L62-66 校验拒绝游戏自身写入的存档，重载时报"存档签到记录无效"，需用户确认签到 WIP 意图。

## 魔法星详情图标缩小与"跟随主角"单选钮

- 按用户截图要求：`view_adventure_membership.js` 身份区改为 `.magic-star-emblem-stack` 竖排，徽章由 88px 缩到 56px，图标正下方新增单个圆形单选钮"跟随主角"；等级文字、激活状态与有效期仍在其右。
- 控件用原生 `input[type=radio]`（圆形外观 + 单键开关语义）：浏览器不会主动取消已选中的单选钮，因此 click/Space/Enter 手写翻转，禁用时保留可见但不响应。魔法星 0 级（未开通或到期）时禁用，标题提示"开通魔法星后可设置跟随"。
- 语义按用户确认：取消跟随＝场景中不再绘制魔法星（非原地停留）。显隐判断集中到 `adventure_star.js` 新增的 `starCompanionVisible`，`adventure_renderer.js` L188 仅供该函数决定 `starFollower` 是否启用；`motion.style.vip`、标题、隐藏标签页、捕鱼、传送等既有条件不变。
- 偏好按用户要求**只存本机**：不再由 `applyAction` 写入、不再进云端。字段 `magicStarFollow` 仍挂在存档对象上（默认 `undefined`＝跟随），但被 `adventure_storage_core.js` 的 `durableSave` 剔除，改由 `runtimeValues` 的 `prefs` 随角色运行时写入 IndexedDB，`restoreRuntime` 无条件恢复；切换走 `adventure_app.js` 新增的 `setMagicStarFollow`（`persist()` 落盘 + toast，不 `revision++`、不 `queueCloudSave`）。`checkedProgress` 白名单加入该字段，否则角色校验会把它剔掉。脚下星形粒子与飘带属移动特效，不随该开关变化。
- 英文词条：`en.txt` 新增"跟随主角""开通魔法星后可设置跟随""取消后在场景中不再显示魔法星。""魔法星会继续跟随主角。""已取消跟随，场景中不再显示魔法星。"；`node scripts/scan_locale.mjs` 中这 5 条均不在"源码有、词典没有"列表内。
- 验证：`tests/adventure_storage_split.test.mjs` 新增"follow preference is device-local…"（durableSave/split 剔除、runtimeValues 保留、revision 变化后仍恢复、不进 coreCatalogKey），`tests/adventure_magic_star.test.mjs` 新增"scene companion follows by default and hides only when the local preference says so"；`npm test` 642/644，2 项失败（mount_pose、quest-catalog 哈希漂移）与本次改动无关。真实浏览器验收：写入后 IndexedDB 记录 `prefs:{magicStarFollow:false}`、localStorage 文本不含该字段、切换不置脏（改物品置脏作对照）、新页面重载后仍为 false。
- 浏览器验收（Playwright 1.44 + 本机 chromium-1243，静态服务 127.0.0.1:8791，只用 `tests/fixtures/membership.html` 内存角色）：默认勾选、点击取消（isChecked true→false）、Space 切回 true、`?ordinary` 下控件为 disabled；场景隐藏用临时夹具核验，跟随开启时主角旁有蓝色星体，取消后消失（蓝色像素 8068→6243），临时夹具已删除。
- 未提交、未推送、未发布；未改战斗数值与会员等级规则。既有失败项 `adventure_follower_render.test.mjs`（mock 缺 `assets.hero`）仍在，未修。


## 主角分层渲染正式接入与侧向连接修正

- 按用户授权将场景、战斗、钓鱼、角色选择、装备、商城、宠物/坐骑、技能预览及官网骑手展示统一接入 HeroRenderer；保留原整身素材作为缺图回退。正式清单为 data/adventure/hero-art.json，独立对照页继续可用。
- 左右侧连接点向后脑/耳根方向校正，身体WebP短脖子随连接点更新；头部仍覆盖脖子。身体其他像素、原图裁剪、坐骑缩放、座位、前景遮挡保持原配置。
- 头部加入轻微呼吸、种子随机的左右张望，正常偏转限制±45°；移动/附近NPC优先。UI动画共享调度，角色状态不写入玩家存档。
- 八张WebP上传至 Keepwork CDN 20260926/cb13100ea5e8，逐项核验SHA-256及CORS；按用户选择未同步Git镜像。
- 验证：六套身体区域外像素比较通过；66坐骑×男女×四方向×移动/静止共1056个布局组合通过。npm test：641项，639通过；剩余2项为既有mount-demo/mounts.json缺失及quest-catalog依赖哈希漂移。npm run build通过。浏览器内存夹具验证场景步行、骑乘和CDN战斗显示；不读取玩家存档。未提交、推送或发布网站。

- 浏览器追加发现并修正旧 `.pet-hero-figure canvas` 固定96×120样式影响新坐骑合成画布；专用Canvas恢复100%原组合边界，避免缩小和左上偏移。默认CDN的坐骑UI验证通过；明确local模式的既有坐骑列表图片路径校验仍会拒绝demos路径，本轮未扩大通用资源路径白名单。

## 场景坐骑显隐开关（英雄卡下方图标）

- 按用户截图与需求：在英雄状态卡下方、翻译按钮（`.locale-launch`）右侧新增坐骑图标按钮（`.mount-toggle`），切换漫游场景中坐骑的显示；战斗场景 `renderBattle` 不受影响、坐骑恒显示；无坐骑（`!save.mountId`）时不渲染该按钮。
- 渲染：`adventure_renderer.js` 世界场景构造 `visualSave`（`mountHidden` 时 `mountId:null`），仅 `motionStyle`、`avatar`、`starAnchor` 使用它；`renderBattle` 仍用原 save。
- 偏好按用户要求**只存本机 IndexedDB**（与 `magicStarFollow` 同一模式）：字段 `mountHidden` 加入 `adventure_storage_core.js` 的 `prefFields`，`durableSave` 剔除、不进云端与核心副本；`adventure_cloud_core.js` 运行时键列表加入该字段以便角色重载保留；切换走 `adventure_app.js` 的 `setMountVisibility`（persist + toast，不 revision++、不置脏、不触发云端保存）。
- 图标：UI 图集无坐骑帧，使用矢量兜底路径（`paths.mount`，同战斗音效按钮做法）；`aria-pressed` 反映当前显示状态，隐藏态按钮高亮（#fff3d2）。
- 英文词条：en.txt 新增按钮标题两条、toast 两条；`scan_locale.mjs` 无缺词。
- 测试：`tests/adventure_storage_split.test.mjs` 新增"mount visibility preference is device-local…"（durableSave/split/coreCatalogKey 均剔除、runtime 恢复、revision 变化后仍保留、重新打开角色仍生效），storage/magic-star/follower/pet-mount 定向 29 项全过；`npm test` 642/645（3 项失败均为既有问题：mount_pose 缺 data/mount-demo/mounts.json、quest-catalog 哈希漂移、release hash 环境问题）；`npm run build` 通过。未提交、未发布。

### 坐骑/翻译按钮与英雄卡同 z 序

- 用户截图反馈：`.mount-toggle` 与 `.locale-launch` 用 `position:fixed; z-index:80` 浮在整个窗口上（挂 body 或独立于 HUD），窗口小的时候盖在英雄卡上。改为都挂进 `#hud`（`position:absolute`，无 z-index），与 `.hero-status` 同一堆叠上下文：弹窗、toast 等覆盖它们，与英雄卡行为一致。
- `locale.js`：`installLocaleTooltip` 把 launch 挂到 `doc.querySelector('#hud')`（无 #hud 的夹具回落 body）；`syncLocaleChrome` 改用 `hero.offsetLeft/offsetTop/offsetHeight` 定位（同一坐标系，不再用 getBoundingClientRect）。调用时机在世界启动后，#hud 为静态 DOM，无时序问题。
- `view_adventure.js`：`mountToggle` 定位同样改 offset 坐标（status 是 #hud 的 absolute 子元素，offsetParent 即 #hud）。
- `css/adventure.css` L731-737：两个按钮 `position:fixed;z-index:80` → `position:absolute`；`.locale-gloss` 拖拽翻译面板保持 fixed（独立浮层，有意为之）。
- 验证：language/mount/dialogue 定向 39 项全过；`npm run build` 通过。未提交、未发布。

## 云端旧分文件见证不一致的容错读取

- 用户真实账号登录后反复报「云端操作未完成。请检查网络或重新登录；本地进度仍然保留。」，F12 控制台拿到原始错误：`joinRoleSave` 抛 `物品数量账本与收藏不一致`（adventure_storage_core.js），即近期物品存储格式调整后，云端旧分文件的见证字段（items.ownedItemIds 账本、战斗背包副本）与现行拆分规则不再匹配，`readRoles` 的 schemaVersion 2 合并路径被裸 Error 打断。
- 诊断增强：`adventure_cloud.js` `guarded()` 对非 CloudError 异常先 `console.warn('cloud operation failed, original error:', error)` 再包装为通用文案，此后真实病因可直接在 DevTools 看到。
- 容错读取：`joinRoleSave` 新增可选 `{strict:true}` 参数（默认严格，原有调用不变）；`readRoles` 合并分文件先走严格校验，失败时 `console.warn` 后以 `strict:false` 重试——只合并主字段（state+items+battle+records），跳过两处见证校验（ownedItemIds 账本一致性、split/join 往返一致性），`restoreRuntime` 照常执行。旧格式数据主字段完整可载入，下次核心变化同步时按现行格式重写分文件自愈；本地与云端进度均不丢弃。
- 验证：`tests/adventure_storage_split.test.mjs` 新增"tolerant join merges primary fields when old part witnesses or distribution drift"（账本漂移与字段跨文件错位两场景，strict 抛错、strict:false 合并后 durableSave 与原存档一致）；`tests/adventure_roles.test.mjs` 新增"drifted cloud part witnesses load primary fields instead of blocking the account"（SDK 级 mock 篡改远端 items 分文件 ownedItemIds 后 roles() 正常返回、revision 与 inventory 不变、console.warn 有 consistency join 记录；读取用新 client 实例避开 partCache）。定向 50/50；全量 `node --test tests/*.test.mjs` 648/650，2 项失败均为既有 `data/mount-demo/mounts.json` 缺失（packed_startup 与 compact packs），与本改动无关。未提交。

## 随行宠物聊天入口改小号 Talk 图标并缩小点击热区

- 按用户截图反馈：宠物头顶的"咪咪雪泡 · 和我聊聊"文字气泡改为小号 Talk 聊天气泡图标。`adventure_renderer.js` `drawSpeechBubble` 不再绘制文字，改为 24×16 奶油色圆角气泡 + 向下小尾巴 + 三个棕点（#fffbe9 底、#7a6335 描边，正弦浮动、遵循 reduced motion），命中矩形跟随图标本体（外扩 4px）；`language_adventure.js` 的 `bubble` 文案 getter 保持不变（测试与邀请语音仍用）。
- 宠物本体点击热区按用户要求缩小：`adventure_app.js` `clickWorld()` 由 80×96（|dx|<40、y-72..y+24）收窄到 44×68（|dx|<22、y-56..y+12），气泡图标仍按其自身命中矩形独立可点。
- 验证：`node --test tests/language_adventure.test.mjs tests/adventure_storage_split.test.mjs` 30/30 通过；`npm run build` 通过。未提交。

## 修复三项既有测试失败

用户要求把长期记录在案的既有失败全部修掉。先做的基线核对：全量并行跑出的失败项**每次都不一样**，串行跑只剩 3 项，且部分堆栈行号与当前文件内容对不上——查 `stat` 发现 01:11–01:59 期间 `js/adventure_renderer.js`、`js/language_adventure.js`、`js/adventure_storage_core.js`、`js/hero_preview.js` 被外部编辑器连续改动，测试读到半改状态。稳定后单独复跑，7 项里 4 项（follower render / movement / spell_environment / packed_startup）自行通过，真正需要修的是 3 项。

- **mount_pose 缺 `data/mount-demo/mounts.json`**：该目录从未入库，`js/mount_demo.js` 也没有任何 HTML 引用它（孤儿实验室）。按 `scripts/draw_mount_demo.py` 的鞍子位置换算鞍位（格 280×220、锚点在底部中心，seat = saddle − (140, 220)），手写 dragon / car / carpet / wings 四只 + rider，落成 `tests/fixtures/mount-demo-catalog.json`，`tests/mount_pose.test.mjs` 改读夹具并注明换算来源。
  - 先误放在 `data/mount-demo/`，`package_runtime_data.mjs` 立刻报"未配置的数据包"，`packed_startup` 与 `runtime_data`（五包断言）两项挂掉——`data/**` 需显式登记且会进用户下载包，演示数据不应放 `data/`。
- **quest-catalog 依赖哈希漂移**：`monster-catalog.json` 的 `questCatalogSha256`、`boss-art-plan.json` 的 `monsterCatalogSha256` 都是过期戳记（`petCatalogSha256` 是对的）。quest-catalog.json 是最后一次重新导出的、下游没跟上，交叉引用断言（goalIds / mainStoryQuestIds / boss job model 与 sourceSha256）全过，说明内容未漂移、只是戳记旧。本机 `assets_manifest.txt` 与当初导出环境不一致，`export_monster_catalog.py` 重跑即崩（`int('')`），无法整表重导，只改戳记：quest → `784a94f3…`；改完 monster-catalog 自身摘要变成 `d1b400d9…`，再同步 boss-art-plan（顺序不能反）。
- **release hash 测试**：`tests/release.test.mjs` 用 `spawnSync` 拉起 `uploadRelease.mjs`，本环境对任何可执行文件都返回 EBUSY（status=null），异步 `spawn` 正常。改为 Promise 包 `spawn`，断言 `await run()` 的退出码，端到端语义不变。

验证：`tests/mount_pose.test.mjs`、`tests/quest-catalog.test.mjs`、`tests/release.test.mjs` 定向 14/14 通过。全量仍剩 2 项失败，属他人在途改动，未动：`tests/hero_picker.test.mjs` 仍 import 已重命名为 `splitRoleSave` 的旧导出 `splitDurableSave`；`tests/hero_preview.test.mjs` 读的 `data/hero-preview.json`（未入库）有条目缺 `cdn` 字段。未提交、未发布。

## 坐骑按钮改为 emoji 图标并加隐藏禁止标志

- 按用户反馈：坐骑显隐按钮的矢量兜底图形（`paths.mount` 手绘路径）在 36px 按钮里观感差。改为 emoji 方案并删除已无引用的 `paths.mount`。
- `js/view_adventure.js`：新增 `mountEmojiKeys` 关键词表 + `mountEmoji(name)`，按当前坐骑商品名（`c.mountByItem?.[save.mountId]?.name`）匹配 emoji，关键词从具体到一般排序（狼蛛→🕷️ 先于 狼→🐺），未命中回落 🐲（抱抱龙）。按钮内容改为 `span.mount-emoji`；`save.mountHidden` 时叠加 `span.mount-emoji-stop`（🚫，绝对定位居中）。
- `css/adventure.css`：`.mount-toggle svg` 规则替换为 `.mount-emoji`（20×20，字号 18px）与 `.mount-emoji-stop`（居中 15px）；`aria-pressed=false` 时 emoji 透明度 .45（同战斗音效按钮做法），背景高亮沿用。
- 用脚本核对 mount-catalog.json 全部坐骑商品名：100+ 条名称均有合理映射（变身药丸十二生肖、凤凰/神鹰→🦅、猛犸→🦣、南瓜车→🎃、雪地车→🚙 等）；四个非骑乘演示坐骑（dragon/bird/car/carpet）不进 `mountByItem`，不受影响。
- 验证：`npm test` 652/652 全过；`npm run build` 通过。未提交、未发布。

## 坐骑按钮微调：圆形 + 半透明 SVG 禁止标志

- 按用户反馈两处调整：`.mount-toggle` 由 10px 圆角方形改为正圆（`border-radius:50%`）；隐藏态的 🚫 emoji 整枚覆盖太死板，改为半透明 SVG 禁止标志（圆圈 + 45° 斜杠，#c0392b 描边 2.6、整体 opacity .7），叠在 emoji 上方 24px 居中，底图 emoji 仍保持 .45 压暗，禁令与坐骑形象同时可辨。
- 验证：`npm test` 全过（fail 0）；`npm run build` 通过。未提交、未发布。


## 创建角色滑动翻页与多发型头部

- 用户确认6组男女后追加更多不同发型，实际新增8组共16个16方向头部，男女各9款含原版；使用内置image_gen生成，提示词保存于art-references/hero-head-variants.json。WebP保存在assets/hero-preview，新增最大154,308字节，CDN上传及24张完整素材哈希/CORS核验通过，未同步Git镜像。
- 创建页新增独立男女轮播，支持箭头、键盘方向键、横向拖动，避免误触纵向滚动；两种性别独立记忆，步骤往返保留选择。headId随角色保存并贯穿通用渲染与各UI，不改身体或坐骑定位。
- 校验：完整npm test 652/652通过；后续定向24项通过（含新增自定义头像路由、存档拆装、手势取消与循环测试）。构建通过；浏览器390px确认横滑只翻一页及切换性别保留选择。独立验收页不读写玩家存档。

## 男女主角四方向精灵步行

- 新增男女各24帧步态图集，原四向静态身体保留作停步与回退。通过image_gen以现有身体为参考生成；透明无损WebP分别175,884/195,854字节，均低于200KB，新增打包脚本及来源/锚点/帧坐标记录。
- HeroRenderer加载并绘制六帧循环，换头使用每帧脖子锚点。hero_pose_core按实际位移推进独立视觉步态，不动存档或战斗RNG；碰墙/停止回静止，减少动态效果关闭步态，骑乘布局不变。移除原移动时整张静态身体的上下弹跳。
- 两张素材上传Keepwork CDN并验证SHA-256/CORS；未发布网站、未提交或推送，未复制额外Git镜像。
- 验证：npm test 656/656通过，npm run build通过（首次构建碰到其他在途设置页语法，恢复后重跑通过）。浏览器本地/CDN、男女×四向键盘移动、停步、减少动态、390px触摸拖动通过；正式场景用隔离内存角色确认每向0–5全部帧及无位移停步。逐帧接触表校验透明边缘和头身比例。

## 旅途设置改版为页签式选项菜单

- 用户反馈设置页不够游戏化且需兼顾手机与桌面：重构为旅途/声音/语言/关于四个页签，页签条吸顶并复用 Shared tabs 选中语言；内容按分区卡片组织，动作做整行可点单元格（emoji 图标+说明+箭头），开关做右侧药丸按钮（已开启/已关闭，aria-pressed）。
- 新模块 `js/view_adventure_settings.js`（页签与分区编排、`settingsView.tab` 内存态）与 `js/view_settings_controls.js`（section/cell/link/toggle/field 共享控件，注入 el/button）；`view_language_learning.js` 的 `languageSettings` 改用同一控件，签名不变，语言验收 fixture 不受影响。
- `view_adventure.js` 设置分支只调用 `renderSettings` 并再导出 `settingsView`；`adventure_app.js` 打开设置时重置页签为旅途。页签切换原地显隐内容区，焦点不丢；开关等触发整面板重绘时保持当前页签。
- 样式追加在 `css/adventure.css` 尾部，颜色全走共享变量，绘本主题自动生效；390px 下页签四列、语言选择四列（≤360px 两列），开关行允许换行。`.settings-button` 仍被云存档恢复按钮使用，保留。
- 新增文案（页签、分区、开关、提示）同步 `data/adventure/locale/en.txt`；ja/ko 仍回落中文。
- 核验：npm test 656/656、npm run build 通过；本机无 Playwright，改用无头 Chrome + CDP 截图脚本（.cache/settings-check/shot.mjs，不入库）核验新增 `tests/fixtures/settings.html` 在 860px/390px 下四个页签的呈现。未提交、未发布。

- 最终浏览器完整创建流程确认wave-girl序列化回读、场景/战斗/骑乘一致；新增素材已在默认CDN创建预览加载。末次构建与24项定向回归通过，构建包包含18种头部清单且不包含WebP。未提交、推送或发布网站。

## 女主全部头型及男主侧面头颈校准

- 根据用户截图，将头部统一底部中心锚点改为各头型/方向的颈部连接点。九款女头覆盖16向；九款男头只调整左右侧面及相邻转头角，保留前后视图。
- 新增art-references/hero-head-anchors.json与scripts/calibrate_hero_heads.py；两种打包器都应用相同配置，预览及正式清单一致。图片字节、CDN、身体尺寸与坐骑座位未变。
- 浏览器检查18款四向静止及行走接触表、女头斜向与骑乘拼合；17项头像/坐骑相关测试和构建通过。无需上传图片，未提交或推送。

## 云端旧分文件自愈重写

- 容错读取只解决了"能进游戏"，但坏文件永不修复：`saveRoles` 在核心内容未变化时早退（`coreCatalogKey` 相等），且未变化分文件按内容比较复用旧路径，旧格式见证文件被一直引用，每次加载重复告警。
- `adventure_cloud.js`：`readRoles` 返回 `partsStale`（容错合并的角色 ID 集合，供 `saveRoles` 内部重读时静默）；`saveRoles` 存在漂移角色时跳过早退，且对漂移角色不复用旧分文件，全部按现行格式重写。
- `adventure_app.js`：`syncRoles(force)`；`connectRoles` 登录加载后（含无激活角色的分支）与角色列表刷新检测到 `partsStale` 即强制同步，进度无变化也能完成云端重写。
- 新增回归测试 `drifted cloud part witnesses self-heal on the next save so later loads stay strict`；全量 657/657 通过，构建通过。未提交。

### 女主头部继续下移

按用户右侧短辫少女截图复核：九款女头在第一轮锚点基础上整体下移8个头部图格像素，左右侧面再下移6像素，相邻转头角平滑过渡；男主配置不变。已同步预览/正式清单，浏览器复核衣领衔接，相关17项测试通过。

### 修复预览页缓存导致校准不可见

用户再次反馈页面未更新，实际IAB仍加载旧脚本/清单。为预览入口模块增加版本参数，校准页JSON请求加入每次加载标记并使用no-store；清单记录由锚点配置计算的校准哈希，页面显示当前加载版本。已在用户当前标签页验证b5ee2f32da，栗发少女右侧头部实际下移并贴合衣领。此前“当前页面已更新”的验证不充分，现以页面显示版本和可见结果同时确认。未改动图片。

### 女主头颈下移量折半

用户确认新版生效后反馈下移过量。将九款女头上次新增下移量折半：相对首轮整体下移4个头部图格像素、侧面额外3像素；栗发少女右侧锚点由[84,103]回调为[84,110]。正式与预览清单一致，当前浏览器显示f6fa5681f8并已目视核对。男主及图片不变。

### 栗发少女右侧头部微调

按用户箭头，将栗发少女右侧头部向右移6个图格像素（锚点[84,110]改为[78,110]），邻近角度按2/4/6/4/2平滑过渡；高度与其他头型保持。预览及正式清单已同步，当前页面核验版本6f30c5b98f。

### 男女朝前步行身体上移

按用户反馈，仅将男女朝前行走的六帧身体上移5个图集像素（默认78px角色约2.44px），头部连接位置保持不变，收紧颈部与衣领。其他方向、静止和骑乘不变。frame.bodyOffsetY写入两份清单，打包器同步保存；预览模块更新版本以绕过旧缓存。17项相关测试通过，当前页面已更新至front-walk-lift-1。

### 整理主角头身定位快速调整指南

按用户要求，在docs/hero-preview.md补充“下次快速调整头身位置”：修改入口、锚点反向坐标规则、方向索引、同步清单命令、缓存版本确认、静止/行走/骑乘检查，以及本轮过调回退和身体上移的当前基线。仅更新文档。

## 坐骑按钮禁止标志放大并去掉压暗层

- 按用户反馈：禁止标志太小几乎看不清。SVG 由 24px 放大到 28px（36px 按钮内接近满圈，仅比按钮略小）；删除 `aria-pressed=false` 时 `.mount-emoji{opacity:.45}` 的灰蒙蒙压暗层，隐藏态只保留禁止标志本身与底色高亮，emoji 原色透出。
- 验证：`npm run build` 通过。未提交、未发布。

## 坐骑按钮禁止标志改灰色并占满按钮内径

- 按用户反馈：禁止标志由红色改为灰色（#6f6f6f，保持 .75 半透明），尺寸由 28px 放大到 34px，几乎与 36px 圆形按钮内径（含 1px 边框）同大；viewBox 内圆半径同步加到 10.2、斜杠两端延到 4.8/19.2，描边略减到 2.2 保持比例。
- 验证：模块加载 OK；`npm run build` 通过。未提交、未发布。

## 坐骑按钮禁止圈撑满按钮并缩小 emoji

- 按用户参考图反馈：禁止圈要明显大于坐骑图标。SVG 尺寸由 34px 放大到 36px（与按钮同大），viewBox 圆半径加到 11（外缘几乎贴按钮边框）、斜杠延满 4.2–19.8，描边减到 1.9 保持纤细；emoji 字号由 18px 缩到 15px，圈与图标的大小对比更接近参考图中的头像圆环效果。另给 .mount-emoji-stop 加 pointer-events:none，点击穿透到按钮本体。
- 验证：模块加载 OK；`npm run build` 通过。未提交、未发布。

## 坐骑按钮禁止圈与按钮外圈重合

- 按用户反馈仍不够大：禁止圈改为与按钮外圈对齐。SVG 放大到 38px（超出 36px 按钮，flex 默认 overflow 可见），viewBox 圆半径 11.3，实际画出的圆直径 ≈ 35.8px，与按钮可见外圈（36px 含 1px 边框）基本重合；斜杠延满 4–20、描边 1.8。这样禁止标志就像套在按钮边框上的灰色圆环 + 斜杠，与参考图的外层圆环一致。
- 验证：模块加载 OK；`npm run build` 通过。未提交、未发布。

## 坐骑按钮改用当前坐骑形象图

- 按用户要求：图标不再用固定 emoji，改画当前骑乘坐骑的真实形象。`renderHud` 取 `c.items[save.mountId].art`（`installMountCatalog` 写入的图集第一格裁剪，与装备栏/宠物栏同源），经 `art(assets, ref, 20, 20)` 绘成 canvas；坐骑无形象数据（`art` 缺失）时回落 `mountEmoji` 名称 emoji，规则保留作兜底。
- 结构调整：外层改为 `span.mount-mark`（20×20 容器，canvas 或 emoji 内嵌），禁止标志 `mount-emoji-stop` 挂在容器上（canvas 不能有子节点），SVG 尺寸/颜色/圆圈与按钮外圈重合的样式不变。
- 验证：模块加载 OK；`npm test` 全过；`npm run build` 通过。未提交、未发布。

## 坐骑按钮形象图放大并允许出血

- 按用户反馈：形象图太小，允许出血填满圆形底。canvas 由 20×20 放大到 34×34（接近 36px 按钮外径，边缘可超出圆形米色底），`.mount-mark` 容器同步 34px；`.mount-toggle` 显式 `overflow:visible` 保证出血不被裁剪。emoji 兜底与禁止圈样式不变。
- 验证：模块加载 OK；`npm run build` 通过。未提交、未发布。

## 背包钱包货币改图标显示

- 按用户截图反馈：「我的背包」弹窗头部右侧的「仙豆 2190」「奇豆 855」纯文字余额改为与左上英雄卡一致的图标样式。`view_adventure_equipment.js` 钱包改用 `walletEntry(id,label)`：32×32 canvas 绘制 `currency-icons.json` 图集裁剪（17213 仙豆 / 100 奇豆，`assets.draw` 与英雄卡 `hero-currency`、商城 `shop-bean` 同源），CSS 以 `.equipment-bean-icon` 缩到 16px，`.equipment-wallet-item` 行内 flex 排布。
- 豆子名称不再随数字显示，保留在 `title`/`aria-label`（`tr(label) + 数量`）与 `data-zh`（供语言学习词卡），数据格式与英雄卡一致。原 `setText(fairySpan,'仙豆 {count}')` 文案路径删除。
- 验证：无头 Chrome CDP 截图（本地静态服务，走完建角三步后点开背包），头部渲染两枚豆子图标 + 数量；`npm test` 全量通过；`npm run build` 通过。未提交、未发布。

## 角色选择界面英文本地化修复

- 用户截图反馈（英文 locale）：角色选择界面标题、按钮均已翻译，但角色卡元信息「等级 5 · 寒冰」、底部「已有 3 / 5 个主角」仍为中文。
- 根因：`js/view_adventure_roles.js` 直接用模板字符串 `` `等级 ${s.level} · ${SCHOOL_NAMES[s.school]}` `` 拼接，`el()` 内 `tr()` 按整句查 `en.txt`，含变量的句子无法命中键。
- 修复（与其他视图一致的做法）：改为 `setText(node, 模式, 变量)`，系别名经 `fill` 的 slot 翻译：
  - `Keepwork：{owner}`（原 `Keepwork：${model.owner}`）
  - `等级 {level} · {school}`（角色卡）
  - `已有 {count} / {total} 个主角`（底部计数）
  - `{name} · 等级 {level} · {school}`（云端冲突列表）
  - 头像 aria-label `魔法少女/魔法少年` 改经 `tr()`（键已存在）
- `data/adventure/locale/en.txt` 在 `# js/view_adventure_roles.js` 分节新增上述 4 条键。
- 验证：Node 实测 `fill` 输出 `Level 5 · Ice`、`3 / 5 characters created`、`Keepwork: lixizhi`、`deluca · Level 50 · Fire`；locale 扫描 roles 无缺失键；`npm test` 657/657 通过；`npm run build` 通过。未提交、未发布。

## 无文字精灵主角品牌图标

- 使用内置 imagegen 将新标志可见圆耳改为精灵尖耳，保留巫师帽、面部和无文字风格；无损WebP母版与原PNG哈希入库。
- 生成8档32至1024透明WebP、Windows ICO/macOS ICNS/PNG、Steam 184 JPG、Android商店512 PNG及五档Android启动/圆形/自适应前景，共28个输出。
- 13张独立素材上传Keepwork CDN并逐项GET核验SHA-256和CORS；WebP最大164304字节。CDN与尺寸清单见art-references/magic-haqi-logo.json。
- Android资源及Electron打包/窗口图标已接入；Steamworks商店图标仅准备文件，未登录后台或发布商店。
- 验证：28图解码、alpha、WebP预算、ICO各帧、安全区与XML检查通过；Android aapt2资源编译通过。未改战斗逻辑，不执行全量战斗回归。详见docs/brand-icons.md。
- Windows打包验证：electron-builder在解包目录rename时EPERM；确认既有Windows补丁已应用后隔离目录重试仍停留在packaging，已停止本轮测试进程。未生成可验收的新Windows安装包，既有安装包/Steam depot未覆盖。
- CDN Git镜像复制已按技能单独询问，未获授权前未执行；本项目内素材副本已保存。

## 盾、术与陷阱按真实顺序演出

- 修复整轮先结算导致头顶状态读取回合末结果的问题。`view_battle_presentation.js` 仅在本次回合结算期间收集独立状态快照，动画逐事件更新头顶图标、悬停详情和队伍状态；快照不进入事件日志或存档。
- 施法完成后立即显示新增盾、术、陷阱与持续效果，保留700ms可见反馈。头顶图标由28px放大至约48px并亮起；盾消耗时分成四片散开，术/陷阱使用时放大闪光后消退。减少动态效果偏好关闭位移和碎片。
- 引擎添加可选、只读的状态观察回调，不修改公式数值、RNG或事件记录。术、普通盾、陷阱与棱镜按实际逆序消费逐个通知；同base ID每次伤害只用一层，剩余叠层仍显示。吸收盾按既有FIFO顺序逐层反馈，剩余容量降低不误判为破盾。技能尾部静默附加的陷阱等也在本技能结束时采集。
- 回归覆盖真实结界守护、术先于目标盾消费、叠加陷阱2→1→0、棱镜转系后的盾匹配、DoT触发与剩余回合、吸收容量和顺序、观察前后事件/最终状态/RNG完全一致。
- `npm test` 668/668通过。浏览器通过共享绘制模块的轻量只读页 `tests/fixtures/battle-status-timeline.html` 核验图标放大、两层陷阱、术→陷阱→破盾，以及390px内容宽度；未操作真实角色存档。完整资源预览遇到浏览器目标关闭，未完成本轮实际角色完整对战视觉验收。
- 未提交、未推送、未发布；保留工作区原有其他修改。
- 最终 `npm run build` 通过（首次重验写入既有 `official-website.json` 遇到临时UNKNOWN错误，重试成功）；本次改动文件 `git diff --check` 通过。仓库其他既有 `.gitignore` 尾随空白未改动。

## 五系之敌说明、强制施法失败与MISS

- 修复生命之敌等五系卡详情仅显示效果名称和“未完整收录”的问题，明确下一次匹配学系攻击/施法失败、只消耗一层、其他学系不触发，以及原版扣魔力和怪物免疫例外。同步英文词条。
- 核对原版后补齐此前遗漏的dispel_school执行逻辑；先掷命中随机数再消耗之敌，自然失误不扣魔力，命中通过的强制失误先扣魔力。新冒险检查点增加dispelRulesVersion=1，旧战斗重演保持原规则，需下一场战斗使用新判定。
- 所有fizzle在施法者处浮出MISS；对应之敌头顶图标放大碎裂，显示具体名称（例如“生命之敌破掉”），同类剩余层保留。MISS与破除文字分开摆放。
- 全量672项测试通过，4项新增测试覆盖学系匹配、只消费一层、魔力、自然失误、免疫、旧PvE规则、五系说明及Canvas文案。生产构建通过。浏览器390px内容宽度轻量预览 `tests/fixtures/battle-status-timeline.html?dispel&mobile` 核验技能说明、图标碎裂、生命之敌破掉与MISS；未操作真实角色存档。
- 未提交、未推送或发布。

## 游戏启动画面替换（用户预览批准后）

- 用户审阅内置imagegen生成的横/竖版魔法浮岛、精灵主角标志启动画面后明确批准，才替换Android默认及五档横/竖共11张splash.png。
- 无损WebP母版入art-references，加入可复现尺寸准备脚本与资源哈希清单；中心裁切保留比例、主角帽子和尖耳。
- 两张运行时WebP上传Keepwork CDN，逐项GET验证SHA-256与CORS，均小于200KB；原生PNG不进入H5 dist。
- 已完成11张尺寸/解码/哈希检查及全尺寸裁切图目视核验；未操作Steamworks、应用商店或签名APK发布。未获额外Git镜像复制确认，未写入digitalhuman-resource。
- Android aapt2资源编译通过；CDN横版193340字节、竖版186234字节。

## 魔法营地新增码头

- 用户要求魔法营地与其他岛屿一致拥有码头。islandBuildings 原先只对 town/fire/ice/desert/dark 开放，camp 被排除。
- js/adventure_buildings_core.js：camp 纳入船坞生成；村庄/塔楼散布仍限五大岛（营地保留自带建筑）；camp 无独立建筑图集，码头与小船复用小镇图集；小船落位 dy 改为按地图下边缘收拢（min(210, h-40-harbor.y-10)，并要求 y>harbor.y+60），营地为 1 艘、五岛仍为 2 艘。
- harborAccess 通用逻辑生效：营地原版船长 36205 与传送点迁至码头旁 (1084.8,1372)，引路自原传送门 (1000,1430) 接入且全程可走；营地传送门路牌保留在道路尽头。
- tests/island_buildings.test.mjs：营地断言改为存在 town 图集码头、无村庄/塔楼、船只在海上；船坞连通测试两套内容（无目录/有目录）均纳入 camp。tests/fixtures/island-buildings.html 增加营地选项，村庄/塔楼视图回落码头。
- npm test 672/672 通过；本地静态服务+CDP 无头截图核验营地码头渲染正常。docs/adventure.md、docs/qa-report.md 已更新。未提交、未推送。

## NPC对话点击行为改版

- 用户截图反馈：点击霸王虎坐骑对话的空白区域会直接打开默认操作（商品面板）。改为三段式：框内非控件区域点击无效果（仅打字未完成时补全全文）、框外空白点击关闭对话、默认操作只由按钮触发；空格/回车快捷键经用户确认保留。
- js/view_adventure_dialogue.js：bindDialogue 新增 close 选项；click 处理器删除"非控件点击即 advance"分支，先判 box.contains，框外立即 close，框内仅在 !ready 时补全文字；键盘路径不变。js/view_adventure.js renderDialogue 传入 cb.close。
- 提示文案与 en.txt 两个键同步更换；新增 tests/fixtures/dialogue-click.html 供真实 DOM 点击核验。
- tests/dialogue_ui.test.mjs：box 模拟补 contains（backdrop 对象区分框内外），重写点击断言并新增空格触发断言。npm test 668/668 通过；CDP 无头实测四项点击/键盘行为全过。未提交、未推送。


## 冰霜魔镜反向技能演出与头顶状态

- 核对 card_server.lua L3312–3398：普通吸收后整次直接伤害触发反射，反射容量逐次消耗，耗尽破碎；DOT仅消耗容量，反射不递归、不致死。本轮保留既有 reflectionRulesVersion=1 数值与旧检查点兼容。
- 播放层在 reflection 伤害事件前插入原卡牌、交换施法者/目标的视觉施法，复用原技能动画、音效及受击时序；不产生引擎施法、额外费用或随机消耗。反射演出不显示原技能的附带治疗、持续效果或吸血回流。
- 头顶状态使用蓝色椭圆镜面，容量未耗尽时保留并闪动，耗尽时复用逐层碎裂动画并提示“魔镜破碎”。补齐卡牌说明与英文翻译、反弹战斗日志。
- 原版 L3401 起使用专用 Ice_ReflectionShield_reflection 演出；按用户要求，网页改用来袭原技能反向演出，数值不变。
- 定向46项、完整 npm test 674项及 npm run build 通过。新增真实冰霜魔镜的容量保留/破碎/反向卡牌与目标测试，对照无观察器战斗确保事件、最终状态及RNG不变。
- 浏览器隔离内存验收页 tests/fixtures/battle-status-timeline.html?mirror（另加 &mobile 为390px内容区）确认镜面、火焰原技能返回方向和碎裂。使用矢量与现有特效，无新增美术或CDN上传；未操作真实玩家存档，未提交、推送或发布。

## 卡牌效果收录范围约定：仅儿童版（kids）

- 用户明确（2026-09-26）：卡牌效果说明、效果文案与 en.txt 翻译补录等面向用户的内容工作只覆盖 `data/kids/`；不移植、不补录、不翻译青年版（teen）内容。
- 已写入 AGENTS.md 硬规则第 10 条与 docs/card-i18n.md「范围约定」；teen 专属 type（Global2、Absorb_Adv、SingleAttackHP、AreaControl 等）保持现状。

## 战斗结束统一结算符文消耗

- 按用户要求，取消每回合背包符文扣除，累计消耗留在本机 pendingEncounter 中；战斗结束或撤退统一扣除。
- 保留战斗中禁止云同步的既有门禁；与宠物结算中的旧晶球迁移协调，防止重复扣除或覆盖迁移所得符文。
- 重演校验同时核对背包与累计消耗，兼容旧版中途存档。
- 验证：npm test 全量 675/675 通过，覆盖结束、撤退、恢复、旧版已扣库存及重复结算。未连接真实服务器，未提交或推送。

## kids 卡牌效果补录（35 类）与姿态引擎对齐

- 起因：用户截图（死亡巫术）反馈卡牌说明出现「此牌的特殊效果说明尚未完整收录」。脚本扫描 kids 701 张卡，217 张会触发该提示，涉及 35 种 type。
- `js/card_description_core.js` 重写效果描述：新增 stats 串解析（standing ward / miniaura）、单次触发类 charm/ward 统一文案（含 boost_accuracy / boost_heal，即截图中的「死亡巫术」= 下一次治疗 -50%）、全局场地光环、6 种 kids 姿态（按引擎实际结算：fire_kids 全队攻击 +15%、storm_kids 全队暴击 +20%/暴伤 +20%、death_kids 韧性/命中/受治疗、life_kids 穿透、ice_kids 全队受伤 -10%、defensive 攻防 -25%+仇恨 ×3）、眩晕族（含 4 层眩晕抵挡说明）、小光环、溅射引暴、对等陷阱、隐身、持续陷阱（stats 结算）与未实现卡（冰封/护盾转换/替身守护/激怒，明确标注「当前模拟器未实现」）。系统卡（Dead/PickPet/Fizzle/HoT/DoT）标注为系统牌。
- 引擎对齐（以 Lua 为准，记入 docs/lua-mapping.md）：kids 姿态同队加成补齐（player_server.lua L2660-2686/L2759-2784/L2816-2840/L2945-2969/L3045-3069/L3621-3641）；SingleAttackWithPercent 上限按 target.isMob 选 damage_max_mob/damage_max_player（card_server.lua L3281-3290）。combat_unit_core 相关 Get* 增加 arena 参数，combat_cards_core 调用点同步，新增 getCriticalStrikeDamageRatioBonus。
- en.txt：新增约 90 条词条（含 2052/1052/3052 ward desc「忽略55/60/65%风暴抗性」），更新 1 条被改写句式；scan_locale 缺失回落至基线 124。
- 新增 tests/stance_sibling.test.mjs（同队加成 + 百分比上限，2 项）。npm test 677/677、npm run build 通过。701 张 kids 卡英文化零中文残留。未提交、未推送。

## 首页显示语言选择

- 用户需求（截图）：首页（标题画面）要能直接选择界面显示语言，此前只在游戏内设置窗「语言」页可切。
- 共享控件下沉：`view_settings_controls.js` 新增 `localeSelector(current, onPick)`（中文/English/日本語/한국어，当前项主色 + aria-pressed + lang 属性），设置窗 `languageSettings` 改用它，`renderEntry`（创建向导左栏）与 `renderRoles`（角色列表底部）接入；角色列表忙碌态与其他按钮一起禁用。CSS 仅新增 `.entry-locale` 排布（标签颜色随深色题头/纸色卡片继承，720px 以下居中）。
- 持久化分层：`setLocale` 写入本机偏好 `haqi.locale.v1`（localStorage），`readBootLocale` 与 `applyLocale`/`enterWorld` 的 `configureLocale` 均优先本机偏好、回落角色存档 locale；词典加载取角色存档语言 ∪ 偏好语言的并集。角色存档 locale 继续随云同步；新建角色沿用首页当前显示语言（`next.locale=displayLocale()`）。
- 附带修正：`configureLocale` 统一同步 `document.documentElement.lang`；`setLocale` 同步 `document.title`，修复切换后标签标题停留在旧语言。
- 验证：`npm test` 679/679 通过（新增 readBootLocale 偏好优先/非法值忽略 3 项断言）；`scripts/scan_locale.mjs` 缺失保持基线 124，新用文案（界面语言）词典已有；`npm run build` 通过。本地静态服务 + 无头 Chrome CDP 实测：创建向导与角色列表两处选择器渲染正常，切 English 后整页（标题、按钮、角色列表）重绘、toast 提示、刷新后偏好保留、切回中文正常；390px 手机宽度无横向溢出；英文状态下新建角色表单为英文。未提交、未推送。

## 魔法营地教学指针

- 用户需求（截图）：参考原版，在魔法营地用指针动画教玩家点击任务面板的「追踪」；不看等级，只要身处魔法营地即视为教学模式开启。
- `view_adventure.js` `appendTrackerQuest` 新增教学判定：`save.zone==='camp'` 且当前追踪条目是营地任务（chapter 看 startNpc 所在 zone，目录任务看 region）且未接取时，给「追踪」按钮加 `.teach-target` 高亮并叠 `.teach-pointer`（👇 手势，aria-hidden）；每帧只标第一个符合条件的任务。
- `adventure_app.js`：`model()` 增加 `autoWalk:!!destination`；主循环 `frame` 调 `V.syncTeachPointer(nodes.hud,!!(destination||dialog||panel))`，自动寻路或对话框/面板打开期间隐藏手势与脉冲（`.teach-walk`），寻路结束后由下一次 HUD 渲染重新判定。
- CSS（`css/adventure.css`）：手势绝对定位于按钮右上方（`bottom:55%`），避免被追踪面板 `overflow:auto` 裁剪（首版 `bottom:-36px` 伸到面板外被裁，已修复）；按钮金色脉冲 `teach-pulse`、手势纵向敲击 `teach-tap`；`prefers-reduced-motion` 下停用动画。
- 验证：npm test 679/679 通过；本地静态服务 + 无头 Chrome CDP 实测：新建角色进入魔法营地后追踪面板出现手势与脉冲（元素级截图确认渲染）、点击「追踪」后自动走向青龙并打开对话（dialogueTitle=青龙），期间手势隐藏（hidden=true、teach-walk 类生效）。未提交、未推送。

## 2026-09-26：偶遇式双语学习支线

本节取代下文旧的固定学习路线与青龙兜底入口描述。左上角坐骑区域新增“双语学习”介绍窗口：先选择显示语言、目标语言，再确认开启；已有语言偏好保留，新角色默认中文界面学英语、英文界面学中文、其他界面学英语。旧档没有选择标记时保留已有有效目标语言，不猜测用户过去是否手动选择。

宠物保持原点点点，仅在150世界单位内有启用且具有可用课程的NPC时出现；无NPC不显示、不兜底青龙。点击锁定实例，走到85单位内，NPC显示该故事第一句问题及可选释义，主角显示点点点；2.5秒后打开同一个故事。点击NPC台词可提前进入，点击别处或手动移动取消。演出不计口语完成，自动朗读打开时只播放一次开场。参数集中于BalanceParams.languageAdventure。

青龙、杰西卡、法斯特船长改为9个手写零基础故事，每个三轮，作者源为scripts/camp_beginner_stories.mjs，生成器仍为scripts/prepare_camp_conversations.mjs。每位都有无主线前提的入门故事，其余按63000/63006/63008/63009/63010已交付状态及实际宠物条件开放。优先未完成故事，允许复习；不添加任务追踪、顺序路线或自动主线进度。其余NPC原故事、150句型/900模板保留，本轮不把整个库宣称为已经完成零基础重编。

通过依据是当次ASR转录基本语义正确：接受短语、简短回答和合理同义表达，指定句型及不影响理解的小语法错误不再强制卡关。不做音频发音评测。文字求助不计通关。基础默认完整提示，完成后可选同故事独立表达挑战；挑战默认隐藏答案，主动查看提示仍可完成，记录为辅助表达。

固定奖励区显示三轮目标、实际可领金额、共享限额原因及用途。基础默认10奇豆，营养餐价格读取现有配置；可选入门挑战30仙豆，拥有可用装备时显示强化费用及入口。奖励组、每日100奇豆/200仙豆总限额和跨NPC/语言共享次数不变。完成记录增加firstAt及independent，不追补旧奖励，不把单次完成叫作掌握。奖励与记录原子保存，录音与完整转录不持久化。

验收页：tests/fixtures/learning-encounter.html，仅使用内存角色；phone=1为390px嵌入视口，live=1连接真实语音，默认模拟不代表真实麦克风通过。本轮浏览器控制服务持续出现nodeRepl.fetch错误/Page.navigate超时，未完成截图、手机视觉与真人麦克风验收；也未组织目标年龄体验者测试。这些仍是扩岛前门槛。


## 魔法营地教学指针扩展至整套任务

- 用户更新需求：新手岛（魔法营地）整套任务都要有指引，单条任务全部完成（交付）后才没有指引；不再只限「未接取」状态。
- `appendTrackerQuest` 教学判定由 `!state.accepted && !ready` 放宽为 `!state.claimed`：营地任务未接取、进行中、可交付均显示手势与脉冲；完成交付即消失，追踪面板切到下一条营地任务时指引自动跟随新任务。
- 验证：npm test 689/689 通过；无头 Chrome CDP 端到端：未接取（去找青龙，接取新的任务）指引显示 → 点击追踪接取并交付 63000（获得经验+9、仙豆×100）→ 面板切换到 63001「与居民探讨魔法」后指引重新显示。未提交、未推送。

### 本轮验证结果

- 全量 `npm test`：690/690通过；最后的奖励用途、次数展示、开场朗读等待和目标文本保护修改后，20项相关测试再次通过。
- 最终 `npm run build`：地图及遭遇快照检查、Vite构建通过。隔离验收页模块语法检查通过。
- 新增测试覆盖语言草稿不修改存档、已有偏好保留、主线阶段筛选、附近距离/真实实例、课程锁定、退出/换角色/换语言取消、奖励限额及用途、可选挑战、提示与独立表达记录、目标语言文本不被界面翻译覆盖。
- 浏览器控制服务三次尝试失败（nodeRepl.fetch/Page.navigate超时）；没有截图或真人语音通过证据。桌面/390px视觉、真实中英麦克风、学生体验仍待验收。
- 自动朗读开启时，开场至少展示2.5秒，并等待该句播放结束再进入；点击台词仍可提前进入。主角点点点按站立/坐骑头部位置绘制。
- 没有新增学习任务追踪，没有扩岛，没有提交、推送或发布。

## 双语学习入口改为双人聊天图标

- 左上角文字入口改为两个角色聊天的内联SVG，复用坐骑按钮的36px圆形样式和布局间隔。
- 关闭时使用与坐骑完全相同的灰色圆圈及斜杠SVG；抽取共享生成函数，开启时移除标记。保留状态悬浮说明与无障碍标签。
- 新角色仍默认关闭；保留已有角色的选择。点击图标仍打开介绍及语言设置窗口，可随时开启或关闭。
- 验证：视图模块语法检查及8项设置/语言回归通过；本轮未做浏览器截图验收。

## 双语入口改用聊天气泡 emoji

按用户反馈，将双人聊天SVG替换为聊天气泡emoji，居中显示于原36px圆形按钮内；继续复用坐骑的灰色禁用圈和斜杠，开启/关闭与设置入口行为不变。视图语法及差异空白检查通过。

## iOS和macOS品牌资源

- 应用户要求替换iOS通用1024不透明AppIcon及原三张Splash占位图；补齐横/竖尺寸类别图片，默认方形适配iPad，LaunchScreen等比完整展示并用深紫底补边。
- macOS打包已引用精灵ICNS，本轮增加Dock图标显式设置，避免开发启动仍显示Electron图标。
- 加入prepare_apple_branding.py与10图哈希清单；catalog唯一槽位/路径、图像解码、RGB无alpha、storyboard/plist和8个ICNS表示核验通过，Electron语法通过。预览见.asset-cache/apple-branding-preview.png。
- 未执行Xcode编译、Mac签名、iOS真机或商店发布；未提交/推送Git。
