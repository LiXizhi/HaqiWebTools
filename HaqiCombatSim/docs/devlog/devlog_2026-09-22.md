# 2026-09-22 开发日志

## 海上捕鱼

- 点击海岸外海面打开休闲渔场。鱼影游动，金色光圈是键盘撒网落点；普通网需罩住鱼影，必中网和捕鱼器点中海面即结算。
- 奖励、扣网和精力变化来自 extendedcost 1569、1605、1606、1645、1646、1852、1853、1854。分支等概率，分支内按 p/1000。50401 表示空网。排行标记只记在角色上。
- 精力低于10不能撒网。中、大药剂按 21130/21131 回复并封顶100。小药剂因前提 -15 未开放。不出售海产，因为物品表收购价为0。
- 8项规则测试通过。内存页面验证普通网5→4并捕到闪闪皇冠鱼，必中网1→0，药剂恢复到100/100；1280与390宽度无横向溢出，关闭按钮可关掉窗口。未写真实存档，未构建或发布。

## NPC 固定底栏

- 分页与钱包移出滚动正文，合并到弹窗固定底栏；训练点不再单独占行。弹窗保持稳定高度，正文独立滚动，切页回到正文顶部。
- 内存角色在1280/390/320px宽度、631px高度下验证底栏不随滚动移动，训练点和分页同排且不重叠，无页面横向溢出。DOM点击验证第2页到第3页及scrollTop归零。
- 集成浏览器鼠标点击等待稳定性超时，截图工具未返回可检查画面；未完成真实鼠标及截图视觉验收。不读写玩家存档，未构建、未发布。

## 导师课程提示完整显示

- 移除课程状态与附加说明的单行省略；桌面状态列按内容占宽并允许换行，520px以下将状态放在技能名称下方，学习按钮保持右侧。
- 内存角色验证1280/390/320px布局，无提示裁切或横向溢出；截图对应的前置技能和无可用课程文案在1280/390px均通过DOM尺寸检查。截图工具未返回可辨识画面，不宣称完成截图视觉验收。分页自动点击超时后改用测试模型切页核验。
- 仅改导师列表样式，不改学习条件、不读写玩家存档，未构建或发布。

## 共享物品详情

- NPC 商店移除搜索框，商品图标点击使用 ItemDetails，保留分类、分页和导师卡牌预览。
- 商城、背包复用 ItemDetails；任务奖励和会员奖励沿用已有共享入口。宠物成长详情保持专用组件。
- ItemDetails.render 支持只渲染和 owned/instanceGuid；商城预览基础属性，背包读取指定实例强化属性，保留实例选择、套装、换装对比、穿戴、强化、镶嵌和道具使用操作。
- 24项装备/商城回归通过；内存角色浏览器检查 NPC 点击、Esc/Enter与焦点恢复，背包对比及商城详情，390/1280px无横向溢出。自动点击未命中时改用 DOM click 核验处理器；未修改真实存档，未构建或发布。

## NPC 商品图标

- 普通商品复用背包 art 绘制器，不再直接用名称首字；卡牌预览不变。
- prepare_shop_icons.py 增加 --npc 增量模式，保留旧图集，补齐 NPC 专属物品原图并转换 WebP；全部126张图集经 CDN 哈希及 CORS 验证，每张不超过200KB。
- 浏览器内存角色核验白色魔力晶石及四件圣诞派对装备；1280px和390px截图、有效画布像素检查通过，手机无横向溢出。未修改真实玩家存档，未构建或发布游戏页面。

## NPC 通用材料兑换

- 修复无导出按钮的商店兜底入口被统一标记为未接入；显式 canshow 条件仍拒绝，未执行原版脚本。
- 按 extendedcost.db.mem 已导出的确定性单奖励兑换支持永久装备、普通材料与既有符文；材料按实际种类和数量扣除，沿用动作保存及装备实例同步。
- 读取 globalstore 模板的持有上限、有效期及限购字段（paraworld.globalstore.lua L508-554），运行时数据投影保留 exchangeLimits；源码目录无需重导出，保留已有 NPC 隐藏配置。
- 红帽200雪球、绿帽20雪球、出奇蛋1魔法琉璃珠真实兑换回归覆盖不足不扣款、上限、装备实例、存档重读及发布数据一致性。失败保存不改变原角色。
- 未开放：限时物品、概率或多奖励、原服直购、魔豆及装备抵扣、服务器计数、活动限购、特殊前置条件。禁用商品也展示已知兑换成本。不代表原服交易写入或所有 NPC 特殊玩法已完成；未更改会员映射、任务奖励或真实玩家存档。
- 本轮不运行 npm 构建、不发布。

## 启动资源加载进度

- JSON读取器支持流式下载进度，兼容源码分文件和Vite合并包；保留数据包请求去重、独立副本及失败重试。
- 启动屏分别显示配置已读取MB、图片完成数量、世界准备和账号恢复。可靠Content-Length可用时显示下载比例；未知大小或gzip/br响应显示不定进度，避免压缩长度与解压字节混算。
- 资源与初始化完成前保留loading，失败沿用错误页与重试入口；未操作真实角色存档。
- 验证：6项读取/打包测试、完整npm run build及编辑器诊断通过。生产预览1280x800、390x844截图与布局检查无横向溢出；拦截配置包时保持loading，放行后真实读取从0.01MB增长至3.44MB，最终显示角色创建入口。

## NPC目录构建精简

- 在现有Vite数据包流程中为npc-catalog.json增加字段白名单，不修改原始目录。
- 删除原始XML、原始商品列、重复奖励字符串、旧引擎元数据及导出来源/报告，保留NPC显示与交易所需字段。
- 定向验证：runtime_data与adventure_npc共7项通过；覆盖体积预算、源数据不变、数据包读取，以及五学系在1/50级的全NPC商品课程与交易资格对照。# 2026-09-22

## 装备附卡纯提示

- 装备附卡不再绑定普通卡牌的点击、长按和右键操作，仅鼠标悬浮或键盘聚焦展示纯卡面，移开/失焦隐藏；无放入、移出或关闭按钮。
- 11项配卡测试通过；浏览器内存角色核验提示为hover/tooltip、按钮数0、移开后隐藏。后台页面动画帧等待超时后，改为直接读取DOM状态完成检查。

## 配卡数量角标

- 移除已学卡牌下方的数量行，改为卡片右上角20px圆形数字角标；数量为0时不创建角标，未学卡牌保留学习条件。
- 10项配卡测试通过，覆盖正数角标、零数量隐藏及数量行移除；浏览器1280px与390px宽度核验角标为圆形且位于卡片内，无横向溢出。

## 卡包图标与独立配卡

- 对照 `script/apps/Aries/Desktop/CombatCharacterFrame/CombatCardDeckSubPage.lua` 的 `Bag_RemoteSave` / `Bag_RemoteLoad`：原版按当前口袋物品读取和保存配卡。
- 顶部展示已拥有口袋的原图图标，同种物品仅一个入口；不符合等级或学系条件的口袋禁用并提供原因。移除编号方案、新增、改名、删除及口袋下拉框。
- 配卡通过 `bagItemId` 绑定口袋，各自按自身容量校验；切换小口袋不裁掉其他大口袋配卡。保存同时装备选中口袋。
- 旧编号方案优先保留当前配卡，其余顺序分配给其他可用口袋；超出可用口袋数量的旧重复方案不再作为独立实例。未装备时保留基础配卡入口。
- 底部移除常驻状态文字，只保留保存按钮；失败原因按需显示。牌库计数改为 `0/3` 格式。
- 验证：15 项卡包/配卡测试、51 项冒险/原服导入相关测试通过；完整 `npm run build`（含地图门禁）通过，编辑器诊断无错误。
- 浏览器使用内存角色，1280x800 与 390x844 无横向溢出，四种口袋图标像素非空；切换、推荐、保存后选中口袋及45张配卡保留。集成浏览器普通点击等待稳定帧超时，使用 `force:true` 点击通过；未写真实玩家存档、未发布。

## 六岛NPC与原版商店导入

- 增加独立可复现导出器与来源完整快照：292 NPC实例、1764商店行、8导师、1699兑换；保留原始XML、物品/兑换记录、哈希和缺失兑换/未落地商店NPC报告。
- 运行时合并目录、用原岛居民替换访客，新增居民沿道路确定性放置；原教学布局和任务保留，缺图使用通用人物标记。
- 交谈增加居民商品与课程面板，复用绘本窗口、卡面与关闭组件。按原生菜单参数关联白龙符文与通用课、五系导师及伍迪/火百灵课程；未实现条件明确禁用。
- 学习/普通货币符文购买通过persistReward原子持久化；原价10火玉购买白龙愈合符文，等级/学系/训练点/前置技能校验，不新增货币来源。
- 374项全量测试及4项定向回归通过；npm run build含地图门禁通过。隔离浏览器鼠标学习、购买数量0→1、搜索保留、Enter关闭、390px宽度无外部横向溢出通过。没有写真实角色、提交、推送或发布。
- 边界：全量数据收录不代表全部原服交易玩法迁移；装备/坐骑/直购/付费/限购/特殊条件/洗点/回售暂未开放；NPC立绘和三维位置未完整复刻，任务剧情未改。

## 居民商店与课程布局

- 对照 kids `CombatSkillLearn_panel.kids.html` 与 `NPCShopPage.html`：课程改为单列矮行（小图标、名称、提示、系别、等级或费用、学习/已学会），商品改为两列约 80px 的图标、费用与购买格。整张卡面改为悬停或聚焦图标时预览。
- 窗口标题改为居民姓名，身份放在眉题；底栏显示训练点或本店可用货币。同时提供商品和课程时用页签分开。
- 隔离页核验：伍迪课程可学习并变为已学会，训练点 11→10；搜索只留匹配项；白龙热卖购买后已拥有 0→1，商品/课程页签与分类保持。1280 与 390 宽度无横向溢出。卡面在无图时用文字缩写，悬浮仍可看完整说明。未写真实存档。

## 场景天气锚定世界坐标

- 灰烬、降雪、沙尘、余烬和薄雾原先按屏幕坐标循环，镜头跟随角色时粒子粘在画面上。绘制时减去镜头位移，粒子留在世界里，走出屏幕才从另一侧补入。
- 数量上限仍为64，默认萤光继续不绘制，减少动态效果时仍停用。未改地域天气配置。
- 验证：天气定向测试通过。火鸟岛西南登陆地暂停天气后，角色东移320世界单位，地面对齐的像素平均误差8.7，屏幕原位误差23.4；灰烬随地面滑动，不再粘在角色周围。

## 全岛居民原版小图

- 为 NPC 目录增加独立 npc-art 清单与逐实例绑定，原版 PNG/PeNote 立绘优先，无缩略图时通过原引擎隔离 mini-scene 渲染模型和 CCS 装备；保留源清单完整条目、哈希、外观参数。
- 每种外观为单独透明 WebP，严格小于 16,000 字节；原版不可见触发器保留数据但不绘制人物。冠军雕像使用原底座，未伪造原服排行榜玩家。
- 按可见区域懒加载，世界连续绘制不使用异步回调补画旧坐标，避免移动镜头后图片留影；发布配置保留图片绑定，移除仅供来源追踪的字段。

- 完成235种外观、233个去重WebP，最大15,918字节；覆盖292实例（278可见，14为原版隐藏触发器/空几何）。233个CDN文件均核对SHA-256和CORS，保留项目本地副本；用户选择不复制到digitalhuman-resource。
- 统一启动阶段图片模块的JSON读取器，避免同一发布数据包重复下载；380项全量测试通过，构建通过。浏览器默认CDN及明确本地模式都显示莫纳、伍迪、火百灵原版形象，营地居民亦已核验；未修改真实玩家存档，未提交、推送或发布站点。

## NPC图片分辨率升级（用户更新）

- 用户将预算改为每张48KB以内、截图至少256×256。原流程选用了大量64px原版缩略图，放大到对话窗口不清晰；新流程拒绝低分辨率源图，使用原模型512×512离屏截图或足够清晰的原版立绘，输出独立256×256透明WebP，严格小于48,000字节。
- 保留模型、CCS与截图/原图尺寸来源；运行时及发布数据同时校验尺寸和字节预算，新增低清图片拒绝回归与真实对话预览fixture。

### 2026-09-22 NPC高清图片验证结果

- 233张256×256透明WebP：225种外观由原模型512×512离屏截图，8种取自原版高清立绘；拒绝原64px缩略图，最大47,422字节，总计6,896,126字节。逐个CDN下载核对SHA-256、CORS，未同步独立Git镜像。
- 真实对话组件中安吉奶奶256×256、21,986字节，帽子、面部、衣纹和拐杖清晰；显式本地模式验证通过。默认CDN地址使用内容哈希文件名，避免命中上一轮低清资源。
- 全量381项测试通过，构建与地图门禁通过，dist仍仅HTML/JS/CSS/JSON；增加源数据和发布配置的最低分辨率拒绝测试。没有读写真玩家存档，未提交、推送或发布站点。
- 边界：目录292实例中273可见。12个原版隐藏触发器及7个离屏空图实例不绘制；空图来源为泉水位置、headarrow隐藏标记（4实例）、过山车及摩天轮，源数据和失败记录保留在npc-art.json，未以低清放大图冒充高清。此前可见的过山车模型本轮无法离屏渲染；这是场景设施，不是人物头像。

## 技能商店去掉搜索框

- 导师课程面板不再显示「搜索技能」输入框，列表按原顺序分页；商品商店仍保留「搜索商品」。
- 隔离页核验艾米娜课程无搜索框，商品页搜索仍在。

## 全岛任务导出与剧情首领美术准备

- 新增 `npm run export:quests`，常规冒险导出亦刷新：684个kids任务、全部辅助表、607个怪物完整模板/611个mob与540个法阵实例。原对白、条件、奖励、生产者掉落、cardsets/sequences/genes、脚本字符串和来源哈希保留，不执行原脚本。
- 按原Lua实际使用的Excel目标表及世界NPC解析，保留2处NPC30529引用缺失、3个缺失怪物模板和growrec.xml原始格式错误。废除257条仍保留；670条新增目录任务未自动接入线性教学执行器。
- 359种宠物对应530个怪物外观候选，23个主线首领保留原模型，58个待选图。映射尚未应用，不改变原怪物数值。原图MD5/字节数验证后，19张64×64原始缩略图转为暂存WebP；已检查对照图。它们不是正式高清立绘，未上传CDN。
- 本机8099原生渲染服务不可用。23个Boss渲染任务记录模型和原资源条目；准备工具支持导入512px以上透明渲染，输出256px且小于48,000字节的暂存WebP。实际高清渲染及该导入分支的真实模型验收尚未完成。
- 三个目录重导出逐字节相同；7项定向回归覆盖范围、原条件/对白、当前引用、完整怪物模板、美术候选、哈希与发布隔离。当前共享工作区全量401项测试通过；安装锁定开发依赖后完整build通过，dist确认不含未接入源目录和静态媒体。未修改本任务外的并行捕鱼/副本工作，未提交或推送。
- 完整源目录不进入启动包，避免无用的约10MB下载。详情见 `docs/quest-catalog.md`。本次为完整静态导出与资源准备，不宣称完整剧情可玩或高清Boss图已完成。


## 副本小世界与自动云存档

- 新增可复现的副本导出器及制作指南；原版92个实例、315组竞技场、372份模板，61个地图可探索，35个全部怪物组可挑战，244组可开始重演。源XML/坐标/卡位/哈希完整归档，发布投影剔除原XML且与开发检查点保持一致。
- 副本独立位置、出生/出口、行走寻路、原阵容与空卡位、多怪经验奇豆汇总、伙伴经验、清怪/继续/重开、回原岛与云端存档字段校验均接入。原副本机关/门锁、过场、门票、困难倍率、掉落及专属美术尚未复刻，UI和目录明确说明。
- HUD右上副本替代云存档，副本内显示目标数量与寻路；设置仍可管理云端。重要操作触发自动同步，最短尝试间隔30秒、10分钟兜底、失败重试、上传中修改保留，账号冲突和访客不自动上传。
- 桌面/390px隔离浏览器核验搜索、进入三组六怪宝库、寻路开双怪战斗、跳过一轮两怪行动（主角415→192）、撤退、退出返回营地、键盘共享关闭和新入口图标。未用真实账号执行远端读写；无玩家存档写入、提交或发布。
- 已完成全量407项与完整构建，后续增加开发/发布检查点一致性测试后定向7项通过；最终全量结果见QA记录。

副本最终核验：全量408项、7项定向及完整构建通过；导出重复哈希一致。快制作配置保存在config/dungeons/custom.json，默认不加入示例世界，文档提供可直接复制的两组怪物模板。

## 副本完整 JSON 按需预加载

根据用户明确要求，副本内容保持单个完整JSON。启动仅加载44,763字节目录，首次进入任意副本加载完整包并一次注册全部世界，后续共用缓存；并发请求合并，失败清空待处理请求以允许重试，失败不修改存档或半安装地图。当前角色位于副本时，本地角色、云端角色/检查点与备份恢复均先准备数据再校验重演；历史通关记录只需目录。

新增独立vite.dungeons.config.mjs与npm run build:dungeons，产物为dist/data/adventure/dungeons.json（1,619,920字节，Vite报告gzip约101.91KB）。主adventure.json排除该完整包，仍保留轻量目录；完整build自动串联。原配置data/adventure/dungeons.json与原创config/dungeons/custom.json不变，无逐副本数据文件。

验证：全量412项通过，完整构建通过，产物断言独立目录仅一个dungeons.json且包含92世界/372怪物；新增首次整包请求合并/跨副本复用、失败原子性/重试、历史记录无需加载/副本战斗恢复、本地角色加载前准备等4项测试。隔离浏览器确认目录可用、首次进入三组六怪火鬃怪宝库成功；未操作真实存档或云端账号，未提交发布。

## 副本数据并入普通 Vite 构建

按用户要求移除独立副本 Vite 配置及 build:dungeons / prepare:dungeons 命令。现有 vite.config.mjs 在同一次构建中生成独立的完整 data/adventure/dungeons.json，启动包仍只含轻量目录；Vite 开发启动、构建与副本导出自动刷新目录。整包按需加载逻辑不变。

验证：npm run build 通过；11项副本加载、核心与运行时数据测试通过；产物断言单个JSON含92世界/372怪物，主包仅含索引。未提交或发布。


## 剧情Boss原模型WebP完成

- 按用户指示找到 `/Applications/Paracraft.app`，通过仓库macOS启动脚本打开项目530，8099健康检查确认worldEntered；原应用配置自动备份。
- 新增 `render_boss_models.py`，复用原生Canvas3D独立mini-scene方案，渲染23个原模型至512px透明PNG。单张试渲染通过后完成全批；未放置世界实体或保存世界，专属离屏控件用完停用/隐藏。
- `prepare_boss_art.py --render-dir ... --archive` 完整归档23张透明256×256 WebP；总809,766字节、最大44,354字节，均低于48,000。新增boss-art.json记录模板、原模型清单、渲染输入及输出哈希；计划重导出核对哈希后标记prepared-local。
- 检查全批原模型预览、23张解码/alpha/边距/大小/哈希；11项目录及资源打包定向测试通过。保留本地美术副本，源清单及美术不进入发布包。未上传CDN、接入战斗、提交或推送。

## 副本入口 AI 图标

使用内置 image_gen 生成金色石门与翠绿传送门图标，透明背景、无文字；完整提示词及原图哈希记录在 data/adventure/ui-art.json 的 additions.dungeon。新增 scripts/prepare_dungeon_icon.py 从保留的原图集追加一格，先尝试无损，再选84质量WebP；图集由512×512扩展为512×640，17格共97,696字节，比原版增加3,124字节。单图归档为112像素级透明WebP（20,502字节），运行时仅请求合并图集。

共享图集加载器按清单行列数生成背景坐标，保留原16格和SVG失败回退。永久CDN： https://cdn.keepwork.com/haqi/haqicombatsim/ui/storybook-edbc67b355aa.webp 。已核验远端哈希相同、HTTP 200、image/webp与CORS；现有图集测试及npm run build通过。隔离副本页面查看实际HUD与390px布局，未操作玩家存档；未提交或发布游戏。

## 底部任务清单接入全岛目录

- 沿用既有QuestListPage/QuestDetailFramePage式清单与详情布局，显示全部684条，加入六岛/公共/试炼筛选、七类状态、名称或编号搜索、每页20项和可点击前置任务。目录任务展示原目标、NPC、条件和奖励组，已知奖励物品共用ItemDetails。
- 只读纯模块投影和筛选；原14个教学任务依旧由既有核心判定进度及追踪。其余670个仅展示，区分尚未开放与原版已废除，不改存档或发放奖励。
- 精简任务展示JSON为471,814字节，打开窗口时按需读取；重试可用、并发请求合并、关闭后不再回填DOM。构建与导出刷新该独立文件，完整源归档和任务展示均不塞入冒险启动包。
- 桌面1280与手机390px浏览器验证684总数、20行分页、岛屿/状态/编号搜索、主线Boss目标、前置跳转、257废除项、空结果、网络失败重试、原教学追踪和共享关闭按钮；图片正常及无图兜底均可用，角色JSON前后相同。截图已检查，无外部横向溢出。
- 14项定向回归、当前共享工作区416项全量测试、完整构建通过；发布产物确认684条独立任务文件存在，完整源目录不进启动包。全量首轮受并行界面图集地址暂未填写影响有一项失败，图集工作完成后重跑已全部通过。
- 未提交、推送或发布；任务可玩范围仍为原有教学章节。

## 2026-09-22：原版Boss CDN与战斗外观

23张Paracraft原模型透明WebP已上传至Keepwork永久CDN，逐张核验SHA-256、文件字节数与CORS；256×256，合计809,766字节，最大44,354字节。`boss-art.json`保留原始模型来源、本地文件与实际CDN地址，`boss-art-plan.json`标记cdn-verified。

`package_monster_art.mjs`生成运行时`monster-art.json`，按原始模板路径与模型绑定607项（116项原Boss模型外观、491项宠物外观），共复用25种现有宠物。27项额外外观改编记录在adaptations中，其余沿用已匹配模型；仅改变显示，不改变战斗属性、捕捉身份或存档。地图遭遇与战斗单位统一走`drawMonster`，23种Boss全部支持现有攻击/受击/倒地位移效果。原版脚本未支持的遭遇仍按原有规则禁用，不因美术接入而开放。

验证：419项测试通过，npm run build通过；全部611条原怪物记录、372项副本怪物与7项主线怪物都有外观绑定。独立只读浏览器预览中，23张Boss与25种宠物均在CDN和显式local模式解码成功，Canvas可读；全部Boss分组通过实际renderBattle绘制，桌面与390px检查无脚本错误。预览入口为`tests/fixtures/monster-art.html`，不读取或写入玩家存档；Boss战斗预览使用原属性但不执行AI，不代表新增剧情已可玩。构建只含代码和JSON，不重复携带静态美术。未提交、推送或发布网站。


### 世界地图入口自动补法斯特船长

修复加载原版居民目录后跳过来访船长的问题。createWorld 对每个岛屿世界地图入口检查160像素内是否已有法斯特船长：保留已有居民；缺少时优先复用 visitingNpcs 的既定位置，没有配置则站在传送法阵中心。复用已安装的船长图片与原有打开世界地图交互，不修改内容目录或存档；副本出口不补船长。

验证：npm test 全部420项通过，npm run build通过（含地图门禁）。新增回归覆盖六岛入口、已有位置保留、美术引用、交互/空间索引、无配置自动补齐与内容不变。本轮未做浏览器视觉验收；未提交、推送或发布。


### 船长位置调整（2026-09-22）

按用户补充，程序补放的船长直接站在传送点中心，不再使用来访配置中距传送点115像素的位置；无居民目录时的来访船长同样处理。已有手动摆放保留。NPC与世界地图相关13项测试通过。

## 任务窗口紧凑筛选

按用户要求移除搜索框，将岛屿与任务状态下拉框移入标题栏，正文减少一行。默认显示角色当前岛屿；副本等非岛屿区域优先使用存档 dungeonReturn 中的上个岛屿，无有效记录则回退哈奇岛。不新增存档字段；前置任务链接自动切换至目标任务地区并定位分页。

验证：任务目录3项回归通过，构建通过。1280px与390px浏览器覆盖当前岛屿、上个岛屿、哈奇岛回退、标题栏无重叠、分页、状态筛选、失败重试、追踪、关闭及存档不变；手机截图确认下拉框与统一关闭按钮保持同排。隔离预览页支持zone/previous参数，不写玩家存档。

## 不再显示废除任务

任务展示JSON的生成器排除257条原版废除任务，运行时quest-journal.json仅保留427条；前置任务展示也移除指向已废除任务的条目。去掉废除状态筛选，并在视图及筛选函数中防御性排除旧缓存记录。原始quest-catalog.json作为不发布的来源归档保留，不进入游戏任务列表。6项任务/打包回归与构建通过；手机浏览器逐页检查427条任务均无废除记录，存档不变。

## 副本选择界面改版

参考原版 CombatRoom/LobbyClientServicePage.html 的勇士大厅插图、等级与进入操作，副本窗口改为绘本卡片：准确Boss肖像（复用现有monster-art肖像，缺图使用传送门）、等级、首领名、组数与探索进度。删除玩家界面的导出数量、怪物模板列表、复刻说明，保留文档中的技术边界。列表仅显示可进入的非测试世界，支持名称搜索、适合我（建议等级不高于角色等级+5）、进行中筛选及每页6张。重新挑战在加载期间禁用。

轻量目录增加按生命值最高怪物选取的首领名称、模型路径与等级，首次进入前不请求完整副本JSON。相同首领摘要也由核心安装时计算，加载前后展示一致。现有11项副本/运行时回归及完整构建通过。

隔离浏览器验证：桌面三列卡片、390px横向单列卡片；进行中空状态、名称搜索、准确章鱼Boss肖像与进入梦幻火鸟岛成功。缺少准确肖像时使用副本入口图标，不用宠物替代图冒充Boss。无真实存档写入。

## 副本道路推进与自动遇敌（2026-09-22）

副本使用不交叉连续道路，原坐标保留供追溯；最高生命值怪物所在的最后一组移至末端作为Boss。道路外禁止行走，各种输入共用碰撞，远处寻路截停在第一处未清关卡；核心拒绝跨组挑战。接近自动战斗，胜利后怪物消失；前置组与Boss清完后末端出现出口，走近自动返回原岛。撤退/战败回起点，菜单保留暂离，未支持战斗仍挡路。旧版自由行走位置不在道路或越过关卡时恢复起点。

验证：全量421项、最终9项副本定向回归通过，完整构建通过。覆盖大步移动不能穿透守卫、跨组拒绝、清怪消失、Boss后出口出现及重开隐藏。隔离浏览器点击火鬃怪宝库第一组后方道路，角色自动接近并开启双怪战斗；未写真实玩家存档。

## 副本选择窗口紧凑化

移除搜索框、守卫组数、怪物数量和进度条；已挑战副本只保留“探索中/已通关”。标题、分类与分页放在滚动区域之外，分页固定窗口底部，切页/换分类回到列表顶部。缩小肖像区、卡片内边距与间距，桌面三列，390px手机使用横向单列卡片。

轻量目录新增 battleRewards，仅从未阻断战斗的原怪物经验/奇豆配置提取奖励类型；整包加载后按同样条件展示。不显示未实现的装备、宝箱奖励，不改变结算。

验证：9项副本/加载定向测试及 npm run build 通过。隔离浏览器桌面与390px预览确认无搜索及怪物统计、奖励类型正确；滚动时分页固定，下一页切换成功，进行中空态和禁用分页正确，Enter关闭成功。手机页面宽度390px无横向溢出。未操作真实存档、提交或发布。

### 入口与末端双传送点（2026-09-22）

副本入口新增常驻离开传送点，以短路段连接起点，相距150避免出生即退出。走近入口可随时返回原岛，保留清怪进度；重新进入时若上次停在传送点范围，恢复到起点。末端出口由最后Boss组的击败记录控制出现。鼠标拾取、附近交互、自动触发与绘制同时接入两个传送点。10项副本回归与完整构建通过；包含入口可达、无出生误触、离开重进保留进度及Boss条件测试。
