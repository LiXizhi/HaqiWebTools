> 2026-09-25 存储更新：角色云端已拆成小型状态、完整收藏、战斗背包、历史记录；临时血量/饥饿等存IndexedDB。旧存档自动兼容，详见[用户存储结构](user-storage.md)。

# Haqi.html · 初心之旅

## 哈奇岛居民显示开关（2026-09-22）

`npc-catalog.json` 的居民实例支持布尔属性 `hidden`：`true` 隐藏，`false` 显式显示。未配置时，目录安装阶段将哈奇岛中不属于现有章节、且没有商品或导师服务入口的旧乐园居民默认设为隐藏。保留原始资料和美术，不删除NPC；其他岛屿不自动隐藏。已有服务入口即使部分交易尚未开放仍保留。

场景创建统一过滤隐藏居民，因此人物、名称、地图标记和就近交互均不再出现。未来功能接入后，可在对应实例上配置 `hidden: false` 恢复；原版禁用状态和美术不可见状态仍独立生效。发布数据保留此属性，重新导出未带显式开关时仍应用默认规则，不影响玩家存档。

## 原服角色导入（2026-09-21）

新建角色第一步（起名字）及角色管理入口已接入原服只读读取，限定背包0/1/24/25。用户先通过正式背包与卡包面板检查暂存数据，查看两者后才能确认创建独立角色；取消不改存档，最终确认再次核验身份。未支持装备、卡牌和成长差异在折叠说明中显示；非完整原服进度迁移。详见 [player-import.md](player-import.md)。

`Haqi.html` 是独立的 kids 单人冒险入口，使用 Canvas 2D 场景与 HTML 菜单。原 `HaqiCombatSim.html` 的 PvP、批量模拟和调参入口保持独立。界面语言与 `data/adventure/locale/` 词典见 [locale.md](locale.md)。

## 运行与重新导出

```bash
cd web/HaqiCombatSim
python3 -m http.server 8791 --bind 127.0.0.1
# http://127.0.0.1:8791/Haqi.html
```

没有运行时构建，也不需要 npm install、ParaEngine、父目录、兄弟模拟器或账号。本机 loopback 默认使用 Git 中的 WebP，不需要 CDN 网络；线上域名默认使用永久 Keepwork CDN。`data/adventure/` 与 `assets/adventure/` 是完整的运行时内容；音乐是可选资源。使用 HTTP，不能通过 file:// 运行 ES modules / fetch。

开发者刷新内容时：

```bash
npm run export:adventure       # Python 3 标准库；读取默认 ../../ 或 --root 指定的 paraworld
npm run assets:adventure       # Node >=20 + Python/Pillow；准备、解码、无损转换 WebP
npm run check:adventure        # 纯引用检查 + 本地文件哈希/尺寸/格式检查
npm test                      # 69例，包含原34例、五系章节、资源与云端存档测试
```

卡牌数值沿用本项目已经入库的 `data/kids/cards.json`、`charms.json`。要同步原始 Card XML 的改动，先运行已有的 `npm run export`，再导出本章。导出器用 ElementTree 解析 XML，用限制为数据字面量的 `scripts/lib/lua_data.py` 解析数据库表；不会执行 Lua、MCML、NPL 或嵌入脚本。

资源清单保留原始路径大小写、`.p` / `.z` 后缀、MD5 和长度。查询采用小写及统一斜线，纹理的 `;x y width height` 裁剪矩形单独存储。CDN URL 以完整清单行构成，包含 `,md5,size`。`.p` 保存原图；`.z` 先验证下载容器再解压 ZIP/zlib。`media.json` 另外记录无损 WebP 的本地路径、实际 CDN URL、尺寸、SHA-256、源像素文件哈希及完整源清单行；不会混淆源 MD5 与转换后哈希。裁剪矩形继续来自章节引用，图集尺寸不变。Pillow 完整解码并比较 RGBA 像素；Node 检查 WebP 容器、尺寸、SHA-256；浏览器实际解码并执行 alpha 边界裁切。

## 操作

- WASD / 方向键移动；点击或轻触地面寻路，PC 鼠标点击在松开左键后才寻路或触发目标交互；按住持续跟随光标（拖动立即生效），松开停止，不再触发点击寻路。点击居民或敌人走近互动。点击海岸外的海面打开休闲渔场。
- E 交谈；B / I 背包，C 卡包，J 任务，P 宠物，Esc 关闭面板。
- 手机轻点地面会自动寻路到该位置；轻点居民、怪物或出口，会走近并交谈、挑战或切换场景。按下后拖动超过 6 像素才显示浮动摇杆，控制方向与速度（支持斜向），松手停止；拖动和双指缩放不会误触交互。也可靠近后用交谈 / 挑战按钮互动；PC 鼠标仍支持点击寻路和按住跟随。卡牌选牌后点击自己或敌方按钮施法。
- 任务的「追踪目标」自动寻路；装备、强化、宠物和配卡任务直接打开对应面板。
- 战斗中可弃牌、跳过、撤退及导出存档；胜利后收取奖励，失败后回到安全地点。
- 设置可开关背景音乐、导出/导入 JSON、回到开始画面。新游戏会替换当前浏览器存档。

## 章节与来源

| 任务 | 原角色 / 目标 | 浏览器中的完成方式 |
|---|---|---|
| 63000 | 青龙，开启魔法之旅 | 阅读移动与交谈教学，回报 |
| 63001 | 茜茜、莫尼 | 两段原版讨论对白 |
| 63002–63005 | 烈火、寒冰、风暴、生命侦察兵 | 原怪物属性、技能池、战斗奖励；领取本系帽/衣/鞋和仙豆 |
| 63006 | 莫纳 → 青龙 | 领取晶石法杖1912 |
| 63007 | 青龙，强化79016 | 背包装备并强化法杖，原成本70 / 140 / 280仙豆 |
| 63008 | 青龙 → 杰西卡 | 打开出奇蛋17307，再了解宠物 |
| 63009 | 杰西卡 → 青龙，喂养79019 | 口粮17172增加300经验 |
| 63010 | 伏尔坎、艾米娜、高登、爱丽丝、磊奥 | 与五位学系导师交谈 |
| 63011 | 亚尔弗列得 | 阅读原版考核建议，领取翡翠口袋等原奖励 |
| 63012 | 青龙，配卡79037 | 装备24003并保存卡包 |
| 63013 | 死亡侦察兵 | 毕业战，回青龙处领取奖励 |
| 镇区尾声 | 罗德镇长、苏菲、白龙导师 | 居民交谈、水咕噜和死亡泡泡重复遭遇 |

`chapter.json` 保存源任务ID、NPC ID、原坐标/模型、场地ID及怪物槽位、道具GSID、学校限制、经验/货币/选项奖励，以及源文件 SHA256。共14任务、15居民、7种怪物、19个原场地参考。`combat.json` 是45张相关卡牌及 charm/ward 模板的独立快照。怪物的 `round="1-"` 是首回合前的额外动作；死亡侦察兵18回合脚本完整保留。

## 明确的改编

- 任务按63000–63013串联，取消旧的等级上限锁。两张地图为按原地图参考绘制的紧凑步行场景，有建筑/树木/海岸碰撞、深度排序、镜头、小地图和路径提示；原缩略图只在地图面板作参考。
- 毕业后前往哈奇小镇是本章尾声。原63014指向火鸟岛，不在本章。
- 等级1–10累计经验阈值：`0, 41, 114, 255, 495, 869, 1418, 2479, 3654, 4654`。源任务与遭遇的经验数值不变。基础法术按1、1、2、3、4、6、7级学习，每种三份；学习时间及初始卡组为本章安排。角色等级上限为50（原版 kids 生命公式按 `/49` 取到50级），网页版在曲线上多算一段，使上限那一级也有"下一级"目标；HUD 经验条按原版 `EXPArea.lua` 的本级经验／下一级所需经验显示，因此满级角色仍能看到本级进度与未填满轨道。
- 装备附带法术作为额外固定卡加入抽牌顺序；未装备口袋时容量10、同卡2份；装备口袋后使用道具自身容量与同卡上限，翡翠口袋容量20、同卡3份。角色属性使用已装备物品的原stats及强化值，不读取模拟器的自定义调参。
- 旧3D视角、瞬移和自动演示文本替换为浏览器操作说明。逐条 `dialogueAdaptations` 与 `originalText` / `originalLabel` 保留原文。宠物捕捉、选修学系等后续课程明确留待以后。
- 出奇蛋固定孵出10136「咕噜噜」，随行与喂养，不加入战斗。宠物经验使用原XML增量与 `CombatPetProvider:GetLevelInfo` 级别约定：300经验为3级，464经验满级显示5级；不再消耗满级宠物的口粮。
- VIP礼盒与变身奖励保留原ID与数量，未开放变身系统。2026-09-21起，已有消耗符文可在战斗的符文卡入口施法，成功后扣一张，失误不扣，不占普通卡包容量；不支持的法术不开放。2026-09-22起，RuneList的45项符文记录商店、任务和原版掉落；普通、高级、顶级抓宠符文可对野生宠物施放，成功率按原版权重和剩余生命计算，成功或失败都消耗一张。安卓婆婆出售前两张。顶级符文、未开放任务和怪物掉落只展示来源，当前不会发放。2026-09-23起，商城不再出售捕获晶球，野生战斗用符文卡里的抓宠符文；背包里尚未使用的晶球换成普通抓宠符文。已经用晶球出手的旧战斗仍按当时的记录重演。装备附带法术仍不消耗背包符文。
- 树木、房屋、四方向角色和怪物用新绘制的透明精灵；NPC立绘、原卡面、道具图标和音乐来自清单。部分居民共用可用的原版立绘，见资源说明。
- 战斗独立恢复满生命，无装备耐久、服务器计时、暴怒/节日活动、多玩家威胁排序、捕捉或战宠能力。1名玩家意味着 `threat_highest` 只有一个敌方目标。其他岛屿、家园、帮会和付费系统不在本章。

## 规则、存档与接口

纯模块可在 Node 中直接导入，禁止引用 DOM、存储或网络：

- `adventure_core.js`：`createAdventure`、`applyAction`、`questProgress`、`questReady`、`playerSpec`、`beginEncounter`、`recordDecision`、`settleEncounter`、`parseSave`。
- `adventure_world_core.js`：`createWorld`、`walkable`、`movePosition`、`findPath`、`followPath`、`nearestInteraction`。A* 检查每条边及真实起点连接，移动采用分段碰撞。
- `adventure_content_core.js`：`validateAdventureContent`。必需NPC、任务依赖、目标、奖励、卡牌、效果模板、AI指令、对话动作和资源引用不支持时立即失败。
- `combat_pve_core.js`：`createPveBattle({dataset,player,monsters,seed})`、`playPveRound(battle,decision)`、`restorePveBattle(dataset,content,checkpoint)`。

`AdventureContent` 是 `chapter.json` 中的版本化定义，`AdventureSave` 是 `createAdventure` 返回的JSON对象。专用键 `haqi.adventure.kids.v1` 与模拟器设置隔离。保存角色、经验、物品、装备、强化、拥有的牌、卡包、宠物、任务、位置、遭遇序号与奖励账本；移动时定期保存，完成操作及每次战斗决定立即保存。

战斗检查点只保存种子、起始角色规格和决定序列。加载时以相同种子重新执行决定，还原事件、魔力、生命和随机数状态。导入先验证所有数据并重演战斗，成功后才替换当前进度。奖励领取以任务claimed标志和遭遇ID账本去重；动画尚未结束时重载也不会重复发奖。失败、平局或撤退不删除任务和物品，回到安全地点。

玩法随机使用遭遇种子；地图装饰使用单独的固定种子，动画仅使用时间。云端文件的时间和 UUID 由 IO 层生成，不消耗玩法随机数。


## WebP 与永久 CDN

- `http://127.0.0.1:8791/Haqi.html`：默认永久Keepwork CDN；`localhost` / IPv6 loopback与所有线上域名同样处理（2026-09-18更新）。
- `Haqi.html?assets=cdn`：本机验收线上资源模式；其余域名（包括局域网 IP）默认 CDN。
- `Haqi.html?assets=local`：仅源码HTTP服务中明确使用 Git 资源，可用于无网开发。dist与CDN发布不包含美术，不使用此参数。
- 91项资源（90张 WebP、1段可选 Ogg）合计8,326,728字节；转换前10,823,856字节，减少23.1%。原PNG从当前工作树移除，历史仍在Git；原始完整条目可重新下载。
- 图片加载在设置 `src` 前指定 `crossOrigin='anonymous'`；所有资源的HTTP、CORS、尺寸、内容哈希已经核验。可选音乐失败不影响游戏。

准备环境只用于开发，游戏不加载 Pillow 或 npm 第三方库：

```bash
python3 -m venv .asset-cache/venv
.asset-cache/venv/bin/pip install Pillow
HAQI_ASSET_PYTHON=.asset-cache/venv/bin/python npm run assets:adventure
.asset-cache/venv/bin/python scripts/prepare_adventure_media.py --verify
npm run plan:adventure-cdn
# 根据 AGENTS.md 的 Maisi 七牛上传技能上传 .asset-cache/publish 中计划列出的文件。
# 文件名为 SHA-256；object key/完整URL在 data/adventure/cdn-publish-plan.json。
npm run verify:adventure-cdn
```

发布脚本本身不存放凭据：先生成计划，使用 Maisi 的上传器上传，最后对每个实际URL重新GET、验证CORS与完整字节，全部成功才写入 `media.json` 的CDN地址。线上资源仅允许 `https://cdn.keepwork.com/`，不使用临时URL或其他CDN。修改原图后重新准备；`--prune-png` 只删除已经验证转换成功且源哈希匹配的PNG。原生成图集丢失时从Git恢复已准备的WebP，或重新提供原始图集。

## Keepwork 云端旅途

开始画面、设置和战斗中的「云端旅途 / 云端存档」提供可选手动检查点。点击连接后才加载已验证的 `https://cdn.keepwork.com/sdk/keepworkSDK.core.iife.js`，使用SDK的中文登录窗；普通本地游玩不加载SDK。

每次保存会在 `sdk.personalPageStore.withWorkspace('HaqiAdventure')` 下新增 `checkpoints/<UTC时间>_<UUID>.json`。封装版本1包含 `app`、`id`、`updatedAt`、完整AdventureSave；不会上传SDK token、密码或密钥。无固定latest文件，不自动覆盖其他设备的记录，也没有后台自动云同步。

2026-09-25 起按用户要求启用服务器 pageCache：`savePageData(path,'content',text,false,true)` 暂存后等待 `syncToGit(path,true)`，再以 `getFileByFullPath(...,undefined,true)` 核验缓存返回内容。角色目录使用 `loadPage({useCache:true,useServerCache:true,...})`；仅明确的缓存 404 表示新账号，网络错误不视为空目录。保留账号切换、版本冲突、本地备份与写入核验。目录 `listDir('checkpoints',false,{remoteOnly:true})` 的 SDK 内部使用缓存目录 API，仍仅在用户刷新时调用。

恢复流程先读取实际远端记录，校验版本、角色、任务、装备及战斗重演，再显示本地/云端进度供玩家选择。点击「确认恢复」前不会修改本地存档；先备份到 `haqi.adventure.kids.v1.before-cloud`，再替换主存档。备份写入失败则中止恢复；菜单可导出上次备份。更新时间使用独立 `.updated` 键，不改变原存档兼容性。账号变化使旧预览失效。

网络超时或未确认写入时，本地进度不受影响；SDK请求可能随后完成，因此应先刷新检查再重试。云端记录的可见性遵循用户Keepwork项目设置，不宣称其为加密或私密存储。真正登录账号后的服务端往返尚需验收；当前已验证真实SDK加载/中文登录/取消，以及模拟SDK上的保存、失败、预览、恢复与备份。

## 技能特效

当前45张冒险技能卡均接入配置化粒子和特殊演出，召唤海狮、火凤、火魔等角色。新增透明WebP图集，线上采用Keepwork CDN。支持系统减少动态效果偏好，不改变战斗数值或存档。独立预览页为 `HaqiEffects.html`，开发配置见[技能特效](spell-effects.md)。

### 2026-09-17：单张WebP体积上限

当前91张WebP全部不超过200,000字节。超标的城镇地图、主角/场景、怪物、召唤图集改用质量85/75 WebP，必要时从原图重采样缩小；其余合格图片保留原编码。92项资源（含可选音频）共5,429,760字节。`prepare_adventure_media.py`自动执行预算，`--verify`拒绝超限。召唤frames保留1774×887设计坐标，通过实际图集尺寸缩放；主角和怪物既有裁剪使用比例坐标。

## 2026-09-18：角色装备与背包

点击左上角角色头像或按 R 打开「角色与装备」；B/I 和底部背包按钮打开「旅途背包」，两个标签可相互切换。电脑为角色与物品双栏，手机为单栏，属性区默认折叠；按钮支持触屏与键盘。装备沿用现有角色、道具和卡牌资源，不新增运行时美术或第三方依赖。

当前章节17件装备覆盖五系帽子、法袍、靴子、晶石法杖与翡翠口袋。支持部位筛选、名称搜索、穿戴、替换、卸下、条件提示、来源任务、属性预览、装备附加法术与原三档法杖强化。未实现的会员、符文和变身物品继续标记为旅途收藏。未扩展原章节外的装备库、锻造、宝石或套装规则。

换装前显示最大生命、本系攻击/防御/命中/暴击、魔力与容量变化；数据复用 playerSpec 和既有生命/魔力公式。卸下或换成小容量卡包时按原配卡顺序裁剪，提前提示移出数量，拥有的卡牌不会丢失。卸下不删除物品和强化记录，战斗中禁止装备操作。沿用 v1 存档以及本地、导入导出和云端校验路径，无需迁移。


## 2026-09-18：共享卡牌主体与动作图集

正式卡包、手牌、装备附卡及施法演出改用五系空白背景和生成主体，文字/数值仍动态绘制。儿童版225个基础技能覆盖701条定义；18张共享主体图集加6张高费九帧动作图集，每张≤100,000字节，背景<24,000字节。冒险只预加载章节需要的图集，工坊和全卡页面按需加载；线上默认永久Keepwork CDN，本地模式明确选取WebP。详见 [卡牌美术说明](card-study.md)。


## 2026-09-18：设置中的调试属性编辑器

设置 →「属性编辑器 · 调试」提供等级、累计经验、仙豆、奇豆、运行时物品数量、法杖强化等级和本系卡牌份数；原章节宠物存在时可改其累计经验。字段上限取当前内容定义，支持名称/编号搜索和分类筛选，PC双列、手机单列。

编辑只改草稿，预览完整变化后点击「保存并生效」。等级设定映射到该级起始经验，修改经验反算等级并按原规则学习卡牌；降级保留已学卡牌。失去物品或不满足穿戴条件时自动卸下，删除无所属装备的强化记录，按持有量和卡包容量裁剪配卡。生命和攻击等计算结果在只读区查看，仍通过等级与装备产生，不覆盖战斗公式。

所有字段必须是合法范围内的整数；进行中的战斗不可编辑。保存前自动写入独立 `.before-debug` 本地备份，按钮可恢复上次修改前的完整存档；备份写入失败不应用修改。该备份与云端恢复备份分开，不自动上传。未开放任务奖励账本、随机种子、战斗检查点等内部状态的任意编辑。

## 2026-09-18：全宠物与50级科技树

已接入三色初始抱抱龙、359项MagicHaqi宠物、四阶段成长、四卡位编队、同位附卡、独立宠物自动战斗、捕获、自动进食及持久HP。全局商店展示1008件可用原版装备、359项宠物和补给，提供1–50级重复试炼。旧咕噜噜任务保持独立，v1存档迁移至v2。先前“只到10级、宠物不参战、每战满血”的范围说明由本节取代。

家园仅接入正式入口和版本化转换接口，尚未双向同步。规则、数据刷新、排除装备和接口详见[宠物与商店](pets-and-shop.md)。帕鲁式继续创新、治疗/口粮 VIP 化见计划需求 [pets-vip-innovation.md](pets-vip-innovation.md)，尚未实现；「升级会员」按钮目前只提示暂未开放。

## 米酒葫芦签到

右上角“签到”打开五个米酒葫芦，参考原版 `Desktop/MiJiuHuLu.lua` / `.html`，按当天累计在线 1、15、30、60、90 分钟分别解锁。可领取的葫芦高亮，直接点击领取；未到时间显示剩余时间，已领取显示完成状态。每个葫芦每天限领一次，默认各 100 奇豆。

网页计时只累计游戏页可见时的游玩时间（含战斗和面板），离线不累计；每天北京时间零点重置。进度保存在 `checkin` v2 中。旧版单计时器存档保留已有奇豆，新葫芦进度重新开始。参数集中在 `BalanceParams.checkin.minutes / coins`。原版会员额外奖与抽奖尚未接入，奖励简化为奇豆。
## 2026-09-18：战斗显示优化

手机与窄屏手牌改为重叠排列，默认展开可用牌，点击露出的牌边可切换；宽桌面仍平铺。首轮发牌和次轮新增手牌依次滑入，打出的卡面飞向法阵。手牌、底部按钮和法阵按实际可用空间避让，低高度横屏采用左右分列；角色位置与法阵站位符文共用坐标。

## 多角色管理（2026-09-18）

没有角色时直接进入新建流程；只有一个角色时直接继续游戏。设置中的“切换 / 新建角色”打开列表，显示外观、名称、等级、系别和最近使用标记。访客与各Keepwork账号分别最多5个主角，新建不会覆盖原角色。旧单角色存档自动迁为访客角色并保留旧键。登录默认进入最近角色。

本地自动存档、云恢复备份和调试备份均按角色隔离。登录账号在游玩时每分钟尝试云同步，也可在角色列表手动保存；关闭页面前可手动保存以便另一设备读取最新进度。失败保留本地数据，冲突需先备份后加载。网页登录及云端角色目录已经接入；真实账号跨设备读写仍待验收。

新建角色分为起名字（含外观）、选择抱抱龙、选择系别三个步骤，返回修改保留草稿。仅抱抱龙页显示小字“导入魔法哈奇角色”，目前仍为尚未接通的提示入口。系别页参考原版 `Login/Tutorial/PickSchoolOfSpell.kids.html` 的代表技能，每系提供3个现有战斗动画预览，可切换、暂停、重播，最终确认才创建角色。演示不直接赠送技能。

按用户最新要求，冒险界面移除文件导入/导出；保留浏览器自动存档、角色隔离备份和Keepwork云同步，云恢复备份可直接在浏览器内恢复。新建页面在低高度窗口允许整体滚动，避免居中布局裁切。详细存储协议和并发限制见[player-import.md](player-import.md)。

## 2026-09-18：原版格子卡包与多套配卡

参考 `script/apps/Aries/Desktop/CombatCharacterFrame/CombatCardDeckSubPage.html` 的卡包标签、逐张方格及装备附卡分区，以及 Lua `Bag_RemoteSave` / `Bag_RemoteLoad` 的重复卡逐项保存结构。网页用现有卡牌中央主体图集绘制小方框，一张一卡位，三张同卡显示三个图标；不新增美术或外链。

每个角色可保留1–6套命名布局，新增时复制当前布局，编辑、切换和删除布局后点击“保存并使用”一起生效。容量仍由当前装备决定，不模拟原版尚未接入的付费解锁。已学法术点击加入一张；长按550ms移出一张，移动超过10px、离开或取消触摸即取消。也支持点选后的移出按钮和键盘Delete。至少保留一张，装备附卡单列只读。手机格子至少46px，单栏滚动。

v2存档新增 `deckLayouts` / `activeDeckLayout`，旧存档自动初始化第一套。`deck` 保持为战斗使用的当前布局；各布局共享持卡许可，不重复消耗库存。导入验证全部布局；换装备、调试改持卡量时同步裁剪所有布局。战斗中禁止修改。布局随角色现有本地/导出/云端存档保存。

### 2026-09-19 抱抱龙相遇界面

第二步增加主角与抱抱龙同场景展示，复用已选外观与名字；点选伙伴后跑到主角侧边。小字导入入口位置不变，手机保持可滚动布局，支持减少动态效果偏好。

### 系别技能自动演示

未点选技能时，三个代表技能依次播放，结束后停留1.8秒再播放下一个并循环。点选缩略卡或重播转为单次预览；切换系别重新轮播。技能选项使用卡包同款主体缩略图加旁侧名称，当前项高亮。暂停同时冻结演出与轮播间隔。

## 2026-09-19 经典魔法绘本界面

主UI和角色对话使用同一张94,572字节透明WebP。保留无头像的左上状态栏和右上功能图标，四种面板支持九宫格拉伸；桌面、手机竖屏和横屏分别适配。资源清单为 data/adventure/ui-art.json，复用方法、生成来源和验证记录见 [界面美术](ui-art.md)。

## 2026-09-19：紧凑卡包与全系牌库

卡包改为横屏左右分栏（左卡包、右牌库），手机竖屏上下排列。压缩标题与工具栏；新增卡包为标签末尾“+”，改名按需展开。大卡不再常驻占列：鼠标悬浮预览、点击固定预览，手机居中弹层；装备附卡默认展开，仍可折叠。牌库页码、训练点与保存按钮同一行。

卡包图标支持拖出格子区域移出、右键移出、550ms长按移出及Delete。拖动取消长按计时，在格子区内松手或pointercancel不删除；每次只移出一张，至少留一张。触摸卡位用于拖动，页面及牌库区域用于滚动。

牌库从已有 `data/kids/cards.json` / `card_names.json` 构建，包含六系681条法术定义，排除20条内部回合/状态指令。支持本系、平衡、其他系筛选、已学筛选、名称搜索及每页36条分页。656条已支持法术可学习配卡，25条未支持效果保留预览并标注；不静默当成可用卡。学习不限角色系别，每种获得3份许可，保存时学习结果与全部布局一起验证并提交，关闭未保存草稿不发卡。

学习等级采用源卡 `requireLevel`，缺失时保留既有课程等级（后缀变体继承课程）；无等级数据的条目在网页允许1级学习。这是网页全卡学习入口，不声称复现原版训练点、符文消耗或付费获取规则。自动升级赠卡和推荐配卡仍沿用本系原课程。跨系持卡纳入旧存档校验、调试编辑、云端保存和战斗规格，不改战斗公式。

### 手机战斗手牌手势（2026-09-19）

按住卡面左右滑动可高亮浏览，继续向上拖动并松手出牌；上拖超过56px显示就绪提示，拖回后松手只选牌。只有一个合法施法目标时直接施法，否则保留选中卡牌并提示点击目标。魔力不足、冷却中或待弃牌不会提交施法；战斗演出期间手势禁用。单点选牌和弃牌按钮保持可用。

### 战斗队伍状态布局（2026-09-19）

敌方状态集中左上，我方集中右下，按实际阵容显示最多各四人；自己的面板以浅绿金色背景区别。面板提供名字、等级、生命、普通/超级魔力点及附加状态，并可点击选择合法施法目标。法阵角色下只保留名字与血条。炫彩品质改为卡底粗色条，沿用原品质颜色；符文卡与宠物卡新系统尚未扩展。

### 全屏法阵与回合演出（2026-09-19）

法阵使用整个战斗视口，手牌浮在法阵上方。提交行动后隐藏手牌与出牌操作，直到本轮所有单位的施法及后续效果播放完成才恢复；剩余手牌直接出现，新补牌依次进入。队伍角落状态在演出中保留，胜负结果出现时不再展示手牌。

### 主动选牌与中央瞄准（2026-09-19）

开局和新回合不默认选中手牌。点选卡牌后，其余手牌隐藏，当前牌移到屏幕中央，下方可弃牌或重新选择；点击法阵角色、左上敌方状态或右下我方状态选择合法目标。中央卡面不拦截法阵点击。手机上拖进入瞄准时隐藏其他牌，回拖或中断恢复；松手仍沿用唯一合法目标自动施法、多目标等待点选的规则。

## 2026-09-19：原版卡包装备与等级限制

卡包是背包中可购买、可装备到24号部位的独立物品。复用原始 globalstore 导出的21种口袋，以167读取总容量、170读取同卡上限、168读取使用等级、169读取学系条件。角色升级解锁更高等级的口袋，容量不会仅因升级自动增加。例：金光口袋20级30/4，30级精良口袋45/5，斗士大卡包48级70/5。

商店新增“卡包”分类，卡包编辑器显示已拥有口袋的选择器及购买入口。换装先在草稿中预览，和所有配卡方案一起保存；较小口袋按顺序裁剪，保留已学法术及物品。标签和“+”仍为最多六套配卡方案，不等同于购买新物品。网页已有未装备时14/3的基础配卡保留；购买价格继续使用网页 BalanceParams 奇豆定价，不声称复刻原服售价或付费解锁。

全卡牌模式将已学记录解释为法术使用资格，同卡可放数量由口袋决定，不再被历史记录的3份卡死。旧存档的已学字段无需改写；原章节独立测试模式保留原有持卡份数。当前导出学系口袋171/172/173/175/176均为3，与通用170一致；尚未扩展不同上限的学系加成卡包。

新增 bagRulesVersion=1。旧存档中不符合等级/学系条件的口袋自动卸下，物品留在背包并显示说明；已开始的旧战斗仍按原规格重演，结算或撤退后迁移。普通存档、配卡保存及调试降级均执行新装备条件。

### NPC 对话操作（2026-09-19）

NPC 对白逐字显示并在标点短暂停顿。点击或按空格先显示全文；全文显示后，点击空白处或按空格执行高亮默认回应。其他回应可直接选择，关闭按钮和 Esc 可退出。Tab 可在对话按钮间切换，按住空格不会连续跳过台词。系统启用减少动态效果时不播放逐字与入场动画。桌面和手机共用羊皮纸对话面板，任务奖励独立显示为标签。

## 2026-09-19 强化装备入口
背包和角色装备面板新增“强化装备”标签，仅列出持有且原配置支持强化的装备。任务63007追踪直接打开该标签并优先选择晶石法杖，详情顶部显示强化按钮、累计属性、消耗与持有材料。成功强化后回青龙交付任务；未穿戴也可强化，穿戴后属性生效。晶石法杖依次消耗70/140/280仙豆，累计攻击1/2/3%；其他装备取自身原版配置。具体来源及实例/高阶材料限制见 lua-mapping.md 的本日记录。

## 2026-09-19 原版装备强化窗口

背包“强化装备”、单件装备详情“装备强化”、任务63007追踪均打开独立强化窗口。桌面750×450，复用原版蓝色窗框、道具槽、箭头、标签与按钮；七分类，每页12件，显示下一级累计属性、所需及持有材料。选择装备后点击“强 化”，成功后任务79016推进，再回青龙交付。

完整收录468件原强化装备与仙豆、祝福魔珠要求。同款多件使用独立实例，可在装备详情切换穿戴；满级只从强化候选列表移除，不删除装备。强化只改实例等级，保留宝石数据。老存档自动迁移，背包数量汇总仍保留。手机使用纵向布局。

原版高阶副本、宝石镶嵌、装备继承及账号接口未由强化功能新增；祝福魔珠的原获取说明可在材料提示查看，不添加虚构掉落。规则、资源与差异详见 lua-mapping.md。`tests/fixtures/strengthening.html` 是内存隔离验收页，不读写玩家存档，不进入发布构建。


## 2026-09-19：世界地图与跨岛传送

右上角地图打开可选择的海岛地图，显示当前位置、角色等级、开放等级与锁定状态。选择岛屿后点击传送；魔法营地和哈奇岛1级、火鸟岛10级、寒冰岛20级、沙漠岛30级、幽暗岛40级。等级通过 BalanceParams.worldTravel 统一配置，显示和规则检查共用同一函数。达到等级即可免费传送，哈奇岛不再要求毕业；毕业任务与奖励不因传送自动完成。

岛屿名和世界标识参考 Scene/WorldManager.lua L1160–1165；等级检查参考 L593。本机缺少 config/Aries/Scene/AriesGameWorlds.config.xml，上述等级是网页改编，未声称复刻原服门槛，也不模拟传送石或VIP限制。

新增四岛使用现有场景资源和不同地表色彩，开放行走、全局商店与现有试炼入口；2026-09-20已升级为独立大地图，入口与船长统一打开世界地图。尚未导入各岛原版NPC、怪物与任务。地图采用原生CSS绘制岛屿、海洋与地标，没有新增图片或第三方依赖。窄屏使用两列岛屿布局。

传送统一经过纯规则校验，战斗中不可传送；存档、云端预览和位置标签支持六岛，读档拒绝未达等级的岛屿。调试降级时自动回营地；在外岛追踪初心任务会先回任务所在地。保留既有camp/town标识与v2存档版本。


## 2026-09-20：世界地图美术

世界地图改为参考原版kids世界地图重绘的羊皮纸六岛WebP，按需读取Keepwork CDN；文字与等级仍由界面生成，原有解锁规则保持不变。手机可横向查看，当前岛屿自动居中。图片失败显示文字目的地列表。来源、完整生成提示词和资源说明见[世界地图美术](world-map-art.md)。

### 2026-09-20 强化界面风格调整
按用户最新要求，强化窗口统一采用当前程序的故事书皮肤、纸色背景及绿色主按钮；保留左侧操作区、右侧七分类/12格分页和手机纵向布局。原版蓝色皮肤资源仅保留来源归档，运行时只复用其中的材料图标。此调整覆盖上节原版皮肤与固定尺寸说明，不改变强化规则。

### 奖励反馈（2026-09-20）

任务和战斗获得经验或物品后，显示奖励横幅及角色光环；升级时播放角色金色光柱、粒子与实际等级文字。底部背包入口发光，新装备和卡牌逐件弹出提示：装备可立即穿上（保留等级与学系检查），卡牌进入配卡编辑器。普通物品通过横幅显示名称和数量。同一时刻已经排在队列里的多件奖励会按物品合并数量，并叠成一组提示同时出现，不再逐条各占数秒。超出一屏的部分仍汇总为“另获”。女、男角色均支持；动画为临时表现，不进入存档，读档不会重播。面板和对话打开时暂缓演出，任务领奖完成后关闭对话以展示奖励。无奖励或全部处理后不显示空白气泡。

## 2026-09-20：哈奇岛大型探索样板

按原版地图的北部雪山、西部林田、河流桥梁、东南小镇与东侧海岸关系，哈奇岛扩展为5600×4400；新增八区路牌与岛内导览，现有居民和怪物移入对应区域。采用有上限的分块地面缓存、视野空间查询和道路寻路。旧哈奇岛存档一次性迁至新出生点，保留进度与战斗记录。五个主岛原图已校验归档；其他四岛现已分别扩建，专属美术和新剧情留待逐区制作。详见 [大岛探索](island-exploration.md)。

### 法斯特船长与旅行入口

法斯特船长（36205，或其他岛同名居民）点击交互直接打开世界地图，不再通过对话按钮直接前往哈奇岛。六岛既有旅行入口也统一打开世界地图，玩家选择目的地并点击传送后才切岛。入口标签和岛内导览提示同步更新；探索隔离预览页采用同样流程。

## 2026-09-20：六岛配置生成

统一编辑入口为 `config/maps/generator.json`，集中道路、森林、地域配色、雪山和天气。`config/maps/islands/` 保存六岛各自布局，`npm run generate:maps` 生成 `data/adventure/maps/` 下的独立 JSON，并同步章节尺寸/出生点索引；构建检查生成配置未过期。火鸟5200×4400、寒冰5200×4600、沙漠5800×4600、幽暗5000×4800，均有六区导览、回环和安全入口；营地保留教学规模。世界对象从生成数据装配，森林不再在运行时随机生成。

同一区域配置同时控制地面、积雪树木、山体与限额天气。所有岛使用分块地面缓存和视野对象查询，不增加图片下载。旧外岛存档迁移入口，已有哈奇岛大地图位置保留，布局版本更新为2。制作步骤和字段说明见 [大岛探索](island-exploration.md)。

## 2026-09-20：自然积雪树、天气图集与场景查看器

四种积雪树替代普通树上叠加白椭圆，六种天气共用透明图集，预算和减少动态效果行为保持。两张图集总计292,470字节，使用已校验永久CDN。场景预览页支持直接选择六岛、区域和天气，缩放、天气暂停、HUD切换，不读写玩家存档。

地图交互统一：“世界地图”普通入口默认“当前地图”，顶部两按钮切换；法斯特船长和旅行入口默认世界地图，必须显式传送才换岛。移除单独岛内导览按钮。素材和查看器说明见 [environment-art.md](environment-art.md)。

### 2026-09-20：卡包顶部精简与物品限制
移除卡包装备标题和升级说明，卡包标签、口袋选择器与购买入口合并到紧凑工具行，继续使用左侧卡槽、右侧牌库布局。新增标签受已拥有且满足使用条件的卡包物品总数量限制（最多六个）；没有口袋时保留一套基础配卡。旧存档已有方案可继续编辑保存，但不能超出物品数量新增。此规则取代上文免费新增六套方案的说明；物品选择仍沿用统一装备口袋，尚未改为每个标签绑定独立装备实例。

### 2026-09-20：宝石镶嵌精工坊
背包增加“镶嵌宝石”，单件装备详情可携带实例直接打开。采用原版左侧装备/宝石/三张镶嵌符、右侧七分类与九格分页布局，沿用当前绘本皮肤和共享关闭按钮；手机上下排列。装备复用已发布图标，宝石和符采用原生CSS标记配中文名/等级，尚未导入其原版图片。

依据原kids代码实现部位限制、容量、同类升阶替换确认、成功率和四级以上100%限制，以及调羹剥离；操作结果进入本地自动存档，实例宝石属性进入装备详情、角色属性和战斗规格。原宝石/材料104项由数据库导出，不增加商店价格或掉落；未持有时展示空状态。服务端随机结算的本地适配差异见lua-mapping.md。

`tests/fixtures/gems.html` 是独立内存验收页，可体验全部宝石与材料，不读写玩家存档，不进入发布构建。

## 2026-09-20：训练点与课程学习

卡包默认开启“只看已学”，学系默认“全部”，因此已学的跨系法术也会显示。关闭筛选可以浏览其他卡牌。牌库显示剩余训练点；预览及快捷学习按钮共用核心校验，学习和扣点在“保存并使用”时一起提交，关闭草稿不会扣点。重复放入、移出卡牌不重复收费或退点。

从原版 extendedcost 数据导出105条学习项至 chapter.skillLearning，按原课程的等级、学系、前置资格与训练点费用检查。本系课程免费；外系必须存在原兑换配置。未核实学习途径的卡牌（包括许多绑定/品质变体）可预览，但不能直接学习；缺少可实现条件的课程保持锁定。这取代此前全卡库任意学习的规则。

升级发点表在本机缺失。网页暂采用 BalanceParams.skillLearning.pointLevels：4、8、12、16、20级各1点，之后25至50级每5级1点；此为明确的网页规则，不宣称与原服完全一致。存档记录已达到的发点等级和已花费点数，重复同步/读档不会补回已花点数。旧存档已学卡保留、不追扣，首次迁移按当前等级提供训练点。当前章节已有本系自动赠卡继续保留。

导出：python scripts/export_skill_learning.py；常规 export_adventure.py 也会刷新学习快照。原 Mentor/7Mentor.xml 缺失，因此尚未复现导师菜单排序、全部任务课与洗点付费接口。外系资格与前置条件以已导出的兑换表为准，不用名称或技能后缀猜测。

### 2026-09-21：卡包点击、拖动与详情

牌库整张卡牌点击直接放入左侧，移除卡牌右上角的加号；不能放入时沿用容量、训练点及课程规则提示。手机长按550ms打开详情，滑动滚动或长按后不会误触放入；电脑悬停预览，右键打开固定详情。

左侧卡槽拖动时，卡牌图像跟随指针，原卡位变淡。拖出格子区释放移出一张，区内释放、取消或丢失指针捕获保留；长按/右键改为详情，不再直接删除，Delete/Backspace及详情内移出按钮保留。训练点并入牌库底部分页行，取消独占的一行。

### PC卡包窗口高度（2026-09-21）
桌面宽度大于650px时，卡包窗口目标高度为680px；视口较矮时收缩至视口高度减44px。筛选、分页、卡牌增减不改变窗口高度。牌库占据剩余空间并内部滚动，保存按钮保持在底部；手机纵向布局沿用原规则。


### 2026-09-22：六岛居民、原版商店与导师目录

新增 `npc-catalog.json`，由 `python scripts/export_npc_catalog.py` 可复现生成，常规冒险导出也会调用。完整保留六岛292条NPC实例（营地27、哈奇218、火鸟9、寒冰14、沙漠11、幽暗13）、1764条商店原始行、8位导师课程、1699份关联兑换配置，以及相关物品原始记录、XML和来源SHA-256。商店表中不在六岛的NPC、负数/活动商店也保留在目录并列入报告，不虚构场景居民。

地图使用各岛真实居民替换临时来访居民；已有教学坐标保留，新增居民沿可行走道路确定性排列。这是二维地图适配，不等于还原原版三维坐标。尚未准备专属立绘的居民使用通用人物标记；来源模型、位置与原对白保留。没有导入全岛任务剧情或执行原Lua交互脚本。

交谈可打开该NPC原菜单关联的商品与课程，支持搜索与分页。五系导师、营地对应导师、白龙导师通用课程、伍迪和火百灵的选修课均按原导师表选择本系/外系兑换，检查等级、前置技能和训练点。本次找到的 `Mentor/7Mentor.xml` 覆盖此前“导师表缺失”的历史记录。课程表中的optionskill只引导到指定导师；不绕过原服务条件。

可本地执行已支持的技能学习、永久装备与材料的确定性兑换，以及已支持的符文兑换；白龙导师愈合符文为10火玉。2026-09-24起支持原兑换表中的魔豆费用：汉特的精良捕鱼器3魔豆、捕鱼网2魔豆。普通永久装备/材料的持有前提只检查、不消耗，费用仍严格读取costs；兑换装备进入实例背包。学习/购买先保存后替换角色，失败不扣费、不发货。

会员商品复用网页现有会员身份，购买前刷新并检查到期；这是Keepwork会员适配，不等于原服魔法星等级。原服星级、职业和服务器标记等特殊条件继续阻断。坐骑、原服直购、限购/有效期、装备抵扣、洗点和回售仍未实现，不能将当前状态称为原版商店完整迁移。直购物品快照没有正数奇豆售价，不据此免费发货。洗点原kids费用为100魔豆且保留任务卡，当前存档尚无完整学习来源记录。不会把已学技能当成可出售的库存物品；未同步或修改原服资产。

验证：374项全量测试通过，补充导师跨系/重复学习、白龙通用魔法、火鸟本系课程、符文原价兑换/重载/保存失败与六岛坐标测试。完整构建含地图门禁通过。隔离浏览器验证课程学习、符文数量0→1、搜索保留、键盘关闭和390px无横向溢出；本轮为无美术兜底预览，未进行全岛原画验收。

### 居民小图（2026-09-22）

全岛居民外观由 `data/adventure/npc-art.json` 按目录 instanceId 绑定；`scripts/prepare_npc_art.py` 从原版清单校验下载高清 PNG；低清缩略图或缺图时通过 Paracraft CLI 在独立 mini-scene 以 512×512 渲染模型与原装扮，再裁切到 256×256，禁止放大 64px 缩略图充数。每张独立透明 WebP 至少 256×256，严格小于 48,000 字节（2026-09-22 用户更新，覆盖之前 16KB 约定），默认使用已验证的永久 Keepwork CDN 地址，`?assets=local` 读取仓库归档。来源条目、原文件与转换哈希均在清单保留；原版隐藏触发器和空几何不绘制，冠军雕像保留原底座（在线获奖玩家外观不在本地静态数据内）。运行时按视野加载，不再沿用未绑定 NPC 的通用人形。

重建需先启动项目的 `paracraft-cli-530.bat`，再运行 `python scripts/prepare_npc_art.py`；上传后运行 `python scripts/prepare_npc_art.py --verify-upload-log <上传日志>`，逐文件核对 CDN SHA-256 与 CORS 后才写入 URL；本地检查使用 `--check`。新目录 NPC 缺失绑定或图片超限会直接报错，防止再次悄悄退回占位人形。


## 2026-09-22：全岛任务和怪物原始目录

`npm run export:quests` 导出684个kids任务（含257个废除任务）、607个完整怪物模板/611个定义和540个法阵实例，保留原对白、条件、奖励、掉落与AI数据。530个怪物提供现有359种宠物的外观候选，23个主线首领准备原模型渲染清单。19张64px原版缩略图已转为仅供对照的WebP；高清图待原生渲染服务可用后制作。这是完整静态导出，尚未把670个目录任务变为可玩任务，也未应用宠物替身。来源、限制和复现方法见[全岛任务与怪物目录](quest-catalog.md)。


## 2026-09-22：副本小世界与自动云存档

从原版世界表、竞技场出生点及怪物模板导出92个kids/通用实例世界、315组出生点、372份怪物模板，保留源文件哈希和原XML。61个世界可进入二维投影地图，35个世界全部组可挑战，共244组当前可开始并重演；其余按组显示缺失模板、卡牌、目标或怪物先手/场景脚本原因。31个无可用出生点或缺配置的实例仅收录目录。地图支持多怪组、原卡位、清怪进度、继续/重开、出口与返回原岛位置，清完显示通关；地图投影与通用学系外观为Web适配，不宣称原三维地图、机关、过场、难度、门票、宝箱/服务器奖励完成。

右上角“副本”替代云存档，设置保留云端恢复入口。升级、战斗结算、任务/重要操作、进入退出副本请求后台保存；相邻操作合并，30秒重试间隔，普通游玩10分钟兜底。保留访客本地存档、账号隔离、远端核验和冲突保护；不自动弹登录、不覆盖远端冲突。副本世界内主角和宠物不会自动回血，需用现有生命通关；回到岛屿后自然回复继续。

制作入口见[dungeon-authoring.md](dungeon-authoring.md)，可编辑config/dungeons/custom.json增加原创世界，再运行npm run export:dungeons；自动生成[dungeon-catalog.md](dungeon-catalog.md)逐组来源与迁移状态。隔离预览tests/fixtures/dungeons.html不读写玩家存档。


### 2026-09-22：副本改为整包按需预加载

按用户澄清，保留一个完整dungeons.json，不拆成每副本小文件。启动只带轻量目录；首次进入任意副本整包加载并缓存，恢复正在副本中的存档也先预加载。现有Vite构建同时生成主站与独立副本JSON，无需额外打包命令；完整副本JSON排除出主adventure.json。原始配置和原创入口不变，详见dungeon-authoring.md。


### 2026-09-22：23个剧情首领原模型图

已启动本机Paracraft，在独立mini-scene渲染完整23个剧情首领，归档透明256×256 WebP至 `assets/adventure/bosses/`；最大44,354字节，清单 `boss-art.json` 保存来源、原始模型条目及哈希。替代此前“高清图等待原生服务”的状态。当前仍是本地资源准备，CDN上传和战斗绑定未执行；原宠物复用候选及剧情任务开放范围不变。

### 2026-09-22：任务手记接入全岛目录

底部“任务”打开原有左右分栏任务手记，现可浏览全部684条原版任务。增加地区、状态筛选，名称/编号搜索，每页20项及前置任务跳转。详情保留原目标、NPC、接取条件、奖励组和学系选项；已收录物品复用通用详情窗口。手机上下分栏，筛选和分页固定可见。

14个教学任务仍读取章节进度。其余未废除任务可以接取、追踪和交付，进度写进角色存档。手记里的“前往接取”会走到居民身边；居民不在当前地图时直接接取或交付。剧情对白脚本和变身不执行。红蘑菇对战、充值、人气和原服徽章显示为无法推进。

`quest-journal.json` 是约472KB的精简展示数据，首次打开任务窗口才加载；失败保留教学任务并提供重试。完整源目录不进入启动包，构建生成独立任务JSON；普通静态服务同样可用。桌面与390px隔离内存角色通过筛选、搜索、前置跳转、废除/空态、失败重试、追踪和关闭检查，无横向溢出。全量416项测试与完整构建通过。

## 2026-09-22：原版Boss CDN与战斗外观

23张Paracraft原模型透明WebP已上传至Keepwork永久CDN，逐张核验SHA-256、文件字节数与CORS；256×256，合计809,766字节，最大44,354字节。`boss-art.json`保留原始模型来源、本地文件与实际CDN地址，`boss-art-plan.json`标记cdn-verified。

`package_monster_art.mjs`生成运行时`monster-art.json`，按原始模板路径与模型绑定607项（116项原Boss模型外观、491项宠物外观），共复用25种现有宠物。27项额外外观改编记录在adaptations中，其余沿用已匹配模型；仅改变显示，不改变战斗属性、捕捉身份或存档。地图遭遇与战斗单位统一走`drawMonster`，23种Boss全部支持现有攻击/受击/倒地位移效果。原版脚本未支持的遭遇仍按原有规则禁用，不因美术接入而开放。

验证：419项测试通过，npm run build通过；全部611条原怪物记录、372项副本怪物与7项主线怪物都有外观绑定。独立只读浏览器预览中，23张Boss与25种宠物均在CDN和显式local模式解码成功，Canvas可读；全部Boss分组通过实际renderBattle绘制，桌面与390px检查无脚本错误。预览入口为`tests/fixtures/monster-art.html`，不读取或写入玩家存档；Boss战斗预览使用原属性但不执行AI，不代表新增剧情已可玩。构建只含代码和JSON，不重复携带静态美术。未提交、推送或发布网站。

## 2026-09-25：捕鱼直接融入海岸场景

点击岛屿海面显示原场景鱼群和底部操作条，不再打开模态渔场。点击鱼影或使用撒网按钮（空格可操作），普通网需在撒网后的金色时间区间收网；提前、超时或未罩住鱼影均不消耗渔网与精力。必中网/捕鱼器仍点海面自动结算。鱼群、网圈、渔获浮字与本次连中计数仅是表现，不改变兑换奖励、概率、库存、精力和存档协议；动作继续通过 persistReward 先持久化后替换角色。

鱼影有60×52触控区域，原生选择器切网，药剂沿用现有入口；支持PC鼠标、空格和手机点击。低动态偏好取消收网时间限制。点陆地、移动、Esc、关闭、打开其他面板、失焦或切后台会退出并取消尚未结算的操作。副本不开放捕鱼。点击底部海面会把鱼群调整到操作条上方的可见海域。

隔离预览：tests/fixtures/fishing.html，仅用内存角色；?empty / ?tired / ?fail 可验无网、精力不足和动作保存失败。此页不读写真实存档。

## 坐骑（2026-09-25）

`data/adventure/mount-catalog.json` 从 `demos/mount-lab/catalog.json` 复制，并写入图集与战斗属性。可骑乘的是四向图集齐全、且原物品为 class/subclass 2/6 或 10/1 的条目；龙、鸟、车、飞毯概念图留在美术演示里，不出售。限时坐骑和两项价格都为 0 的条目不进入商店。

岛屿 NPC 的 `exchangeId === 0` 直购按原 globalstore 价格扣费：`count` 为魔豆（984），`ebuyprice` 为奇豆（100）。霸王虎 16071 为 300 魔豆，且 `stats[180] === 1` 仍要有效会员。`expiretime` / `expiretype` 不为 0 的条目不出售。名称带“天”的短期坐骑仍按原价在 NPC 商店出售；网页没有到期计时，买下后可以一直骑乘。哈奇商城每个坐骑只列长期款，价格与对应 NPC 相同。购买只增加背包数量，骑乘不消耗物品。

背包在炫彩鞋子后增加坐骑槽，筛选增加坐骑。骑上写入 `save.mountId`。地图按当前朝向、战斗按朝右绘制坐骑和骑手；跟随伙伴仍是英雄身旁的小伙伴。骑乘时移动速度乘 `BalanceParams.adventure.mountSpeed`（默认 1.35）。战斗属性来自目录里的 `stats`，与装备实例分开累加；变身类原属性不是战斗加成，目录里另写少量生命、伤害和抗性。战斗中不能上下坐骑。

## 地表装饰图集（2026-09-25）

新增 terrain-decoration-art.json，登记三张永久 CDN WebP 和本地副本，共48格草花、石木与滨水装饰；每张小于200000字节。资源阶段并行加载，失败保留基础地形。adventure_ground_decorations_core.js 使用独立世界格子种子按区域放置，保持道路、桥梁和交互区域通畅；adventure_large_terrain.js 只在瓦片烘焙时绘制，概览不加装饰、运行帧不新增装饰队列。图集重新准备使用 scripts/prepare_terrain_decorations.py，CDN 核验使用 scripts/verify_terrain_decorations.mjs；默认仍用 CDN，源码服务 ?assets=local 使用已归档副本。

## 营地教学对白精简（2026-09-25）

14个教学任务及8次居民/导师交谈从90段压缩到37段。接取通常1段、开场2段，交付与居民交谈各1段；任务简介同步到任务手记。删除旧PC操作与过时功能说明，改用当前网页的点击入口。任务编号、顺序、目标、奖励和存档结构不变。

文案维护于 `config/opening-dialogue.json`，由 `scripts/lib/opening_dialogue.py` 在章节导出时应用；原始对白及替换内容保存在章节 `dialogueAdaptations` 中，原始任务目录不改写。`scripts/package_quests.mjs` 使用章节的教学任务简介覆盖手记展示。隔离预览入口：`tests/fixtures/opening-dialogue.html`，只创建内存角色，逐段显示全部对白。

## 2026-09-25：主角持竿垂钓（替代上一版场景撒网）

根据体验反馈，取消屏幕底部操作条与瞄鱼节奏条。点击海面，角色在近岸站定并挥竿抛线，浮漂轻动等待，咬钩时主角上方与身旁按钮给出金色提示；点击或空格提竿。过早或错过咬钩窗口属于未命中，不扣网与精力。必中道具在咬钩后自动收杆。低动态模式取消咬钩截止时间。

新增竿身弯曲、鱼线张弛、浮漂、水花、收杆后仰、实际奖励鱼跃出水并飞到头顶的动画，以及头顶物品图/名称/数量展示。复用现有鱼类物品图，竿线与水效使用原生 Canvas 绘制；鱼竿是演出道具，不新增背包物品或付费消耗。钓鱼期间临时站立，不改变已有坐骑存档；退出恢复。捕获结果先经过既有 persistReward，再展示真实结果，失败不虚报渔获。

所有操作环绕主角：68px抛竿/提竿按钮、64px浮漂点击区、脚边精力/渔获及渔具/退出入口；渔具展开后切换原渔网/捕鱼器与使用药剂。任务追踪临时隐藏，减少干扰。点远海只选择附近水域，内陆角色先寻路走到可站立的岸边，再在175世界单位内抛线；不可达则提示，不瞬移。走开、关闭、切换窗口或后台会取消未结算动作。

### 场景钓鱼：多轮拉扯与个人鱼重（2026-09-25）

普通渔具需要 3–5 次金色提示提竿，中间松线 650–1100ms；后续反应窗口逐渐缩短。松线时过早再按或错过提示会逃鱼，未结算不扣渔具。PC 点击/空格、手机点主角旁按钮或浮漂；鱼竿和角色局部抖动，支持设备使用短震动，减少动态效果时关闭抖动/震动并取消提示超时。必中道具仍自动完成。

每条实际渔获独立称重，双倍奖励分别记两条。鱼种（含原有马靴海产）各保留重量前十，按克排序、同重按捕获序号先后；新高和进入前十会在主角上方提示。重量纯展示，不影响掉落、精力、装备或战斗。未改版前的背包物品不补造重量。

`save.fishingRecords = {version:1,total,byFish:{[itemId]:[{serial,grams}]}}` 与奖励一起本地原子保存，导入/角色重载/云存档均校验并保留。个人榜以角色隔离；Keepwork `HaqiAdventure` workspace 的 `fishing/records.json` 是所有本人角色的榜单镜像，首次产生钓鱼纪录后才通过 cache API 写入及读取核验；后续仅在纪录变化或有纪录的角色被移除时更新，不随普通角色保存重复请求。镜像带 owner/revision，权威数据仍为角色存档；多设备冲突走现有角色同步流程，不通过镜像反向覆盖角色。访客离线可用，登录及角色同步后写入 workspace。

### 四方向提竿与临时近景镜头（2026-09-25）

提竿改成围绕角色的上、右、下、左四个 60px 按钮。每轮只亮一个金色方向，连续两轮方向不同；错方向、松线乱按或超时会逃鱼。支持点击/触屏、方向键和 WASD，按键自动重复不推进；水面点击和空格只负责抛竿，不能绕过方向判定。

钓鱼激活时镜头用约 620ms 缓动推近到最近缩放 1.4，结束时同样缓动拉回，中途取消会从当前距离往回放。原探索缩放仍单独保留。钓鱼期间忽略缩放手势；结束/走开/切换界面/失焦/隐藏页面等取消路径统一恢复；重复开始或停止不会覆盖原值。系统开启减少动态效果时仍立即切换。近景和排行榜均保持角色存档与奖励语义不变。

### 宽容钓鱼与重量表现（2026-09-25，覆盖此前失误即逃鱼规则）

每轮错方向或超时仍可继续后续提示；提前点击不会结束，松线时反复按只降低称重质量。整轮至少按中一次即收鱼，后续失误不造成空手；整轮一次未中或主动取消则无奖励。命中比例越高，重量范围越高；相同种子下全中鱼重高于仅中一次。重量仍不改变奖励数量、精力或战斗属性。

场景自动捕鱼器遇到原表空手分支时，99% 改用该工具自身已有的成功分支，故总空手率低于 1%（现有工具约为 1/7 × 1%）；使用独立 RNG，不额外推进原奖励随机流。无操作成绩的旧调用继续沿用原表。此项是用户要求的 Web 玩法覆盖，不是 Lua 原规则。

6 千克起显示“大鱼上岸”，12 千克起显示“巨物上岸”；实际道具图标随重量连续放大，大鱼增加出水波纹、金色光环和星形粒子。多条鱼展示最重那条的对应图标。横屏将鱼和文案分置角色两侧，减少动态效果时保留大图、静态光环及文字。

### 四方向点击动作（2026-09-25）

每次上/下/左/右输入触发 380ms 快拉慢回的视觉动作：按钮内部箭头沿方向短移、轻压缩并出现反馈圈，按钮命中区域不移动；角色左右倾身、上提/下压，鱼竿端点分别沿 X/Y 偏移并带动鱼线、浮漂。正确/错误方向都有反馈，提前和松线输入也会表现动作；自动工具不产生玩家拉竿动作。减少动态效果时保留静态反馈圈，关闭上述位移。动作只影响渲染，不改变重量、奖励或输入宽限。

## 五岛建筑与船只（2026-09-25）

五个大岛新增独立透明WebP建筑图集，各四组船坞、船只、建筑群、特色塔楼。清单为data/adventure/building-art.json，本地副本位于assets/adventure/environment/buildings-*.webp，所有域名默认使用清单的永久Keepwork CDN；明确的?assets=local使用本地副本。每张小于200,000字节。生成提示词为building-art-plan.json，重新打包使用scripts/prepare_building_art.py。

adventure_buildings_core按固定几何选取路旁空地和南向海岸，不改变原地图JSON；陆地建筑加入现有碰撞与深度排序，船坞及海船为装饰，角色遮挡时建筑半透明。当前五岛各10组陆地建筑、1座船坞、2艘海船；营地及副本不变。tests/fixtures/island-buildings.html提供无存档预览。

五岛传送点现有一条60宽道路连至船坞侧面的陆地路口，法斯特船长站在路口旁，完整人物范围与船坞图像不重叠。道路清除沿途树木并避开河流与建筑，沿用地图道路绘制与寻路。原船长实例优先保留，移除重复的临时引导船长；营地站位不改。

后续调整：五岛传送点已同步移到船坞旁法斯特船长脚下，与原交互习惯一致；现有道路从原路口连接到此处。

### 战斗即时弃牌（2026-09-25）

普通手牌向下拖动至少56px松手或右键后立即移出手牌，不保留灰色卡位或撤销入口；选中牌下方的弃牌按钮行为相同。鼠标、触笔与触屏支持下拖，触屏上拖施法与横向浏览不变。宠物卡和符文卡不可弃牌。弃牌随本回合出牌或跳过记录进入战斗重演，下回合沿用原有补牌规则。


### 四岛法阵组合恢复（2026-09-25）

岛屿运行时现按原始113处法阵加载整组怪物，其中23处为多怪组合；不再按模板拆散成单怪。另有15个子场景任务目标保留单怪入口。保留原怪物顺序、重复实例和XML空位，二维道路位置与现有替代美术仍为Web改编。旧单怪检查点保留兼容，地图/任务导航隐藏旧入口；野外多怪可重复挑战，按整组结算奖励与任务进度。

## 唯一装备与旧存档合并（2026-09-25）

装备导出保留原版 maxCount/maxCopiesInStack。maxCount=1 的装备及卡包只保留一件，重复获得不累加数量；其他装备继续使用独立 GUID。读档、奖励同步和原服导入会合并旧重复实例：优先保留穿戴 GUID，强化取最高等级（不相加），保留其镶嵌布局和最高开孔数；若该实例没有镶嵌记录则接收另一实例的记录，其余实例的宝石退回背包。进行中的旧战斗延后到结束或撤退后合并，避免改变检查点。

### 场景陪伴选择（2026-09-25）

探索场景始终显示一只跟随宠物：优先主角同卡位，再按卡位序号距离选择最近的已上阵宠物（等距取较小卡位）；阵容全空时优先背包已有抱抱龙，再选其他已拥有宠物。无宠物旧档展示默认绿色抱抱龙，仅用于陪伴，不改变收藏或战斗编队。语言学习入口与会话标题使用实际伙伴名字。

### 2026-09-25：选中卡牌效果说明

战斗中选中手牌会在卡面旁显示详细效果，重新选择、弃牌、施法或结算时收起。说明读取当前战斗参数，区分即时/持续伤害、治疗及吸收，X费显示每点魔力，冷却与施法规则一致；桌面与手机共用可滚动说明区。复杂特殊牌未完整收录时明确提示。卡面摘要由同一展示模块生成，不改变战斗计算。

### 宠物自选外观（2026-09-25）

宠物详情的四阶段进化路径可点击选择已解锁外观，养成和形态与卡牌页共用选择。偏好保存在宠物的可选字段 `appearanceStage` 中，收藏、场景跟随与己方战斗显示统一采用；未选择的旧档仍随等级自动显示。未解锁阶段不可选，导入存档校验阶段范围。外观不修改经验、等级、属性、技能、卡包容量或战斗编队；战斗中不可变更。

## 全向移动特效（2026-09-25）

主角与坐骑保留四方向身体，魔法飘带按碰撞后真实位移角度摆动。五系分别留下余烬、冰晶、电弧、叶片与幽焰，脚印/尘土固定在经过的世界位置；等级、已穿戴实例强化和镶嵌宝石增强密度、大小及寿命，成长后出现短残影。会员状态已验证且未过期时增加金色魔法星。它是视觉成长映射，不改变战斗属性；身体图集内已烘焙的装备仍保持四方向。

停止移动后自然消退；传送、换地图及上下坐骑清理旧轨迹；减少动态效果偏好关闭新增特效。独立视觉随机源和160个粒子上限。只读预览入口为 `tests/fixtures/motion-effects.html`（默认CDN，可切学系、成长、会员与骑乘，不写存档）。


## 钓鱼按需加载（2026-09-25）

启动只载入 `fishing-items.json` 的物品元数据，供背包展示及旧存档校验。完整 `fishing.json` 从发布启动包拆出，首次在岸边进入钓鱼时加载，并发请求合并、成功后复用、失败后可再次点击重试。加载期间离开场景、关闭窗口、切换角色或移动后不自动进入钓鱼。

### 魔法星跟随（2026-09-25）

游戏已使用蓝色VIP静态图集。已确认会员可见，等级沿用magicStarStatus（当前最多10）；16格是素材容量，未新增会员规则。星星在头顶附近轻快绕行、变换半径和速度，人物移动时产生短暂落后并追赶；骑乘锚定骑手头部，前后遮挡随位置变化。减少动态效果时改为固定相对位置。位置与速度仅留在渲染器内，不进存档。

图集通过data/adventure/magic-star-art.json按需加载默认Keepwork CDN，保留明确的本地资源模式。预览tests/fixtures/magic-star.html可点击移动、自动跑动和切换等级；不操作玩家存档。

### 战斗地面行动指针（2026-09-25）

法阵中央新增金色贴地指针，默认指向玩家；按实际出手顺序先旋转200ms，再开始该角色施法，伤害/治疗结算时保持朝向。覆盖宠物、怪物、失误、跳过和捕获，回合间保留方向；战斗结束隐藏。参照原版movearrow正向旋转，减少动态效果时直接切换方向。仅改变表现，不修改战斗记录和存档。只读预览：tests/fixtures/battle-pointer.html（?mobile为390px画布）。

### 战斗指针回位与样式更新（2026-09-26）

覆盖上一日的金色样式及回合间保留方向规则：指针改为半透明青绿色魔法符文轮廓；每轮结算后先转回下一轮首位存活行动者，再恢复选牌。按己方卡位及敌方先手配置选取待机朝向，结束战斗不再回位。
