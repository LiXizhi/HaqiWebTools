
## 右上角快捷入口圆形样式

- 签到、世界地图、云存档、设置复用底部导航的金边绿色圆环与木质文字底牌，移除整组黄色底框。
- 保留签到倒计时和领取提示，手机双列及横屏布局调整间距，避免任务追踪面板遮挡。
- 验证：npm run build 通过；git diff --check 通过。浏览器工具连接失败（nodeRepl.fetch request failed），未完成桌面及390px实际截图验收。仅修改视图标签和CSS，无引擎改动。
# 开发日志 2026-09-21

## 商城内测道具过滤

- 新增按ID维护的 `data/adventure/item-flags.json`，1122、1126、2025–2030八件测试道具标记 `isInternalTest`（是否内测）；导出器将属性写入候选目录，保留可重复导出能力。
- 扩展层把属性传入物品与商品，商城在搜索/分类/分页前隐藏；购买逻辑拒绝内测道具且不扣款。保留物品与历史商品记录，兼容已有购买记录和背包。
- 回归测试覆盖名单与候选一致、截图三件道具禁止购买且状态不变、旧购买存档可加载、普通商品仍可购买。`npm test` 264/264通过，`npm run build`通过。
- 未提交、未推送。
