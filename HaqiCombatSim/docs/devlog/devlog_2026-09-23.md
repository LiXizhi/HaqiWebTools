# 2026-09-23 开发日志

## 全岛任务可以推进

- 413条未废除、且不属于14条教学的任务进入 `quest-runtime.json`。接取、击败、掉落、交谈、交付和奖励都写入角色，教学任务仍按原章节顺序。
- 掉落沿用一次 `random(100)` 对 `floor(odds*100)`。网页战斗按困难难度计入，噩梦以上的目标不增加。
- 剧情对白脚本和变身不执行。红蘑菇对战、充值、人气和原服徽章保持无法推进。等级下限高于10级时，按当前等级上限处理。

## 会员到期日自动兑换魔豆

- 登录并确认 Keepwork 会员后，按北京时间把未兑换的到期天数自动换成魔豆，每天 10 颗。第一次从今天算起；兑到的到期日记在账号角色列表，下次只计算更晚的日期。
- 发放到当前角色背包。战斗中不兑换，结束后再处理。没有手动兑换按钮。访客不兑换。
- 会员窗口说明当前可兑数量或已兑到的日期。

## 捕获晶球改为抓宠符文

- 奇豆商城不再出售捕获晶球，补给分类只保留营养餐。野生战斗底部按钮改为抓宠符文，有库存时打开符文手牌。
- 背包里尚未用于当前战斗的晶球，在读档或战斗结束时按张数换成普通抓宠符文。已经掷过晶球的旧战斗仍按原记录重演，结束后剩余晶球再兑换。
- 抓宠符文和晶球出手都排在自动伙伴之前，避免伙伴先击倒野生宠物。
- 隔离页核对：补给只剩营养餐，搜索晶球为空；有符文时按钮打开普通抓宠卡，没有符文时按钮禁用。1920与390宽度无横向溢出。未写真实存档，未构建或发布。
