# 2026-09-20 开发日志

## 后续旧版宠物发放约定

- 用户要求：后续导入旧版哈奇宠物系统时，任务奖励及任何其他入口的宠物发放，全部映射到新版300多个宠物目录。统一旧ID到新ID映射，不以换图替代身份映射；具体约定见`docs/pets-and-shop.md`。
- 本次仅记录，未修改运行逻辑、存档或发放流程。

## 咕噜噜收藏外观修复

- legacy_gululu补上shanhaijing_xuangui_gugu（龟龟咕噜）的完整四阶段art副本，复用既有本地WebP、永久CDN、尺寸和哈希；不改身份、名称、生命系、技能、等级经验或教学任务。
- 外观引用、旧存档迁移与原十四教学任务三项测试通过。浏览器临时预览确认CDN解码1024×1024、四行裁剪及截图正常；共享浏览器当前角色未拥有咕噜噜，未修改收藏或存档。未构建或发布。

## 宠物显示比例与滚动条

- 站位宠物使用固定高度的贴底容器，内部图集保持1:1，取消同位宠物固定高度导致的纵向拉伸。桌面1280×800、手机390×844、横屏844×390实测宽高相等。
- 故事书皮肤下选中宠物tab改为深色文字、加粗及底部标记，避免浅色按钮纹理覆盖背景后白字不可读。
- 全冒险游戏故事书界面（包括独立dialog）统一半透明细滚动条、透明轨道与滚动角，支持标准及WebKit样式。卡牌横向滚动验证通过，文件诊断无错误；未构建或发布。

## 宠物卡包容量调整

- 按用户规则把四阶段容量从8/12/16/20改为2/4/6/8，保留1/10/25/40级阶段阈值与每卡最多3份限制。
- 旧存档按原配卡顺序裁剪；进行中的旧战斗保留快照，结算/撤退后迁移。21项专项测试通过，未构建或发布。

## 宠物界面图形化

- 站位底盘改为灰色双环法阵底座。主角/宠物拖动时生成固定定位、穿透指针的真实图像副本，按抓取点随指针移动，原位淡出；Canvas副本复制像素，宠物副本保留图集裁剪。松手、pointercancel及lostpointercapture清理影像；收藏横向滑动仍用于浏览。浏览器验证影像随指针移动50×20像素、主角像素非空、宠物图片有效、取消无残影、手机无横向溢出。未构建或发布。

- 阵容改为卡位共用绿色圆台，主角与宠物站在同一台面；移除主角姓名、副标题和“随行阵容”，仅保留“拖动角色”提示。家园入口改为图标在上、文字在下。四阶段与卡牌合为同页，图鉴只读入口不再显示分类切换；保留已拥有宠物的养成页和卡包编辑。1280×800与390×844合并详情页检查无横向溢出、无纵向滚动。

- 后续精简阵容：移除01–04、空位/等待伙伴和主角移动按钮，空位仅保留加号。复用角色精灵显示主角，与同位宠物并排；拖动主角只改变heroSlot，不移动宠物，保留方向键换位。浏览器双向拖动验证通过并恢复原阵容，角色画布非空，桌面/手机/横屏无横向溢出。

- 四个真实宠物站位替代下拉框；支持指针拖动、点击空位选宠、详情上阵与休息、主角同位切换。已有宠物移动到已占位时交换，不产生重复上阵。
- 收藏改为可搜索的横向宠物列表，左右翻页及指针横拖；养成、四阶段、魔法卡包放到独立详情弹窗。复用技能卡面渲染，以加减按钮配卡，保留等级、单卡份数、容量和非空限制。
- 照料说明折叠，教学伙伴移到独立窗口，保留家园与图鉴入口；矮屏横屏改成阵容/收藏双栏。
- 验证：19项宠物/宠物卡专项测试通过；浏览器检查1280×800、768×1024、390×844和844×390布局；桌面/平板竖屏/手机竖屏主面板与卡包无纵向滚动及横向溢出。实际验证卡牌加减、点击上阵、双向指针拖动并恢复原阵容。代码诊断无错误。未运行构建或发布。

## 签到倒计时布局

- 将签到按钮的倒计时改为绝对定位的浅金色标签，配细描边和轻阴影，最终放在“签到”文字下方，允许略跨下边框，不参与按钮的垂直居中布局。
- 保留原按钮尺寸、桌面四列和手机两列布局，不修改签到计时或领取逻辑。
- 手机两列导航行间距调整为12px，避免标签遮挡下一行图标。在现有本地预览中验证 390、768、1440px 宽度：签到与同排按钮文字对齐，标签位于文字下方、不遮挡下一行图标；检查桌面及手机按钮截图。
- 未运行构建。

## 2026-09-20：参考原版重绘世界地图

找到并校验原版 kids 的 `texture/aries/worldmaps/worldmap/worldmap_bg.dds.z`（完整条目和来源哈希见 world-map-art.json），ZIP解压DDS后归档参考WebP。使用内置 image_gen 参考其羊皮纸、海洋与岛屿方位生成新版，补画独立魔法营地，保留动态中文标签和等级条件。

新版1280×640、190,982字节；先尝试无损后采用高质量有损压缩。已上传永久Keepwork CDN，GET 200、SHA-256与字节数一致、CORS为*。world-map-art.json包含本地/CDN、独立来源哈希、完整源条目与六岛点击锚点。仅打开地图时加载，失败仍可用基础绘制；本地模式可读归档。网站构建不包含WebP。

浏览器实测桌面1280×720和手机390×844，CDN图像解码1280×640；选火鸟岛显示10级限制且传送禁用，选哈奇岛可传送。手机横向浏览、当前营地居中（scrollLeft=170，视口340，锚点340），详情与按钮可读。修复通用按钮active变换覆盖热点定位、导致点击时位移的问题。4项传送规则测试通过；生产构建通过。提示词与来源说明见docs/world-map-art.md。

仅发布图片资源，未发布网站、未提交或推送。额外digitalhuman-resource镜像需用户明确选择，本轮未复制。

## 强化窗口统一为当前程序风格
- 按用户最新要求保留左侧强化操作、右侧七分类/12格列表和分页，手机继续纵向排列。
- 移除原版蓝色窗框、黄色按钮与纹理标签的运行时样式，沿用当前程序的故事书窗口、纸色背景、主题色边框和共用 primary/secondary 按钮；只保留仙豆/祝福魔珠道具图标。
- 调整为内容自适应布局，避免旧固定坐标裁切属性和提示。强化规则、材料消耗、实例存档与任务事件未改变。
- 验证：12项强化专项测试通过。浏览器工具连接失败，本轮未完成截图验收。


### 清理旧版地图绘制

按用户要求删除旧岛屿、山峰、城堡、树木、罗盘、海洋网格和航线的DOM/CSS及旧布局坐标。只用新版WebP地图和动态标签/传送热点；载入中或图片失败显示文字目的地列表，不再生成被图片遮盖的旧图。

### 角色信息栏与会员入口

- 名字前复用卡牌的五系图标，保留系别的悬停提示和无障碍名称；移除信息栏原有“某系学徒”文字。
- 等级右侧显示背包中的奇豆（100）与仙豆（17213）余额，缺省为0；长余额换行，长角色名省略，保留会员按钮空间。
- “升级会员”不检查VIP身份，统一打开可关闭的说明窗口。现有项目尚未接入会员开通/续费服务，窗口明确显示暂未开放。
- 验证：npm test 全部221项通过，npm run build通过。浏览器连接连续两次超时，未完成实际页面截图和点击验收。未发布、提交或推送。

### 经验、升级与背包奖励提示

- 对照原版 `script/apps/Aries/Desktop/Dock/DockTip.lua` L630–687：装备提示“立即穿上”，卡牌提示进入配卡。女角色及男角色共用反馈。
- 任务领奖、战斗结算、签到、购买与学习动作按成功前后的实际差量产生表现事件。经验/物品显示奖励横幅与角色光环；升级增加金色光柱、升腾粒子与等级文字，支持连续升级及减少动态效果偏好。
- 新装备/卡牌在下方背包上方逐件提示，装备仍走原有等级、学系、持有及实例检查；卡牌进入现有配卡编辑器。提示不写存档，不在读档或角色切换时重播；窗口打开时暂缓播放。
- 修复用户现场指出的空白提示框：初始、处理完、reset 都显式隐藏；空内容 CSS 额外兜底。背包入口仅在实际奖励提示期间发光。
- 浏览器隔离验收页 `tests/fixtures/rewards.html` 不读写存档：验证待机无空框、升级特效、立即穿上实际生效、逐件处理后完全消失；1280×720 与 390×844 下截图检查通过。
- 奖励专项8项测试通过；全量224项中220通过，4项失败位于原有 adventure_pet_cards.test.mjs（期望6张附卡而当前得到2张，以及相关选牌失败），奖励表现未改动该战斗逻辑。构建通过。未提交、推送或发布。

## 2026-09-20：哈奇岛大地图与探索样板

用户选择先重做哈奇岛。找到五主岛原版俯视地图，按assets_manifest完整条目下载并核验MD5/长度；DDS压缩资源解压后保留alpha转为WebP，来源及转换哈希分开记录。额外核对了loginisland图片，确认它们是登录场景而非岛图，没有作为地形依据。参考归档在assets/adventure/island-references，不进入运行时或dist。

哈奇岛从1800×1600扩为5600×4400，参考北雪山、西林田、河流与桥、东南小镇、东海滨关系，八个区域、路牌、独立湖泊及环路。原三居民、两怪物重新布置，保留章节来源坐标与任务，不虚构原服新剧情。岛内导览提供地形、位置与按地标步行。追踪使用world实际坐标；worldLayoutVersion=1迁移旧镇位置，重演及奖励账本不变。

渲染采用最多24张516×516 Canvas地面缓存、可见范围空间桶查询、道路图寻路。大视野降低地面采样分辨率，避免LRU反复淘汰当前可见块；每帧最多生成两冷块，低分辨率地图补底。检查发现西桥引路擦到河岸与农舍，调整道路并新增所有道路双向实际行走测试。地图加载失败不涉及新下载，场景继续复用原CDN图集。

浏览器核验了1440×900桌面、390×844手机、正式Haqi.html与默认CDN（122个成功图片响应，无pageerror），导览、自动步行和局部地图画面。修复导览按钮被任务面板遮挡、关闭按钮换行、角色选择背景仍使用小岛相机中心的问题。隔离入口tests/fixtures/exploration.html不读写玩家存档。

完整验证见qa-report；后续分区美术、剧情制作规范与原图链接见island-exploration.md。本次保留程序绘制地面和复用建筑，尚无完整城堡、游乐设施或其他四岛新内容。没有提交、推送或上传发布操作。

## 法斯特船长打开世界地图

按用户要求，将船长交互和六岛旅行入口统一接到现有世界地图面板，取消船长对话里的直接传送动作，更新入口及导览文案。隔离探索预览页同步支持地图、目的地选择及旅行入口步行抵达后打开地图。地图原有等级门槛和传送按钮校验不变。

验证：Edge隔离浏览器逐一检查营地船长与哈奇/火鸟/寒冰/沙漠/幽暗五岛旅行入口，全部打开地图且存档zone保持原岛，无pageerror。探索预览也验证了入口打开地图后仍在哈奇岛，选择魔法营地并点击传送才切换。npm run build通过，git diff --check无格式错误。

## 六岛拆分与统一地图生成

按用户要求，拆分 config/maps/islands 六份作者源文件与 data/adventure/maps 六份生成结果，集中 generator.json 中的道路、森林、地表、雪山、天气和岛屿主题。新增离线纯规则生成器、生成/一致性检查命令；构建前验证生成配置，章节导出后自动生成。运行时直接加载生成JSON，生产压缩数据包支持 maps 子目录；出生点和尺寸由同一索引供存档、旅行、调试使用。

火鸟、寒冰、沙漠、幽暗按归档原图空间关系分别扩大到约2300万至2670万世界坐标面积。各自六区、回环、山体、水域、地标和入口，复用船长但不虚构新任务与怪物。自动桥梁采用沿路旋转矩形，实际碰撞与绘制共用几何，避免方形桥面过大。寒冰/雪山有分区降雪及雪树，沙漠沙尘、火山余烬、幽暗薄雾采用当前视口固定预算，最多64粒子，减少动态效果时关闭。

验证：235项Node测试通过；新增生成确定性/共享规则变化、六岛可达性、旧外岛存档迁移、天气预算和生产包读取测试。npm run build通过。Edge六岛隔离预览及导览无pageerror，390×844寒冰导览无横向溢出。使用dist和默认CDN逐岛点击船长，世界地图打开且存档zone未改变。寒冰六区同步绘制计时暖帧P95约1.0–16.7ms，首次切区最高约61.9ms；这是本机headless测量，不代表手机实机帧率。完整说明见island-exploration.md。未提交、推送或发布。

## 主角背包界面压缩与物品详情弹窗

- 顶栏统一为单行小字号「我的背包」，移除副标题。右侧分类从18项压缩为9项：全部、帽子、法袍、靴子、武器、卡包、衣饰、饰品、炫彩；保留左侧全部装备部位。
- 物品数量改为右上角数字角标；单件装备不显示数量，同款多件装备仍显示总数并支持实例选择。
- 点击格子打开原生 dialog；详情独立滚动，底部固定「穿上／卸下」与强化操作。关闭或 Escape 返回背包并恢复焦点。
- 继续复用 previewEquipment 的真实换装计算，以当前／换装后／变化表格展示角色数值；已装备物品显示卸下预览，保留替换目标名称、附加法术、卡包缩容提醒、穿戴限制。参考原版 Desktop/ApparelTooltip.html 的身上装备对比展示，不改数值公式。
- 验证：npm test 235/235；npm run build 通过。浏览器隔离内存存档预览检查1280×720和390×844，确认数量角标、单行标题、弹窗按钮可见、穿上后装备与属性刷新、多件装备选择器和Escape返回；未修改实际玩家存档。预览文件与输出位于忽略目录 .asset-cache。
- 修改保留本地，未提交、推送或发布。

### 背包分区与原版布局对齐（追加）

- 参考原版 Inventory/TabCharacter.html 的分类分区及现有强化窗口的4×3格子、分页、紧凑筛选按钮；统一角色装备与旅行背包的面板、图标尺寸和详情入口。
- 角色装备页只显示装备；旅行背包只显示非装备物品，分类为全部、消耗品、卡牌、收藏，不再混入装备筛选或已穿装备栏。货币仍显示在顶部。
- 格子仅保留图标、角标与已装备标记，名称通过悬停、无障碍标签与点击详情展示；空格补齐12格，切分类／搜索／切页签重置分页。
- 压缩左侧角色装备栏，属性默认折叠；保留单行「我的背包」、数值对比及固定穿上／卸下按钮。左侧已穿装备直接打开其实际实例详情，避免分页后找不到已穿物品。
- 验证：npm test 235/235；构建通过。浏览器检查1280×720及390×844；33种装备分页2/3，切回旅行背包只有2种消耗品且页码复位1/1；点击已穿帽子成功打开卸下数值预览。使用隔离内存存档，未修改玩家存档。

## 自然雪树、天气素材与统一地图入口

按用户反馈，使用image_gen生成四种积雪融入枝叶的冬树及六种天气粒子图集，移除旧白椭圆覆雪。新增alpha裁剪/压缩脚本、资源清单与完整提示词。WebP为180,648和111,822字节，通过项目上传技能发布永久CDN，重新下载核验SHA-256并在浏览器验证CORS/alpha。其他原图集不修改，dist不带美术。

统一renderMaps窗口：HUD“世界地图”默认当前地图，两种视图可相互切换；船长与旅行入口默认世界地图。原岛内导览步行回调保留，移除重复HUD入口。探索fixture改成带岛屿、区域、天气、缩放、暂停天气、HUD显示控件的测试页，维持内存角色隔离。

浏览器检查默认CDN与本地资源、雪树场景、六岛切换、八种天气选项、桌面和390px手机导览；生产构建六岛逐一验证普通入口当前地图、船长世界地图、切换成功且未自动传送。新增资源字节/哈希/alpha/裁剪回归与粒子图集预算测试。素材上传成功，网站未发布，未提交或推送；未获额外Git镜像复制授权，未复制。

本项最终验证：237/237测试通过，生产构建通过，git diff --check通过。

## 地图窗口精简

按用户截图反馈，地图页标题只显示岛名或“世界地图”，旁边放一个切换至另一视图的按钮。移除角色等级/当前位置摘要、品牌副标题、岛屿介绍、可传送/当前位置文字、底部教程与任务追踪入口，当前地图移除说明段落。世界图保留岛名与选中高亮，选其他岛才出现单个传送/解锁按钮，等级检查不变。

Vite构建通过；Edge检查岛名/世界地图标题、单按钮切换、传送选择、冗余元素移除及390px布局，无pageerror和水平溢出。未提交或发布。

### 背包标注图布局调整（追加）

- 仙豆和奇豆移入「我的背包」标题栏，切分类和页签时复用单个货币区。
- 姓名、学系与等级左对齐，与角色形象并排，减少角色区纵向占用。
- 按标注图的压缩意图移除搜索框和说明文字，分类直接连接物品格子，保留分类、分页和详情弹窗。
- 验证：JS语法检查与构建通过；浏览器验证桌面和390×844手机宽度，标题货币、关闭按钮、分类和分页正常显示。

## 地图底部只保留传送与关闭

根据最新截图要求，世界地图底部固定保留“传送”和“关闭”，当前岛屿或未解锁岛屿禁用传送，条件放在按钮提示和无障碍标签中。加强标题与右侧切换按钮的横向布局约束；角色信息、摘要、介绍、任务追踪提示均已移除。

验证1280px和390px：标题/切换按钮垂直中心同排，底部恰为两个操作，无重复信息DOM；实际选择火鸟岛后传送成功，无浏览器错误。生产构建通过。未提交或发布。

## 地图切换按钮文案

按用户指定，岛内地图按钮改为“打开世界地图”，世界地图按钮改为“返回当前岛屿地图”，无障碍标签同步；切换行为不变。

## 地图地名直接传送

删除当前地图列表底部重复的“查看世界地图”。当前地图采用可聚焦DOM地名覆盖层，点击图中地名或右侧同名按钮直接抵达已定义地标；实际控制器核验地标、可通行性与战斗状态，清空旧路径并保存新位置。世界地图点击已解锁岛名直接执行原travel动作，移除选中状态和底部二次传送区，保留标题关闭入口与等级检查。

验证：构建通过；浏览器检查岛内地名直接到达、世界岛名一次点击传送、重复入口/选中状态移除、390px显示，无pageerror。正式dist页面验证岛内位置已持久化并可刷新恢复。未提交或发布。

## 统一关闭按钮与GUI指引

将通用弹窗/NPC对话、岛内地图、云存档、卡牌浮层、物品详情、宠物详情、教学伙伴窗口全部接入 view_adventure_controls.js::createCloseButton。复用现有绘本图集的金边绿色关闭图标；统一48×48点击区、40×40图标、中文可访问名称、悬浮说明、焦点轮廓及加载失败时的SVG后备。清理storybook中历史重复的关闭按钮尺寸覆盖，子窗口只保留必要的位置规则。

新增 docs/gui-style-guide.md，涵盖组件入口、色彩与主次操作、标题/切换/关闭布局、地图直接传送、移动端、资源预算和验收步骤；AGENTS.md将其列入GUI修改前必读，明确禁止另造文字叉号。

验证：构建通过；Edge浏览器检查14处窗口/子窗口（两种地图、设置、任务、卡包及卡牌浮层、背包及物品详情、宠物及详情/教学、商店、云存档、NPC对话），图标与点击区一致。嵌套关闭保留父窗口，图集后备SVG可见，390px手机48/40尺寸不变，Enter可关闭；无pageerror。既有5180预览服务间歇启动超时，最终使用独立5182静态服务完成整组验证。未提交、推送或发布。

## 冒险手记目录与任务详情

- 参考原版 `QuestListPage.html`、`QuestDetailFramePage.html` 的目录/详情结构，改为左侧章节任务目录、右侧目标、接取/交付人物和奖励；已完成任务不再整体降低透明度。
- 手机改为上方任务目录、下方详情，两块独立滚动；沿用共享关闭按钮和已有绘本皮肤，无新增美术或依赖。
- HUD 任务标题通过 `openPanel('quests', {questId})` 打开指定任务，普通任务入口默认当前任务；“追踪”保留寻路行为。已完成与未开启任务可阅读，但不提供错误的寻路动作。
- 验证：构建通过；浏览器 1100×850、390×844 无横向溢出，指定任务选中、已完成状态、详情追踪回调通过。全量测试 237 项中 236 通过，1 项卡包 DOM 模拟测试因共享关闭组件需要 document 而失败（非本次任务窗口变更）。
