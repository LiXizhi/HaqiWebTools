# AGENTS.md — web/

## 分支约定（用户指定，2026-09-17）

- 本目录是独立的 Git 仓库 `LiXizhi/HaqiWebTools`。
- 默认始终在 `main` 分支提交并推送到 `origin/main`，除非用户另有明确指示。
- 此约定覆盖父级 `paraworld/AGENTS.md` 中先提交 `dev` 再合并发布分支的要求。
- 各子项目的其他开发与验证规则仍遵循其对应的 `AGENTS.md`。
