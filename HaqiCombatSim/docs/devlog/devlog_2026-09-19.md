
## 抱抱龙选择页：初次相遇

将第二步改为主角与伙伴同场景展示，沿用草稿的名字和少年/少女外观。点选抱抱龙后用约1秒的跑入与蹦跳动画来到主角侧边；切换立即替换旧伙伴，返回步骤保留选择。使用现有图集与原生CSS，无新增资源依赖。保留小字原版导入入口，减少动态效果设置下直接展示最终站位。

验证：角色创建/角色管理相关16项测试通过，生产构建通过；浏览器桌面1280×720和手机390×844核验，确认跑入时位移变化、结束后归位，以及少女外观、名字和上一步选择同步。

## 系别技能轮播与小卡牌

进入系别页自动按三个代表技能顺序播放，每个结束停留1.8秒后继续并循环；手动点卡或重播后改为单次预览。暂停及隐藏页面均不累计等待时间，离开或切系取消旧播放。技能入口改用与卡包相同的主体图集缩略卡，名称放在卡片旁边，当前播放项高亮。

验证：创建预览3项测试通过，覆盖播放完成后的间隔、暂停及取消；生产构建通过。浏览器确认第一技能结束后切到第二技能，缩略图成功加载。

## 经典魔法绘本 UI 图集与响应式接入

根据用户选定方案，用内置 image_gen 生成4×4透明素材表。四种九宫格面板与十二个图标共用512×512 WebP，总计94,572字节。补齐裁剪坐标、源图与WebP哈希，上传永久Keepwork CDN并核验实际内容及CORS。主界面保留无头像状态栏、右上功能图标和底部五个入口；新增独立商店图标。对话保留原版立绘与真实DOM文字，手机竖屏选项纵排，横屏限制高度并允许正文滚动。

验证：全量170项测试通过，构建通过；CDN与显式本地模式、1280×720、390×844、320×568、844×390实际浏览器核验。测试发现本地CSS变量中的相对背景路径按CSS目录解析，已修复为document.baseURI解析的绝对URL。新增图集预算、alpha、哈希和九宫格坐标专项测试。资源和用法详见 docs/ui-art.md，完整提示词见 docs/ui-art-prompt.md。未执行网站上线发布。

### 紧凑卡包与全系学习
- 按用户截图反馈重排 view_adventure_deck：缩小标题/工具栏，标签末尾新增“+”，横屏左右/手机上下；大卡改悬浮或点选弹层，牌库搜索、六系/已学筛选和36条分页。
- 拖出、右键、长按均逐张移出；原卡包份数与库存校验保留。学习卡牌与布局保存原子提交，未保存不影响角色。
- expansion 从已提交儿童版数据生成681条法术目录；25条未支持效果明确禁用，其余支持跨系学习，包括平衡。排除内部状态/回合指令，等级来源及网页默认规则写入adventure.md。
- 浏览器验证桌面布局、390×844竖屏、右键/拖出、平衡筛选/搜索、学习入包与大卡预览。Node回归补充悬浮/点击预览、拖出/内部松手/取消/长按、跨系学习保存及失败回滚。

## 绘本UI第二版：取消厚框与大卷角

根据用户对实际效果的反馈，去掉主菜单/工具栏的整体木框和状态栏大面板；底栏改独立圆形徽章及薄名字牌。任务面板改紧凑纸条。对话改低矮横排：正文在左、选项在右、立绘在框外、姓名牌骑边；手机采用紧凑换行按钮。面板只从原图集裁出羊皮纸中央纹理，完全不使用四个巨大卷角；不新增网络图片，仍为94,572字节图集。通用按钮保留细九宫格边。

验证：生产构建和图集专项测试通过。浏览器检查1280×720、390×844、320×568、844×390主UI/对话，实际截图保存为.asset-cache/storybook-qa/compact-*.png。桌面对话框从约238px缩到约112px，菜单去掉外包围盒，手机版对话也不再保留空白立绘区。未改战斗/存档逻辑。

## 绘本UI第三版：恢复连续背景

根据用户反馈，保留紧凑布局，但恢复左上状态栏整体背景，名字、等级、经验和血条之间不再透出地图；顶部工具添加薄背景，底部徽章下用窄底座连接。对话背景延伸至立绘下方，桌面立绘大部分落在纸面内，仅上部少量露出；手机采用侧边立绘加正文、底部完整选项行。继续使用原94,572字节图集，无大卷角。生产构建通过。

## 海岛背景缓存美化

- 新增 adventure_terrain.js，将浅水渐变、断续浪痕、湿沙/干沙、草地色块、树下暗部、道路砂砾和路边草簇一次绘入既有背景缓存。
- 道路按整张路网逐层绘制，消除后一段描边压住前段路面的圆头接缝。道路坐标、宽度定义和碰撞逻辑不变。
- 营地与小镇共享远海颜色；每帧用海色填充替代 clearRect，背景缓存四边与视口海色完全一致，镜头越过地图矩形仍为海洋。
- 未新增图片、运行时第三方库或每帧装饰循环；仍只持有当前地图的1800×1600背景缓存（RGBA约10.99MiB）。增加的是切换场景时的静态生成工作，不承诺绝对零开销。

验证：npm test 172项通过；npm run build通过。浏览器使用实际资源和createRenderer核验1280×720桌面、390×844手机画布，覆盖营地西/南岸及小镇北/东岸。两张地图所有边缘像素均与远海底色一致，同种子重复生成像素差为0；最终版本本机单次生成约16.5–20.2ms（非跨设备性能保证）。截图与临时验收页位于.asset-cache/terrain-qa/。未发布CDN网站。

## 场景滚轮与双指缩放

- 冒险场景支持滚轮、触控板滚轮事件及双指距离缩放，倍率限制为设备默认视野的75%–140%。以角色为中心，保持角色屏幕锚点稳定；HUD、弹窗、战斗及标题镜头不随手势缩放。倍率只保留在当前页面会话。
- 第二指接触即停止摇杆并清除移动意图；缩放后全部松手才能重新单指移动。处理第三指、极小间距、取消、丢失捕获、失焦和面板禁用，防止误走或倍率突跳。
- wheel监听仅绑定地图且为passive:false，归一化像素/行/页单位并限制单次增量；场景缩放复用原背景缓存，不增加图片或重新烘焙背景。更新中英文标识符之外的中文操作提示。

验证：npm test 175项通过；npm run build通过。新增/更新手势模拟测试覆盖双指/第三指、wheel单位及禁用、桌面/手机倍率上下限、角色锚点、点击坐标及缓存复用。浏览器实际滚轮检查75%与140%上限和缩小后海面衔接；双指通过PointerEvent逻辑模拟验证，未做实体触屏设备验收。临时浏览器验收页为.asset-cache/terrain-qa/zoom.html。未发布CDN网站。


### 卡包悬停预览精简
- 悬停停留350ms后仅显示完整卡面，移开、按下、失焦或滚动取消；纯图片无关闭/移出/放入按钮，pointer-events:none，不拦截鼠标。
- 预览放在图标行上方或下方；按视口剩余空间等比缩放，保持整张可见且不遮挡同一行。主动点击的预览流程保留。
- 虚拟时钟覆盖延迟、快速掠过取消及纯卡面内容；几何测试覆盖视口边缘和矮屏。浏览器确认无按钮、不拦截鼠标、位于当前行下方。npm test 176/176通过，构建通过。

## 绘本UI操作与配色调整（2026-09-19）

- 通用关闭按钮及卡牌预览关闭按钮统一52×52px点击区域；绘本关闭图标放大到46px。
- PC对话框最大宽度缩为740px；连续对白固定高度和下一步按钮坐标，长正文独立滚动。中间步骤统一“继续”，原回复保留在悬停提示，结束步骤保留接取任务/领取奖励等动作文案。
- 左上状态与右上工具统一使用完整浅羊皮纸背景及细金边，消除中间镂空与配色不一致。
- 任务标题可直接自动追踪，以感叹号表示未完成、绿色问号表示可交付；取消重复追踪按钮。
- 普通UI文字禁止拖选，图片禁止原生拖动；输入框保留编辑能力。
- 浏览器检查1280×720、390×844、844×390；实测桌面连续步骤按钮同为x838/y555/150×46，手机同为x254/y713/116×46，关闭按钮52×52，拖选结果为空。点击任务标题打开对应NPC对白。横屏长回复按钮不再出现滚动条。
- npm test：176/176通过；npm run build通过。原图集94,572字节保持不变，无新增图片。未发布网站。

## 关闭按钮再次放大
按用户要求，所有冒险UI关闭按钮在上一版基础上放大一倍：点击区域104×104px，绘本图标92×92px，文本叉号76px。对话关闭按钮移至边缘上方防止遮字；卡牌预览关闭按钮占用独立行，预览按实际高度限制在视口内。浏览器核验桌面设置关闭操作及390×844手机对话边缘，无裁切。构建通过。

## PC战斗手牌悬停波浪

- 精细鼠标且支持悬停时，卡面在200ms内抬高18px，移开后380ms柔和回落，连续划过形成波浪感；选中牌原有抬起状态保留。
- 使用内部卡面变换，外层命中区域保持稳定，避免底边反复触发；悬停牌临时置于相邻牌上方，禁用按钮不抬起。触屏不启用，减少动态效果偏好下取消过渡。
- 验证：npm test 176/176通过，npm run build通过，git diff --check通过；尚未进行浏览器动画实测。

## 对话回复单行与恢复追踪入口
恢复原对白回复文案，按整段对白的最长按钮文字预留统一宽度；按钮取消固定最大高度与内部滚动，窄屏不足时自然换行。正文与按钮分行，保持连续步骤右下角位置稳定。任务名称仍可点击，并恢复独立绿色追踪按钮，描述为按钮预留空间。浏览器核验桌面长回复单行、无按钮滚动条，独立追踪按钮可打开目标NPC。npm run build通过。

## 战斗右上角精简

- 音效改为26px小喇叭图标，透明背景，保留40px点击区域、键盘焦点和中文提示；静音时增加斜线。图集没有音效格，使用已有SVG图标，避免误用图集格。
- 移除战斗右上角云端存档入口，保留回合提示和撤退按钮。
- 验证：npm test 176/176通过，npm run build通过，git diff --check通过。未进行浏览器实测或CDN发布。

## 手机手牌横滑选牌与上拖出牌

- 新增view_adventure_hand.js：触摸卡面后按住横滑即高亮；手势期间冻结手牌布局，向上超过18px锁定当前牌，超过56px显示出牌提示，松手才提交。拖回不出牌，短按/横滑松手只选牌，弃牌按钮保留原操作。
- resolveHandSwipe复用canCast与validTargets，唯一合法目标直接提交既有playRound；多目标保留选牌并提示点选。弃牌、魔力不足、冷却、死亡、过期手牌及结束战斗不会误出牌。目标按钮仅展示合法对象，手动点击也校验目标。
- 处理pointercancel、捕获丢失、多指、失焦、页面隐藏及重绘/离开战斗的清理，拦截松手产生的合成点击。
- 验证：新增7项测试覆盖触摸横滑/上拖/拖回、取消、重复松手、合成点击、目标唯一性和不可施法情况；npm test 183/183通过，npm run build通过，git diff --check通过。浏览器连接失败（nodeRepl.fetch request failed），未完成实际手机触摸和浏览器视觉验收；未发布网站。

## 原版风格战斗队伍状态与品质条

- 敌方队伍状态集中左上，我方集中右下，按实际阵容最多展示各四名单位；每个面板包含名字、等级、数值血条、普通/超级魔力点与附加状态，主角使用浅绿金色背景及“自己”标记。
- 合法目标面板可点击施法，沿用既有目标校验；面板血条通过已有动画HP快照更新，法阵下只保留名字和血条。敌方大标题与重复目标名字按钮由队伍面板替代。
- 桌面、手机竖屏和矮横屏分别安排状态与操作区；既有手牌布局测量避开上下信息区。卡牌品质沿用variantAuras颜色，将整卡细边替换为卡底15px粗色条（302×460绘制坐标），普通卡不增加品质标记。
- 验证：原183项全量测试通过，新增3项状态/动画HP/目标点击/品质条测试通过，生产构建通过。浏览器连接仍失败（nodeRepl.fetch request failed），未完成真实浏览器多尺寸视觉验收。未实现符文卡与宠物卡新系统，未发布网站。

## 初次加载角色界面闪现

- 启动时保留加载画面，等待已有账号的静默恢复完成后再决定进入游戏、新建角色或角色列表，避免先渲染访客界面再自动跳转。
- 云端冲突或账号角色加载失败时停留在角色处理界面；单角色自动进入增加冲突保护。首次访客仍直接走原本地入口，不额外加载SDK。
- 验证：对实际boot函数执行六种模拟启动分支（访客、恢复世界、恢复战斗、无角色、云端冲突、连接失败），等待期间未调用标题渲染，全部通过；npm test 186/186通过，npm run build通过。未进行实际浏览器视觉复验，未发布网站。

## 全屏法阵与整轮隐藏手牌

- 战斗Canvas铺满战斗层，移除按顶部HUD、手牌和底部操作扣减Canvas高度的逻辑；法阵半径改按视口宽度46%/高度76%取小值，选牌与施法期间保持同一几何布局，手牌叠加在法阵上。
- 整轮animation存在时隐藏且不绘制手牌，隐藏出牌/跳过/捕获及卡包提示；队伍状态继续显示。移除提交后的飞牌前奏，直接进入施法事件队列。
- 动画期间保留未消耗手牌序号，等全部事件结束后恢复选择界面；仅新补牌播放错峰发牌动画，结束战斗不再显示手牌。回合结算、卡牌数值和事件时长不变。
- 验证：npm test 186/186通过；npm run build通过；git diff --check通过。未做浏览器/真机视觉验收，未发布网站。

## 主动选牌与中央瞄准状态

- 取消开局及每轮自动选牌。用户点选后隐藏其余牌，将当前卡移到屏幕中央；下方显示弃牌/重新选择。重新选择回到全手牌且不默认选中，弃牌沿用原待弃牌规则。
- 中央卡面不拦截法阵点击，法阵角色和左右角队伍状态继续通过相同合法目标校验施法。
- 触摸上拖进入锁牌阶段即隐藏其余手牌，回拖解除或中断手势恢复；原唯一目标自动出牌、多目标等待点选的松手规则保留。
- 扩展手势测试验证瞄准隐藏状态与恢复。npm test 186/186通过，npm run build通过。未进行浏览器/真机验收，未发布。

## 对话框关闭按钮缩小

- 对话框右上角关闭按钮由104px缩为48px，图标由92px缩为40px，并将顶部偏移调整为-32px；手机布局同步调整，保留48px点击区域。
- 验证：npm run build通过，git diff --check通过；未进行浏览器视觉复验，未发布。

## 追踪按钮文字居中

- 追踪按钮继承了原双端布局的justify-content:space-between，单段文字靠左，使最小宽度的剩余空间集中在右侧。覆盖为center，保持左右10px内边距与现有点击尺寸。
- 验证：npm run build及git diff --check通过；未进行浏览器视觉复验，未发布。

## 统一关闭按钮与对话框内侧定位

- 设置、任务、卡组、云端、宠物详情及卡牌预览的关闭控件统一为48px，图标40px、文本叉号32px；卡组标题栏也使用统一尺寸。
- 对话框关闭按钮从面板上方移至内部右上角（桌面8px、手机6px），正文和选项右侧预留44px，避免与关闭按钮重叠。
- 验证：npm run build及git diff --check通过；未进行浏览器视觉复验，未发布。

## PC右键弃牌

- 鼠标右键卡面切换弃牌/撤销弃牌，复用原discard回调；整排手牌及中央选中牌均支持。中央牌保持左键穿透选目标，仅右键在卡面范围内命中。
- 仅精细指针启用，排除触摸长按；施法动画和战斗结束时不执行。卡牌外保留浏览器菜单。
- npm run build与git diff --check通过；未做浏览器交互实测，未发布。

## 原版卡包物品规则

修复卡包误读衣物等级/学系属性，以及已学3份许可限制高级口袋同卡数量的问题。商城新增卡包分类；编辑器展示独立口袋装备、容量、配卡方案与购买入口，换装和方案原子保存。旧低等级越级口袋保留物品并卸下，进行中的旧战斗延后迁移。新增购买/装备限制、45张含同卡5张的战斗与存档、调试保留、缩容、旧检查点以及界面草稿测试。

### NPC 对话的 RPG 阅读体验

- 对话框改为独立的角色立绘、地点与姓名、正文、奖励标签和底部回应区；新增局部样式文件，保持既有其他界面改动。
- 加入逐字显示、标点停顿和入场动画；点击或空格先补全文字，再次操作执行默认回应；长按不连续推进。关闭按钮始终可用，Tab 在对话按钮间循环，减少动态效果时直接显示全文。
- 连续剧情使用当前台词对应的 NPC 姓名与立绘；关闭或换句时清理计时器和监听器。
- 验证：现有 npm test 191 项通过；新增 dialogue_ui.test.mjs 3 项通过；npm run build 通过。浏览器检查 1280×720 与 390×844 布局，确认默认空格进入 1/8 剧情，连续两次空格停留 2/8 并补全台词。
- 本次只修改对话呈现与输入，不改战斗公式；未上传 CDN。

## 施法失败明确提示
- 选牌后持续显示魔力点不足的所需/可用点数与重新选牌、跳过回合建议；点击法阵、角色或施法按钮均反馈原因，滑动施法复用相同提示。
- 冷却中显示剩余回合；复用现有魔力判定，未改变战斗结算规则。

- 验证：现有 npm test 194/194 通过；新增提示与手势定向测试 8/8 通过；控制器和视图语法检查通过。本次未做浏览器视觉验收。

## 角色脱战回血速度
- 角色自然恢复调整为每秒最大生命值的 2%，从空血最多 50 秒回满；通过 BalanceParams.adventure.heroRegenPerSecond 配置，在线与离线一致，战斗中暂停。
- 宠物仍每分钟恢复 5%，同步更新宠物面板说明。
- 验证：npm test，196 项全部通过，覆盖恢复速率、50 秒回满、生命上限与战斗暂停。
