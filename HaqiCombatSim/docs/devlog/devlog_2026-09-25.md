# 开发日志 · 2026-09-25

## 冒险坐骑：购买、任务奖励与骑乘

- 运行时目录为 data/adventure/mount-catalog.json，由 scripts/build_mount_catalog.mjs 从演示目录复制，并附上图集、原价和战斗属性。打包只保留姿势、图集、属性和售价。
- 永久且有正价的坐骑可在岛屿 NPC 直购和哈奇商城“坐骑”分类购买。奇豆优先，否则扣魔豆。限时、零价和概念图不卖。会员标记仍要有效会员。
- 任务不再把 2/6 与 10/1 奖励换成战斗伙伴。旧账本只补回坐骑物品一次，已有伙伴保留。
- 背包增加坐骑槽和筛选。骑乘写入 save.mountId，地图和战斗绘制坐骑，属性计入英雄，移动乘 mountSpeed。战斗中不能更换。

## 原模型坐骑异步加载截图与座位校准

- 原截图只等待主模型 IsLoaded，第一方向可能只包含荷叶、粒子等先加载部分。render_originals.py 改为四方向预热至少 6 秒，固定 AnimFrame=0 后每秒采样，连续 3 张像素一致才接受；超时明确失败。支持重复 --id、--force 和可调等待，读取清单明确 UTF-8，保留其他任务失败记录。
- 从原生 3D mini-scene 重绘 12 生肖、魔幻飞毯、仙兔、蓝晶神龟、尤诺的雪地车共 16 种四向 WebP。全部主体完整，透明边界无裁切；最大 170,158 字节。新文件按内容哈希命名，旧副本保留用于追溯。
- 16 张新图上传永久 Keepwork CDN，实际响应逐张验证 HTTP、CORS、SHA-256 与尺寸/字节上限；assets.json 和 native-assets.json 同步更新，未复制到额外 Git 镜像。
- pose-overrides.json 保存仙兔、神龟、飞毯的四向座位校准，分别修复长耳包围盒造成悬空、骑手陷入龟壳、脚底离开毯面。assemble_catalog.py 支持只重建指定坐骑，保留其他配置并应用显式校准。
- 批量校验 16 张图的透明通道、64 帧边界与内容哈希、70 项配置；重复组装字节一致，Python 编译检查通过。原生图集批量对照确认生肖主体恢复；本地预览及默认 CDN 页面确认牛四向、仙兔男女骑手和神龟鞍面贴合。
- npm run build 通过。npm test：463 项，461 通过；2 项仍为旧 mount_pose 测试缺少 data/mount-demo/mounts.json、quest-catalog 来源哈希不一致。未修改相关文件。
- 用户要求加快验收：后续优先批量图集对照、坐标与 alpha 边界检查，只抽查少量代表页面，避免逐只用浏览器反复校准。
- 所有改动保留工作区，未 commit 或 push。

## Paracraft 角色导出 Skill

- 根据用户要求，将实验室原生模型导出经验整理为个人 Skill `paracraft-character-export`，安装在用户 Codex skills 目录；包含原生渲染配方和 Haqi 实验室接入参考。
- 覆盖相机 yaw 与页面方向/格号映射、四向预热至少 6 秒、固定帧后三张稳定采样、复合模型加载限制、共享构图与 alpha 边界、原装扮、WebP 预算和来源清单，以及按需发布核验。明确启动脚本的进程影响及现有批量脚本的失败清单/清理限制。
- 已对照 render_originals.py、assemble_catalog.py、NPC 导出器及原生 CLI wiki，Skill quick_validate 通过。本次仅整理技能，未重新导出或上传资源；浏览器工具连接失败，未新增页面视觉验收。未 commit 或 push。

## 六种坐骑四向鞍座定位

- 按用户截图校正冲锋大象、黄金飞马、王者金龙/英雄银龙/英雄白龙/王者玉龙珍藏版的四向座位；大象侧面从头颈移到紫色坐垫，飞马和四种龙按背部鞍面定位。
- 六种坐骑各自采用统一的四向骑手缩放，去掉尾巴/翅膀包围盒引起的侧面、背面放大；侧面锚点改到骑手臀部，男女配置一起覆盖。校准保存在 pose-overrides.json 并组装进 catalog.json。
- 新增 review_poses.py：调用实际 mount_core.js 的 resolvePose 输出坐标，使用本地 WebP 批量合成四向预览及前景遮挡，无需浏览器逐项点击。六种坐骑 × 四方向 × 男女共 48 种组合已查看；本轮不重新生成或上传原画。
- 70 项目录校验通过，所有显式校准与目录一致，男女坐标有限；Python 编译和 diff 空白检查通过。未改战斗逻辑，未提交或推送。

## 宠物商城不再直接放语言挑战

- 宠物商品格和预览里不再显示「语言挑战」。语言学习开启时，点击宠物的「购买」会打开购买窗口；窗口底部是带对话图标的「跟我说话，就能免费得到我。」，点它仍进入原来的语言挑战。其他商品的语言挑战按钮保持原样。
- 英文词典对应这句为 Talk to me to get me for free.
- 隔离商店页 `?learn=1`：宠物格没有语言挑战；购买窗口底部可点进语言挑战且不扣奇豆；确认购买后奇豆 1000→860 并标记已拥有。装备分类仍保留语言挑战。390px 购买窗口无横向溢出。未写真实存档。

## 右上角身份条货币名

- 等级、奇豆、仙豆原先和数字拼成整句，英文查表对不上已有的 Level / Qi beans / Fairy beans。身份条改为单独翻译标签。魔豆（物品 984）一并显示，词典为 Magic beans。
- 等级改为元素图标旁的圆圈数字，不再写 Level。奇豆、仙豆、魔豆改用原版物品图标（0_JoyBean、17213_GodBean、984_MagicBean）打成一张 WebP 图集，已上传 Keepwork CDN。
- 商城余额和商品标价改用奇豆图标。悬停、键盘聚焦或点击图标会显示「奇豆」或「我的奇豆」；标价里的「 / 件」译为 / each。

## 冒险地形缓存细节与高清显示

- 保留既有岛屿几何、通行、道路与桥梁，增强 Canvas 地形缓存：草地苔色斑块、草叶、碎石、沙纹与雪地高光；道路独立裁切砾石，地面纹理在水面和桥梁之前绘制。
- 海岸色带改为渐进混色并加碎浪，河流中心改为柔和层叠着色，河岸补细石；海洋、河流和湖泊增加固定种子的水纹及明暗变化，冰面、熔岩和暗色水保留各自配色。火山与灰烬地表不种草。
- 纹理使用 rng_core 的世界格子种子，并扩展邻格绘制以覆盖跨瓦片笔触。普通地图仍使用 512 像素缓存，高 DPI/放大时使用 1024 像素；缓存按含出血边的 RGBA 像素限定在 64 MiB，继续每帧最多生成两块和低清概览兜底。
- 验证：地形/探索定向测试 5/5 通过；完整构建通过。完整测试 463 项中 461 项通过，另有两项非本轮地形变更导致的失败：mount_pose 缺少 data/mount-demo/mounts.json；quest-catalog 的怪物目录引用任务目录 SHA-256 与当前文件不一致。新增 DPI 回归在全量运行后单独通过。
- 浏览器已检查地形预览的金色山谷、河流、海岸、雪地与熔岩，并确认 Haqi.html 实际营地正常绘制。1024×1024 区域整图/分块像素对比：1×/2× 平均通道差 0.133/0.128（0–255），缓存命中绘制约 0.2/0.1ms；差异包含 Canvas 边缘抗锯齿，未宣称逐像素完全一致。预览工具位于忽略目录 .asset-cache/terrain-preview/。
- 无新增图片或第三方依赖，无 CDN 上传；改动仅保留工作区，未提交或推送。

## 追加九种坐骑座位校准

- 按用户截图追加蓝血凤凰/浴火凤凰珍藏版、圣诞麋鹿、英雄白龙、土遁神龟、小猛犸、白银猛犸、黄金猛犸、魔力灵狐，共九种的四向校准。
- 凤凰承重点从尾羽移回躯干；麋鹿、猛犸移离头颈并贴合背部或紫色坐垫；土遁神龟上移到真正鞍面；灵狐下移到鞍面。同步校正侧面臀部锚点和各方向骑手比例。
- 调整批量预览工具的上方留白，避免高大坐骑的骑手被验收图自身裁切。批量查看九种 × 四方向 × 男女共72个组合，70项配置及全部显式覆盖一致性检查通过，Python编译检查通过。
- 本轮仅修改位置配置和验收工具，不生成或上传图片，不提交或推送。

## 大麋鹿、金色羽翼、造梦灵狐校准

- 圣诞大麋鹿(31天)骑手移到红色鞍面，造梦灵狐承重点从尾巴附近移到背部，统一各自四向比例和臀部锚点。
- 金色精灵羽翼侧面改用原图翼根坐标与角色背部锚点，修复翅膀长在胸前；正背面按肩胛高度定位，保留背向翅膀覆盖角色的遮挡。
- 批量检查男女24个组合；70项目录校验及三项配置覆盖一致性检查通过。只更新校准和目录，未生成或上传图片，未提交或推送。

## 普通版龙凤与御海仙龟

- 追加普通版蓝血凤凰、英雄银龙、王者金龙、王者玉龙、浴火凤凰五种图集的校准；对照原画确认与珍藏版的躯干/鞍面位置兼容后独立保存覆盖，不依赖名称匹配。
- 御海仙龟的左右座位分别移到金色座椅内部，上移骑手到坐面，四向统一0.50缩放；未套用土遁神龟不同的椅位。
- 批量检查六种男女四向48个组合；70项目录及本轮覆盖一致性校验通过。未改原图、未上传、未提交或推送。

## 地图与界面英文

- 岛屿名、分区名和导览说明写入 `en.txt`。地图窗口、世界地图、营地地名、签到倒计时和若干动态短句改为先查词典再填入数字或名字。
- 静态界面扫描补上六岛地图的名称与说明，以及场景捕鱼的固定句子。
- 未提交或推送。

## 场景内捕鱼（2026-09-25）

- 移除模态捕鱼视图，新增透明场景鱼群与紧凑操作条，点击当前海面进入；原地撒网、金色节奏区收网、捕获浮字与会话连中反馈。鱼种不绑定鱼影，连中不加奖励。
- 普通网失手不扣资源；必中网与捕鱼器自动结算。未改 adventure_fishing_core 的兑换与精力规则，继续复用 persistReward。关闭、移动、面板切换、失焦/后台撤销未完成一网；副本不开放。无网与低精力均有提示，药剂与切网可用。
- 60×52鱼影热区、48px主按钮、手机原生下拉选择器；支持空格/Esc，低动态模式取消限时，不额外引入美术或第三方依赖。
- 13项捕鱼定向测试通过（原8项规则 + 5项场景交互，覆盖命中/失手/超时/取消/必中/重复按键/低精力/低动态/失败重试）；完整构建通过。
- 全量测试467项中465通过、2失败：mount_pose读取缺失的data/mount-demo/mounts.json；quest-catalog的目录来源哈希不匹配。均未修改其源数据或断言。
- 实际浏览器1280px与390×844隔离页核验：无模态、鱼群在海上、命中一网库存5→4且精力100→95、提前收网两者不变；手机必中网2→1且精力100→95，无横向溢出。仍未做真机触屏验收。未操作玩家存档、提交、推送或发布。

## 地表装饰 WebP 图集与缓存烘焙

- 使用 image_gen 生成三张原生透明 4×4 图集，共 48 格：草叶/花丛/蘑菇、石块/落叶/枯木/冰雪、芦苇/贝壳/睡莲/岸边碎石。新增 prepare_terrain_decorations.py，复用环境图集打包器；先试无损，再高质量 WebP 与等比缩放，保留 alpha、格号、源裁剪、原图 SHA-256 和成品 SHA-256。
- meadow 536×536、189070 字节（质量94）；stones 536×536、172456 字节（质量94）；shore 652×652、199748 字节（质量90），全部低于 200000 字节。成品位于 assets/adventure/environment/，清单为 data/adventure/terrain-decoration-art.json。
- 三图已按既有上传技能发布到永久 Keepwork CDN，verify_terrain_decorations.mjs 核验 HTTP、CORS 与远端逐字节内容后写入清单。用户明确选择只保留 CDN 和游戏项目副本，不复制到 digitalhuman-resource 镜像；没有发布网站本体、提交或推送。
- 新增纯展示位置模块 adventure_ground_decorations_core.js：按世界格子固定种子生成、按区域/海岸/河岸/湖面选帧，避开道路、桥梁、居民/遭遇、建筑、树根、山体、农田与中心广场。副本走廊跳过。位置不写存档、不更改碰撞、不消耗战斗随机数。
- 三图在资源阶段并行加载，完成后交给地形缓存绘制器；仅烘焙瓦片时绘制，概览和每帧精灵队列不绘制，加载失败继续使用基础地形。新增图集独立于原雪树/天气素材，失败不影响它们。
- 验证：11 项图集/展示位置/地形定向测试通过；全量 467 项中 465 项通过，另两项仍为已有 mount_pose 缺少 data/mount-demo/mounts.json、quest-catalog 依赖哈希不符。npm run build 通过，产物包含新清单、没有 WebP/PNG/OGG。
- 浏览器完成 CDN 地形预览、显式本地 WebP 预览（含雪石/冰晶）、海岸与草地实际裁切、独立 8792 构建预览角色进入营地检查。透明边缘在草地合成与实际游戏中无色框，冷瓦片完成后正常缓存。8792 只用于隔离验证，未替换既有游戏角色。

## 场景放大后的瓦片接缝

- 修复 `adventure_large_terrain.js`：绘制瓦片时使用一像素已绘制的 bleed 重叠，保留最外侧一像素作采样边界；源矩形和目标矩形同比扩展，不拉伸或移动地图内容，缓存容量不变。
- 新增 `tests/fixtures/terrain-seams.html` 浏览器像素检查：54 组缩放（含手机 0.615 与最大 1.4）、DPR 与小数相机位置全部通过；旧裁剪绘制在其中 53 组出现透明接缝，确认检查能复现原问题。
- 内置浏览器核验隔离场景预览的南岸沙滩，放大到 1.4，未见截图中的细线；不读写玩家存档。
- `npm run build` 通过；`npm test` 共 473 项，471 通过，2 项失败与本次瓦片绘制无关：`mount_pose.test.mjs` 缺少 `data/mount-demo/mounts.json`；`quest-catalog.test.mjs` 的任务目录 SHA-256 与怪物目录记录不一致。
- 保留工作区已有修改，未提交、未推送。

## 青龙对话的英文

- 导师自我介绍已经走翻译，但 15 句营地居民介绍不在 `en.txt`。任务奖励芯片原先把「名字 ×数量」整句去查表，物品名对不上 `{name} × {count}`。对话和任务日志改为先翻译物品名或宠物名，再填入数量。
- 词典补上居民介绍，以及本章任务奖励名（战斗经验、初级五系帽袍靴、出奇蛋、战宠口粮等）。英文下青龙介绍为 I am Azure Dragon, the mentor who stays with you as you grow.；奖励为 Combat EXP × 73、Beginner Fire Mage Hat × 1。
- 浏览器用实际 `rewardChip` 和词典渲染了对话条。未改玩家存档，未提交或推送。

## 任务日志英文与分页

- 接取条件「请先完成前置任务」和奖励分组「固定奖励 / 可选奖励 · 按学系选择」改为按短句查表，不再把拼好的整句当键。奖励名继续走 `{name} × {count}`，并补上假日黄金飞马。
- 任务列表分页改成三列：页码不换行，上一页/下一页的英文可以在按钮内换行，避免把 `1 / 1` 挤成两行。
- 未改玩家存档，未提交或推送。

## 木质场景路牌

- 路牌改为略倾斜的缺角木板，加入固定木纹、木结、钉头、板厚、立柱明暗和贴地投影；取消圆角 GUI 边框。中文与翻译后地名自动适配板宽，最长限制 180 世界像素。
- 新增 adventure_signposts.js，按翻译后的文字缓存完整路牌（含文字和阴影），每个 renderer 最多 128 张、固定 3 倍分辨率；正常帧每个可见路牌只需一次 drawImage，无新增下载、滤镜或每帧纹理生成，沿用对象裁剪和深度排序。
- 浏览器核验金色山谷实际场景及 tests/fixtures/signposts.html 中中文、英文、长地名在 1.4 和 0.82 缩放下的文字与透明边缘。场景预览曾报告约 60 FPS，未做正式前后基准或真机性能测试。
- npm run build 通过；npm test 共 476 项，473 通过，3 项失败位于本次未改的地形 farmland 测试、缺少 data/mount-demo/mounts.json、任务目录依赖哈希。未改引擎、存档或已有其他工作区修改；未提交、推送或发布。

## 海岸、道路与河岸烘焙边缘

- `adventure_large_terrain.js` 将海面浅水色带细分为约两世界像素一级；海岸内侧增加沙地渐隐边缘，宽虚线浪带改为窄的断续浪花。道路弱化深绿色描边，并按整个路网逐层混色，保留路口衔接。河流的岸土、浅水、中心水色连续过渡，沿用水、冰、熔岩和幽暗水域各自配色。
- `adventure_terrain_detail.js` 增加固定世界格子采样的细碎沙边和路肩，河岸石子分布范围稍作变化。全部只在瓦片烘焙时绘制，不新增每帧计算、依赖或资源下载；地图坐标、碰撞、桥梁和存档不变。
- 新增 `tests/fixtures/terrain-edges.html`：隔离地形预览，可选岛屿、海岸和桥口，支持 `?island=ice` / `?view=shore`，显示重复烘焙一致性、耗时及整幅/瓦片像素差异。浏览器检查哈奇岛海岸与桥口、寒冰岛、火鸟岛；重复烘焙逐像素一致，常规整幅烘焙观察约 32–37ms（1000×720，本机单次观测，不作性能保证）。原有整幅/瓦片纹理采样差异仍在：同一哈奇岛桥口旧版超过3色阶的像素14292个，新版13355个，未宣称逐像素相同。
- 14 项地图/地形定向测试通过；现有瓦片接缝页面的54组缩放/DPR/小数坐标检查全部通过。最终 `npm run build` 通过。全量476项中474通过，2项为已有问题：`mount_pose.test.mjs` 缺少演示坐骑数据；`quest-catalog.test.mjs` 目录依赖哈希不一致。
- 保留其他正在进行的工作区修改，未提交、未推送、未发布。

### 路牌文字自适应与双行缓存

- 标签先按实际字体测量，短地名单行、长地名优先按单词分成均衡双行；中文及连续长词按字符分行。双行木板增加高度，超长文字等比缩小，保留完整内容。
- 按字形实际左右/上下边界留出内边距与钉头空间，文字及雕刻高光限制在木板内部；设置内部裁剪作为额外防溢出保护。文字布局、缩放、板形仍只在生成缓存时计算，每帧保持一次 drawImage。
- 浏览器 signposts.html 检查6种中英文地名（含超长中文、英文短语、连续长词），全部文字与高光边界通过，1.4/0.82 缩放视觉检查通过；npm run build 通过。本轮未重复全量引擎测试，未提交或推送。

## 场景鼠标松开后寻路

- 鼠标按下只记录手势并捕获指针；短点击松开后才拾取目标、寻路或打开海面交互。居民、敌人等目标也使用相同规则，避免按下时抢先寻路或交互。
- 沿用按住180毫秒或拖动超过6像素的跟随操作；长按、拖动、取消及丢失捕获后不触发点击，松开停止跟随。触屏与笔输入仍走已有处理。
- 实际鼠标事件处理器的隔离行为检查通过，覆盖按下不寻路、松开单次点击、拖动、长按、取消、丢失捕获、弹窗与非鼠标输入；本轮未做浏览器实机交互验证。npm run build通过；npm test共476项，474通过，2项为此前已记录的坐骑演示数据缺失与目录依赖哈希不一致。
- 保留已有工作区修改，未提交或推送。

## 营地教学对白精简

- 按任务编号新增独立文案配置，14个教学任务、8次居民与五系导师交谈由90段压缩到37段，简介和回复一并改写。只保留当前目标、必要操作及人物口吻。
- 章节导出应用同一配置，保留原始对白审计；任务手记同步教学简介，居民交谈结束按钮读取简短回复。目标、奖励、顺序与存档结构保持不变。
- 对照改动前快照验证任务机制完全一致；从原XML执行导出器的任务提取和改编段，输出与章节快照一致。完整导出仍被本机 assets_manifest.txt 中空文件大小字段阻断，本次未改动资源清单解析。
- 43项相关测试通过，含五系完整14任务链。构建通过。桌面与390px隔离预览逐段检查37段对白，手机无横向溢出、按钮未越界；预览不读写真实存档。
- 全量测试475/477通过，另有2项既有问题：mount_pose测试缺少 data/mount-demo/mounts.json，目录依赖测试的 monster-catalog.questCatalogSha256 与原始任务目录不一致；两份目录本次均未修改。未提交、推送或发布。

## 卡包、技能名与居民简介英文

- 卡包计数、本系页签、牌库页码、训练点、装备附卡和搜索框改为带占位的词典句，不再把拼好的中文整句拿去查找。卡面伤害与治疗、训练点花费同样拆开。文字卡面的魔力与冷却说明一并走词典。
- 导师课程名、其余卡牌显示名，以及 npc-catalog 里尚未翻译的居民简介写入 en.txt。对话里看到的是简介，不是未上屏的闲话语音稿。扫描清单开始核对这些简介。
- 隔离卡包页确认：59/60 cards · max 3 each、Fire · yours、Fire God's Wrath、Damage 176、Lava Bomb、Attached cards 3 · no deck slot、Search cards。未写真实存档。locale 相关 8 项测试通过。未提交或推送。

## 战斗奖励与卡包剩余英文

- 地图奖励条和飘字、胜利结算、战斗卡包剩余改为带数字的词典句。奇豆和抱抱龙名字随词条翻译。
- 对照词典：EXP +2、Qi beans × 50、Gained 2 EXP · 50 Qi beans、61 cards left in deck、Little Green Cuddle Dragon。未写真实存档，未提交或推送。
- 新装备弹窗的标题、名称、提示和按钮改为走词典。隔离确认：New equipment obtained、Beginner Fire Mage Hat × 1、Added to your bag、Wear now、Later。未写真实存档。

## 商城坐骑页英文

- 商城标题、等级、搜索框、筛选、价格、会员标记、坐骑说明和分页改为词典句。25 种在售坐骑名称写入英文，包括梦幻南瓜车、魔法师扫帚和荣誉龙。
- 隔离坐骑页确认 Haqi Mall、Supplies、deluca · level 50、Enter item name、All items、All levels、10 magic beans、Members only · Level 1 · All schools、2 / 3 · 25 items。未写真实存档，未提交或推送。

## 宠物商城价格行与魔法星英文

- 宠物学系页签和宠物名走词典。预览价「140 / each」禁止换行，斜线不再掉到下一行。
- 魔法星等级、有效期、剩余天数、兑换说明、五把左手法杖和属性表等级改为带占位的词典句。
- 对照：Storm、Purple Avan、Green Cuddle Dragon、Magic Star level 2、Energy left: 45 days、Valid until 2026/11/9、Flame Wand。未写真实存档，未提交或推送。


## 首页语言优先与 Loading 界面

- Haqi.html 改用轻量 adventure_boot.js：仅读取上次账号的活动角色（或访客/旧存档）语言字段，先加载对应词典，再显示标识及加载文案，最后动态导入游戏控制器。初始卡片 hidden 且文字为空，避免中文占位闪现；完整存档校验仍由原流程负责。
- 加载页采用深青渐变、金色几何标识和本地化游戏名称、细进度条；支持窄屏宽度及减少动态效果偏好。不新增美术依赖。补齐英文阶段文案，下载大小保留数值与 MB。
- 12项启动语言/locale定向测试通过；全量481项中479项通过，另两项为已记录的mount-demo/mounts.json缺失与任务目录哈希不一致。完整构建通过。浏览器工具连接失败，本轮未完成桌面/390px视觉验收。未提交、推送或发布。

## 商城确认购买与双货币

- 购买不再直接扣费。普通商品先打开确认窗口，点「确认」才购买；关闭窗口不扣费。
- 左上角同时显示奇豆和魔豆，沿用原版两种豆子图标。
- 标价超过 10000 魔豆的坐骑在货架、预览和物品详情里显示「非卖品」。点购买仍打开详情，详情写明非卖品和实际花费，确认后仍可购买。恰好 10000 魔豆继续显示原价。
- 隔离页验收：荣誉蓝血凤凰详情为非卖品，非会员确认不扣豆；魔力灵狐确认后魔豆 2,000,000 减到 1,000,001 并标记已拥有；梦幻南瓜车关闭确认不扣费，再次确认后减 1180。390 宽无横向溢出。未写真实存档，未提交或推送。

## 钓鱼交互重做：围绕主角抛竿与收杆

- 用户指出上一版的鱼群和底部操作条分散注意力，要求参考星之卡比的持竿、提竿和获鱼表现。重做为cast/wait/bite/reel/show流程，取消底部条和节奏计量器；点击水面直接抛竿，等待咬钩后一次点击提竿。
- 真实角色临时站立朝向水面，抛竿倾身、提竿后仰与小幅庆祝；绘制弯曲鱼竿/卷线轮、鱼线、浮漂、涟漪与水花。奖励物品沿抛物线跃出水面、拉至头顶，复用实际鱼类图，展示名称数量。无新美术下载，无新物品或经济数值。
- 增加近岸落点几何和走到岸边的控制器接线，避免跨地图长线；主角附近按钮68px、浮漂64px，鼠标、触控和空格共用状态机。原渔网/精力/奖励规则不变，原道具在脚边渔具面板选择。低动态不限制反应时长，关闭/移动/后台取消待结算动作。
- 浏览器验收发现全局button:active的transform覆盖定位，已用独立translate定位修复；动画不再缩放点击区域。测试覆盖等待/提前提竿/咬钩/超时/退出/必中/低动态/库存不足/保存失败/岸边选点。
- 18项定向测试通过；完整构建通过。全量483项中481通过，仍为已有2项失败：mount_pose缺少data/mount-demo/mounts.json；quest-catalog的来源哈希不一致。没有修改这两项数据或断言。
- 1280px桌面及390×844浏览器隔离验收实际完成抛竿→咬钩→点击提竿→头顶展示，普通网5→4、精力100→95，闪闪皇冠鱼真实图可见；药剂和必中网自动收杆验证通过；390px无横向溢出、无dialog/modal、无控制台错误。未做真机触屏测试，未使用真实角色存档，未提交/推送/发布。
- 补充844×390横屏检查：压缩获鱼展示高度，渔具改放主角侧边，避免底部裁切或遮挡提竿/退出按钮。

### 多轮提竿、鱼重和 workspace 个人榜

- 3–5 次提竿与随机松线间隔，递减反应窗口；防重复输入的短宽限，乱按/超时逃鱼；局部竿身/角色抖动与浏览器可选震动。
- 每条渔获独立生成克数，各鱼种前十、新纪录提示及主角附近可切换榜单，横竖屏适配。
- 奖励和纪录共同原子持久化；旧存档兼容、导入校验、角色/检查点云存档保留。HaqiAdventure/fishing/records.json 镜像经远端核验，与现有角色冲突恢复配合。
- 独立 BalanceParams.fishing 不进入旧战斗 adventure 参数；独立 RNG 不改变原奖励随机流。
- 已用隔离浏览器完成多轮提竿、超时逃鱼、实际 5.363 千克皇冠鱼新纪录和鱼种榜展示；390×844、844×390 与桌面检查通过。真实账号远端写入与真机触觉未执行，SDK 路径使用模拟适配器测试。

### 四方向提竿、镜头拉近与恢复

- 使用角色四周的四个方向按钮代替单按钮，种子化方向提示避免连续重复；增加方向键/WASD，禁止水面或空格绕过挑战。原渔获与重量结算不变。
- Renderer 通过纯 camera zoom 状态管理临时最近距离，scene activeChanged 回调统一进入/退出；保存探索值并锁住钓鱼期间的缩放，不污染原设置。进入和退出用 620ms 缓动推近/拉远，减少动态效果时仍立即切换。
- 浏览器验证桌面完整点击流程、手机竖屏错方向逃鱼、横屏方向键完整流程，以及退出后镜头复原。14 项钓鱼 UI/镜头专项测试通过；全套 500 项中 498 通过，原资源缺失和任务哈希两项失败未改动；构建通过。

## 营地南侧角色间距

- 调整地图配置中亚尔弗列得至 (1450,1040)、霸王虎坐骑至 (1310,1110)、魔狼坐骑至 (820,1370)，避开死亡斥候、船长与营地传送门路牌；同步生成 camp.json，交互和地图标记复用新坐标。
- 实际加载居民目录核验三处位置均可通行、从出生点可达；与邻近居民、敌人、路牌保留至少横向130或纵向135的间隔。地图/NPC定向测试16项通过，npm run build通过。本轮未做浏览器视觉验收。
- 全量测试首轮493/500通过；复查487/500通过，失败涉及装备、任务、奖励、坐骑姿态、资源目录与捕鱼，未修改这些正在变化的工作区内容。
- generate:maps已写出地图，随后被章节worldMapIndex非末尾的既有约束中止；check:maps确认六岛配置与快照一致，未重排章节文件。未提交或推送。

### 降低钓鱼失败压力、大鱼放大

- 错误/超时继续下一轮，至少中一次即可获得渔获；命中比例与松线失误只影响重量范围。增加严格成绩校验，旧调用兼容。自动捕鱼器场景模式总空手率降至低于 1%，原成功分支数量与精力保持。
- 按克数连续缩放实际道具图标，6kg 大鱼/12kg 巨物加入出水双波纹、光环、金色粒子与提示；多鱼显示最重那条图标，减少动态效果下不播放粒子运动。
- 浏览器验证仅中一次仍获得 1.242kg 皇冠鱼，全中获得 10.685kg 泥鳅并实际放大和显示大鱼效果。新增核心/渲染测试验证质量与重量单调、稀有自动空手、非法成绩不扣物品、实际 draw 尺寸和横竖屏边界。

## 获得提示按堆叠显示

- 同一时刻已经在队列里、或当前提示还在显示时新到的物品，按种类合并数量，叠成一组绿条同时出现，共用约 4.8 秒，不再一件一条各占数秒。超过 8 种时其余仍写“另获”。装备、卡牌和宠物的操作弹窗仍逐件处理。
- 奖励反馈测试覆盖合并数量、显示期间并入、以及多件时仍保留穿上提示。隔离页一次获得多件：两条水母合成 ×2，其余六种同时叠在同一组绿条里。窄屏 390 宽下整组不超出画面。升级仍同时显示经验、装备和卡牌，穿上弹窗保留。未提交或推送。

## 装备商城与物品详情英文

- 装备页签衣服、鞋子、首饰、背部，以及物品详情里的拥有数量、部位等级、属性、时效和获取途径改为词典句。属性名仍由核心返回中文，界面再翻译。
- 商城装备名和物品说明补进 en.txt。首屏为 Dark Lolita Dress、Dark Lolita Shoes、Clothes、Shoes、Owned: 0、Rainbow outfit · level 1 · all schools、Power pip rate +2%、All-school Defense +4%、Duration: permanent、Source: Haqi Mall。未写真实存档，未提交或推送。

## 角色名五连击调试入口与货币一键充值

- 左上角角色名成为隐藏调试入口：单独点击（600ms 静默后）仍打开「角色与装备」面板；600ms 内连续点击 5 次直接打开「属性编辑器」调试弹窗。计数与超时逻辑在 js/view_adventure.js renderHud。
- 调试面板工具栏下新增一键充值区：奇豆（100）、魔豆（984）、仙豆（17213）三个「充值到1000」按钮，取 max(当前值,1000) 写入输入框并进入预览，走既有的 prepareDebugEdit 校验、保存备份与 HUD 刷新链路；样式为 css/adventure.css 的 .debug-topup。
- 未改动战斗公式、随机数与 BalanceParams；改动保留工作区，未 commit。
- 浏览器端到端验证 9/9 通过（含单独点击行为、五连击入口、三个充值按钮、预览、保存后 HUD 与 localStorage 存档核验），详见 docs/qa-report.md 当日条目。

## 底部铭牌等高与右上角图标间距

- 底部菜单和右上角快捷入口的木纹铭牌改为固定高度，文字在框内垂直居中。带下伸笔画的英文不再把某一块铭牌撑高一两个像素。
- 右上角四个图标按文字宽度排列，桌面横向留出 12 像素，窄屏两列留出 10 像素，避免英文 Check in、World map、Dungeons、Settings 贴在一起。
- 验证：同一套界面样式下，五枚底部铭牌高度均为 20 像素且顶边对齐；右上角按钮间距为 12 像素，英文标签不相交。中文签到、世界地图、副本、设置仍保持同一高度和间距。未提交或推送。

## 多语言冒险学习：魔法营地

- 主计划落地 `docs/language-adventure-plan.md`，总体计划与语言学习/本地化文档同步。20组意图、中英各120条主表达及允许变体，27个真实NPC与营地地点、13类常用词映射；课程与游戏触发分离。
- 新增纯核心模板/规范化/证据/账本、浏览器会话与语音适配、抱抱龙课程窗口。界面语言独立，缺目标语言内容不回退领奖。事件90秒/来源5分钟冷却，学习RNG隔离。
- 基础ASR两句完成后可开最多8轮剧情挑战，模型提供逐字证据，程序评分。奇豆/仙豆北京时间每日限额与跨语言同课共享次数，货币账本原子本地保存，旧档空记录。新入口移除免费购物和语言直接判胜，旧战斗重演保留。
- 参考本机 `apps/official/apps/tools/HelloLearner/js/speech.js` 的SpeechRTC MP3合成/实际播放等待及取消，核对其AIChat宿主桥；本游戏ASR直接使用已核验Keepwork接口，无需宿主。默认不保存录音，退出/切角色/语言使异步结果失效。
- 聚焦25项通过；完整回归剩两项开始前已有失败：mount_pose缺测试资源、quest-catalog源哈希不一致。生产构建通过；浏览器隔离页验证中英、基础奖励、挑战入口及390px；线上SDK只做接口可用检查，未录真实音频。
- 待真人桌面/手机中英麦克风、发音和模型挑战验收；另补无目标语字幕听力含义选择：播放结束才答题，通过单独记录、不发奖励。听力/朗读/挑战均不直接表示掌握。未提交、推送或发布。

## 居民坐骑店英文与可见中文检查

- 居民商店的价格、已拥有和钱包按「数量 + 货币」分开翻译。分类和身份补进词典，霸王虎坐骑页为 Tiger mount、Short-term mounts、Long-term mounts、350 Magic beans、Owned: 0、Qi beans 152。
- 本地化技能加入 scripts/verify_en_locale.mjs，用来走一遍英文界面并列出仍可见的中文。扫描清单增加居民身份和商店分类。未写真实存档，未提交或推送。

## 伙伴收藏计数英文

- 我的收藏、伙伴人数与营养餐、等级和卡位改为带占位的词典句。一位伙伴用单数。
- 对照：My collection 1 / 359、1 companion · nutrition meals 0、Level 1 · slot 2。未写真实存档，未提交或推送。

## 充值按钮改为点击立即生效

- 一键充值原先只填表单，玩家点完看不到 HUD 变化，需再点「保存并生效」；改为按钮点击后当目标值高于当前余额时直接 cb.applyDebug，走同一条校验/备份/刷新链路。
- 语义保持「补到至少 1000」：已超过 1000 的币种（如仙豆 1,960）点击后不降值也不报错。手动改字段仍走底部「保存并生效」。
- 浏览器复现玩家余额 152/1,960/460 后逐个点击验证 8/8 通过，QA 报告已追加；改动保留工作区未 commit。

### 方向拉竿反馈

新增共享方向视觉脉冲，按钮箭头、角色姿态、鱼竿端点和浮漂共用同一方向/时间；380ms 回弹，点击区域稳定，关闭钓鱼时清理。18 项 UI/动作/绘制专项测试通过，覆盖四向端点偏移、回弹、错误方向反馈及减少动态效果。构建通过；全套 528 项中 526 通过，仅余原资源缺失/任务哈希两项。

### 钓鱼结算文字重叠修复

场景钓鱼成功不再同时触发通用奖励横幅、获得物品 Canvas 标题和捕获 toast，只由鱼重/纪录卡片展示。原本排队的其他奖励在钓鱼期间暂停、退出恢复，进入时清理旧 toast；新错误提示改放底部。重量卡片增加深色底与自动边界留白，矮屏再次抛竿按钮下移避免撞到卡片。456×484 浏览器实测可完整读取“闪闪皇冠鱼 8.523 千克 / 个人新纪录”。

### 装备槽位已装备时不再显示部位名

- `js/view_adventure_equipment.js`：已装备（含骑乘中的坐骑）的部位只画物品图标，不再输出 `equipment-slot-name`；空部位继续保留部位名与 `＋` 占位，`aria-label` 仍包含部位名与物品名。`css/adventure.css` 的 `.equipment-modal .equipment-slot` 补 `justify-content:center`，让只剩图标的格子垂直居中。
- 原因：部位名是英文长词时（炫彩头饰→Rainbow headwear 等）在 66px 高的格子里换成两行，把图标挤到格子下沿并压到下一行的文字，用户截图指出已装备的格子不需要这段文字。
- 新增隔离夹具 `tests/fixtures/equipment-slots.html`：真实 `renderPanel(root,'equipment')`、内存角色、`?assets=local`、`?lang=en`、`?empty=<slot>` 可按需留空槽位，不读写玩家存档。
- 新增项目技能 `.cursor/skills/browser-fixture-check/`（shot.mjs / probe.mjs）：用本仓库 `ws` 依赖走 CDP 截图与量测，不需要 Playwright。1280 与 390×844 中英各一张，量测确认装备槽无部位名、空槽保留部位名、18 个格子高度均为 66px。
- `npm test` 528 项中 525 通过，除既有 mount_pose 缺资源与任务目录哈希两项外，`tests/release.test.mjs` 因沙箱内 `spawnSync` 报 `EBUSY` 失败（环境问题，与本轮改动无关，脚本未涉及本文件）。`npm run build` 通过。未提交、未推送、未发布。

### HUD 经验条显示本级进度（含等级上限）

- 用户截图（英文界面、50 级、生命 `2180 / 2180`）指出经验条只有一种颜色：导入的满级角色 `save.xp` 恰好等于 `xpThresholds[49]`，而 `renderHud` 在 `save.level >= levelCap` 时把填充硬写成 100%，等于把右侧轨道整段盖住，看不出还剩多少。
- `js/view_adventure.js`：填充改为按原版 `EXPArea.lua EXPArea.UpdateUI` L86-94 的语义取 `min(cur,max)/max`，`cur` 为本级内经验、`max` 为下一级所需经验，`Math.min(1,…)` 对应原版 L88 的 `cur_value = math.min(cur_value,max_value)`；等级上限分支不再写死 100%。
- `js/adventure_expansion_core.js`：经验曲线多扩一段（`length <= levelCap`），让上限那一级也有"下一级"目标（50 级区间 = 15000）。上限等级本身不变（`syncProgression` 仍 `Math.min(levelCap,…)`），50 级角色照旧封顶。
- `css/adventure.css`：`.xp-bar i` 增加 `min-width:3px`，对应原版 L93 的 `width = math.max(3,width)`；本级刚满时仍能看到填充色与轨道色的分界。
- 新增隔离夹具 `tests/fixtures/exp-bar.html`：真实 `renderHud`、内存角色、`?assets=local&lang=en`、`?level=<n>&fill=<0-100>`，不读写玩家存档；量测结果写入 `document.documentElement.dataset`（level/xp/previous/next/wanted/measured）。
- 实测（`.cursor/skills/browser-fixture-check/probe.mjs`，真实 `renderHud`，条宽 208px）：`?fill=0/10/40/100` 填充为 3 / 20.80 / 83.19 / 208 px，轨道色 `rgba(75,67,39,0.61)`；`?level=5&fill=40` 仍为 83.42px，说明普通等级行为未变；`?fill=10` 时 `measured` 与期望同为 10.00%。截图 `.asset-cache/storybook-qa/exp-bar-after-en.png`（英文 900×400）与放大图 `exp-bar-after-zoom.png` 可见绿色进度 + 深色轨道两色。
- 该夹具里 50 级裸装生命为 1500（原版 kids 火系公式），截图里的 2180 是 1500 加上装备附加；本夹具不穿戴装备，因此生命显示不同属预期。
- `npm test` 528 项中 525 通过，失败与上一节相同（`mount_pose` 缺资源、任务目录哈希、沙箱内 `release.test.mjs` 的 `spawnSync`），本轮改动未新增失败。未提交、未推送、未发布。


## 2026-09-25：四岛任务怪物补接

根因：全岛任务已安装，但原版野外怪物目录没有进入 content.encounters；任务导航只在已加载遭遇中检索并统一提示副本。

新增 scripts/package_island_encounters.mjs，读取已归档 monster-catalog、quest-runtime 与章节卡牌编号映射，生成 island-encounters.json。火鸟岛29、寒冰岛31、沙漠岛33、幽暗岛21，共114处；当前101处可挑战，13处因原版卡牌缺失或目标规则不支持明确标为待迁移。没有改变原版属性、AI牌组、经验、金币或任务概率。

每个模板单独设置可重复挑战的单怪遭遇，按原世界放置，再补齐该岛任务分类所需的子场景目标；这属于Web二维单人改编，不复刻原法阵阵容、剧情脚本或原坐标。导出时选取道路可行走点，避开居民、地标与旅行入口，并验证寻路可达。任务追踪优先当前岛、任务所属岛，再考虑副本；跨岛仍沿用旅行等级提示。胜利使用已有按模板路径的击杀/掉落结算，不写副本通关记录，支持重复挑战。

全部美术复用现有 monster-art.json 中的原Boss和宠物WebP/CDN绑定，不新增图片。浏览器加载时安装，生产数据包自动包含；构建检查快照与源数据一致，export:quests/export:adventure同步生成。预览页 tests/fixtures/island-encounters.html 不读写玩家存档。

验证：44项定向回归通过，包括四岛任务来源覆盖、导航、可达性、WebP映射、战斗首回合重演、胜利计数和重复挑战；胜利记账测试使用受控结束状态，不代表一级角色能击败高等级怪物。完整构建通过，生产包含114处遭遇。全量531项中529通过，2项已有无关失败为缺少data/mount-demo/mounts.json、monster-catalog的questCatalogSha256过期。未修改这些无关数据，也未提交、推送或发布。

浏览器补验：四岛现有CDN WebP全部114/114解码并可被Canvas读取，检查火鸟岛和幽暗岛实际场景画面。使用隔离测试角色，不读写真实存档；未做本轮手机交互及真实任务交付。

## 2026-09-25：缺失怪物卡牌采用现有相近直伤牌

按用户授权，四岛单怪遭遇不恢复已废弃旧卡数据，也不生成卡面；config/monster-card-replacements.json 显式记录6项替换。火系旧单攻Level1/3分别使用宠物火岩怪1费130、3费370直伤牌，Level5/6使用5费600直伤VIP牌，群攻Level4使用4费305–345直伤Binding牌，物品22226使用同为0费100命中80伤害的Ice_SingleAttack_Level0_80_adv。旧kids火系数值缺失，以上是用户授权的Web近似，不宣称精确复原；保留单体/群体直伤，不替换成DOT，也不使用Crazy高伤牌。

替换覆盖pool、sequence、gene和cardset，保留权重、回合、原模板路径及理由。改编怪物使用独立ID，避免按需加载原副本时覆盖；原目录、玩家卡牌和副本规则不改。原本被阻断的8处遭遇开放，四岛109/114可挑战；旧寒冰吸收与现有群体按魔力吸收差异较大，未替换。冰封及noshield仍未支持，共5处保留明确阻断。

验证：46项定向回归通过，包括全部AI容器替换、原数据不变、特效完整性、8处新开放遭遇回合重演及原副本模板加载隔离。复用现有永久CDN技能资源，无新增美术。未提交或发布。
本轮完整 npm run build 通过。

## 2026-09-25：剩余怪物规则按用户授权近似替换

用户明确同意强行替换剩余近似技能：Ice_Absorb_LevelX → Life_Absorb_Level3（单体600吸收，0费100命中、冷却3）；Ice_Rune_SingleFreeze → Ice_SingleStun（寒冰单体眩晕，0费70命中、冷却4，不保留受伤解冻、抗冰封或附加盾）；noshield → threat_highest（忽略护盾，选最高仇恨）。这是Web改编，不是原版规则迁移。复用现有技能特效，不新增美术。

适配器同时处理sequence和gene目标并记录targetReplacements。仅目标规则变化的怪物也使用独立岛屿模板ID，保持原source用于任务计数，防止副本模板覆盖。重新生成四岛数据后114/114遭遇无阻断；原副本目录保持原规则。

验证：46项定向回归通过，包含13处改编遭遇战斗重演和副本模板隔离；追加目标替换断言后5项岛屿测试再次通过。完整构建通过。未提交、推送或发布。

## 2026-09-25：骑乘时玩家名牌移到坐骑底部下方

- adventure_renderer.js 的 avatar 现在返回绘制内容的底部 y，场景绘制据此把英雄名牌放在底部下方 21 像素，替代原先骑乘时硬编码的 o.y-48（骑手胸前）。
- 底部取坐骑与骑手的较低者。步行坐骑（lift==0）用 ground 线（脚底）作为坐骑底部，因为 sprite 中 ground 线以下是透明留白；飘浮坐骑（lift>0，如飞毯、扫帚）的平台/主体延伸到 ground 线以下，改用 sprite 底部。骑手始终在上方，底部更高。
- 效果：骑乘时名牌显示在坐骑（而非角色）的最低点下方，与非骑乘时名牌位于角色脚下的布局一致。钓鱼姿态因临时下坐骑且带旋转/平移，仍沿用 o.y+21。
- 修正：最初误把 sprite 包围盒底部当作坐骑底部，导致步行坐骑名牌掉到下方透明留白里过远；改用 ground 线后回到坐骑脚下。
- 渲染器相关测试通过。全量测试中 2 项已有无关失败（缺少 data/mount-demo/mounts.json、quest-catalog 来源哈希过期），未修改相关文件。未提交、推送或发布。

## 2026-09-25：树木与建筑柔和烘焙阴影

新增 adventure_shadows.js 共享地面渐变：树冠/建筑宽阴影加根部/基座接触阴影，边缘透明渐隐。大地图在道路、桥梁和广场之后烘焙到现有地形瓦片及低清总览，旧小地图在静态底图完成后烘焙。移除树木原有硬边烘焙椭圆和树/建筑逐帧重复阴影。角色、NPC、怪物与宠物改用相同柔和渐变并随位置移动，避免怪物与宠物兜底重复绘制阴影。无需新图片或CDN资源，未改变数值、存档和缓存内存上限。

验证：完整构建通过；533项测试531通过，2项失败分别为缺少 data/mount-demo/mounts.json、quest-catalog依赖哈希不一致，均不涉及本次阴影文件。浏览器在CDN模式核验火鸟岛角色/树木及哈奇岛树木/建筑；新增 tests/fixtures/soft-shadows.html 内存预览，不读写玩家存档。大树与建筑跨512px瓦片的烘焙对照最大通道差为3/255，无硬切边。保留已有工作区修改，未提交、推送或发布。

## 跟随宠物消失恢复

- 跟随宠物与坐骑继续分别读取 formation/heroSlot 和 mountId；骑乘不替换、隐藏或修改跟随宠物。
- 修复场景宠物图集延迟加载/失败时忽略 drawPet=false、只留下阴影的问题：未就绪时显示已有通用宠物图，成功加载后自动恢复真实外观。
- 跟随宠物被阻挡或路径搜索延后、与角色距离超过360时，重新放到角色附近；原逻辑只检测角色单帧瞬移，无法恢复逐渐掉队的宠物。仅修改表现位置，不改存档、战斗或编队。
- 验证：8项定向测试通过，覆盖加载失败/恢复、骑乘/下骑、1280/390画布调用和掉队恢复；npm run build通过。全量535项中533通过，另有mount_pose所需data/mount-demo/mounts.json缺失及quest-catalog依赖哈希不一致2项失败。小绿/小紫/小橙图集CDN HEAD均200。
- 浏览器连接返回 nodeRepl.fetch request failed，未能读取用户当前游戏或完成实屏验收；用户确认小绿仍在宠物中，本次不能断定其当时具体触发条件。未读写真实存档、未提交或发布。

## 宠物窗口增加坐骑页签

- 收藏区改为共享样式的“跟随宠物 / 坐骑”页签，每次打开宠物窗口默认跟随宠物；页内操作刷新保留当前页签。两类搜索分别保存，支持空列表提示。
- 坐骑页仅展示库存中已拥有且已接入坐骑目录的物品。点击卡片立即调用原有 ride / dismount 动作，允许直接换骑；标识当前骑乘项，战斗时禁用。操作继续经过控制器持久化，主角与跟随宠物编队不变，刷新后键盘焦点保留在同一坐骑卡片。
- 新增英文文案和 tests/fixtures/pet-mounts.html 内存验收页，支持 tab=mount、empty、battle、lang=en、assets=local；不接触真实存档。
- 验证：宠物页交互、坐骑和语言共10项定向测试通过，包含默认页签、仅显示已拥有坐骑、骑上/换骑/下骑、存档重载、编队不变、战斗禁用、独立搜索与空列表。npm run build 和 git diff --check 通过。
- 浏览器连接重试仍报 nodeRepl.fetch request failed，桌面/390px实屏、真实鼠标和键盘视觉验收未完成；未宣称通过。未提交或发布。
- 用户补充截图显示主角在卡位1、小绿在卡位2；已告知按现有规则需把小绿拖到主角同格才会场景跟随。这是此前“收藏仍有但场景不显示”的直接配置原因，上一节的加载兜底和掉队恢复属于额外稳健性修复。

## 坐骑卡片精简与上方骑乘预览

- 按用户反馈去掉坐骑卡片上的骑上/下骑小字，仅保留图片、名称和选中边框；屏幕阅读器操作标签与直接点击切换保留。
- 坐骑页上方改为玩家与当前坐骑的组合预览，下骑显示站立玩家；切回跟随宠物页恢复四卡位编队。共用 resolveMountDrawPose 的男女骑手、骑乘位置、图集格号与前景遮挡配置；分层背景图片避免异步加载互相擦除。
- 6项相关测试通过，覆盖所有可骑坐骑、男女玩家、本地/CDN模式、预览边界、骑乘/换骑/下骑同步、页签切换及卡片不再包含操作小字。npm run build通过。
- 沿用 tests/fixtures/pet-mounts.html 验收入口；当前浏览器连接此前连续失败，本轮未完成实屏视觉验收。未提交、未发布。

## 骑乘时名字贴近坐骑下方

- adventure_renderer.js 改用资源加载器已缓存的当前图格 alpha 边界，按实际绘制比例计算坐骑和骑手的可见底边；名字框顶部保持在组合底边下方7像素，排除飞毯等图格的透明留白，并随朝向、浮动同步。
- 图片未加载或无法读取像素时回退到坐骑配置的地面线；不修改骑乘数据、战斗数值或存档。
- 验证：坐骑、宠物页与跟随渲染7项现有测试通过，JS语法与本文件diff检查通过。完整构建被现有 data/official-website.json 未配置数据包错误阻断；浏览器工具报 nodeRepl.fetch request failed，未完成实屏验收。未提交、未发布。

## 骑乘预览复用主角平台

- 按用户截图调整：骑乘组合直接替换四卡位中主角的站立形象，两种收藏页签都保留同一组平台和其他宠物；移除独立居中的坐骑预览及重复名称。
- 下骑恢复原站立人物，主角换位时骑乘形象跟随主角所在卡位；拖拽支持克隆组合图层。预览限制在原主角半格内，按桌面120px、手机72px、矮横屏64px高度等比适配。
- 6项相关测试、npm run build及定向diff检查通过，新增检查四平台持续显示、预览归属主角按钮和换位后的平台归属。实屏视觉验收仍未完成；未提交或发布。
- 后续按用户要求固定名字高度：骑乘名字取 time=0 的静态姿态可见底边，步行名字取角色地面坐标；角色和坐骑仍正常浮动，名字不随动画上下变化。7项相关测试再次通过，diff检查通过；本次未重复实屏核验。

## 平台骑乘形象放大

- 按用户截图把平台骑乘组合整体放大至原来的1.8倍，围绕图集中偏下的落脚区域缩放；维持原平台、编队和卡片布局。仅作用于骑乘组合，不放大未骑乘人物和跟随宠物。
- npm run build通过。当前工具浏览器连接不可用，本轮未完成实屏视觉验收；未提交、未发布。

## 多语言产品官网

- 新增 `HaqiOfficialWebsite.html`、独立官网样式和中英文词典；采用游戏世界地图与现有永久CDN图集，适配桌面和390px手机。
- 三类用户叙事：老玩家原服数据有限导入、亲子中英情境学习、五系策略卡牌挑战；如实说明实际已接入范围。
- 统计自动读取当前源目录：701条kids卡牌定义、1309项物品候选、684条原任务目录、359种宠物、225套基础技能演出、20组营地课程。目录数量不等于全部可用内容。
- 六岛可点击选择；五系代表卡复用原卡面/技能渲染器，提供播放、简化动画、数值与策略说明。官网展示包约16KB，不加载角色存档或整个冒险资源包。
- Vite和发布器增加第五个入口；官网数据独立输出，主游戏数据打包跳过该文件。发布测试覆盖新包装页。
- 验证：完整Vite构建通过；发布及数据包7项定向测试通过；全量538项中536通过，2项既有失败为缺少data/mount-demo/mounts.json以及quest-catalog来源哈希不一致；未修改无关数据以消除这些失败。
- 浏览器：本地生产构建使用真实CDN资源；检查中文桌面首屏、英文切换、六岛选择、五系播放、390px英文卡牌与地图，控制台无error且无横向溢出。手机英文页头按钮换行已修复。预览不会操作用户登录或存档。
- 已通过现有上传器发布独立版本 `5b3e4a2cfbb98b76`，五个入口及资源均完成逐文件HTTP/CORS/字节哈希核验，release/manifest.json为verified:true；官网地址：https://cdn.keepwork.com/haqi/haqicombatsim/release/5b3e4a2cfbb98b76/HaqiOfficialWebsite.html 。发布器按既有流程同步五个包装页至本机Maisi；未执行git提交或推送。
- 线上HTML与依赖已由发布器逐字节核验；追加线上浏览器复核时浏览器工具连接超时，因此视觉与交互记录以同一生产构建的本地预览（真实CDN美术）为准。

## 坐骑角色四方向统一缩放

- 修正美术实验室按坐骑各方向包围框独立计算人物大小的问题。现有目录以朝前的角色缩放为基准，同步四向，保留方向座位、角色锚点和前景遮挡；男女角色分别保留校准。
- 配置导入归一旧方向缩放；校准面板的角色缩放同步同一主角四向。原模型目录组装与游戏目录生成均采用相同规则，已同步实验室与游戏运行时JSON。
- 9项坐骑定向检查通过；全量541项中539通过，2项既有失败为旧实验室缺少data/mount-demo/mounts.json及quest-catalog来源哈希不一致。完整npm run build通过。
- 浏览器工具连接报nodeRepl.fetch request failed，未完成本轮页面视觉验收；未生成、上传或替换美术资源。改动留在main本地工作区，未提交或推送。

### 后续更正：固定角色尺寸，自动缩放坐骑

- 按用户澄清替换上述“沿用朝前大小并统一方向参数”的方案。恢复原目录四向比例，角色尺寸由场景固定，坐骑边长按“角色尺寸 / 人物与坐骑比例”计算。座位、地面定位、前景遮挡随坐骑一起缩放，人物锚点保持贴合座位。
- 新增共享纯布局模块mount_layout_core.js，实验室和游戏调用同一计算；游戏角色图格大小固定为78像素乘场景缩放，实验室骑乘/下骑共享人物尺寸，变身和列表缩略图继续使用明确的坐骑尺寸。
- 校准面板改为“人物 / 坐骑比例”，调节当前方向只缩放坐骑，不改变人物大小，不再将朝前比例覆盖其他方向；旧配置导入保留原比例。
- 9项定向测试通过，覆盖全坐骑男女四向尺寸、实验室与游戏一致性、比例调整只影响坐骑、座位锚点对齐及变身缩略图。完整构建通过。本轮未完成浏览器视觉复核（前轮浏览器连接不可用）。

## 官网宠物与坐骑展示区（2026-09-25）

按用户新要求替换官网六岛地图章节（保留首屏世界美术背景）。从运行时目录收录全部70款坐骑外观，其中66款rideable、4款展示样式分别标注；展示359种宠物与“300+新宠物，和你一起战斗”。

新增 `js/official_companions.js`：全坐骑可搜索、滚动查看，按可见区域加载美术；宠物每批12位，经rng_core播种洗牌，批内及相邻批不重复。小队预览为骑乘主角和3位宠物，点击添加/移除，满员时替换最早加入者，换批保留小队。复用游戏 `createMountPreview` 的坐标与前景遮挡；网页预览不连接或修改存档。坐骑缩略图使用CDN跨域图像透明边界裁剪，保持完整角色；没有上传新美术。

官网展示包保留全目录的轻量名字/美术字段，骑乘所需姿态与角色图集；宠物英文名复用现有en.txt，坐骑补充稳定ID英文名。修正官网坐骑离线引用为实际demos/mount-lab归档路径。标题、数量、搜索、换批、预览状态及伙伴名称支持中英文，切语言保留搭配。

验证：完整npm run build通过；release/runtime_data/pet_mount_ui共9项通过；浏览器真实CDN美术、70款目录、搜索凤凰/Carpet、切换骑乘、随机12宠物相邻批零重复、添加后仍3位、英文切换、390px无横向溢出与无控制台error均通过。未更改主游戏逻辑或用户存档。
- 已发布版本 d2a0103166ab103d，37个文件全部远端HTTP/CORS/字节哈希核验通过，manifest verified:true，五个正式入口按现有流程同步Maisi；未提交或推送Git。

### 坐骑最大尺寸与四向一致的整体缩放

- 增加MAX_MOUNT_SIZE_RATIO=2上限，按角色原始图格尺寸计算。先用当前男女主角的四方向比例求出最大坐骑尺寸，得到唯一整体缩小倍率；每个方向都使用该倍率，保证主角转向时不变大或变小。
- 未超限时保留角色原始尺寸；超限时人物、坐骑、座位位置及悬浮偏移一起缩小，遮挡沿用缩放后的坐骑坐标。下骑恢复原始尺寸，纯变身/坐骑缩略图保留显式尺寸。
- 实验室与游戏共用限制；面板说明同步更新。10项定向测试通过，覆盖两倍边界、单方向超限影响四方向、男女覆盖参数、全目录尺寸上限、游戏/实验室一致性及锚点贴合。完整构建通过；本轮未完成浏览器视觉验收。未提交或发布。

## 官网小队展示再次简化

按用户截图反馈删除全部坐骑/宠物列表及搜索框，只保留四个大展示位和“一键全换”。点击坐骑换坐骑、点击某位宠物只替换该位；一键全换同时替换全部四位，避免原坐骑及上一组三位宠物，组内无重复。不再清空宠物格。保留70款坐骑外观与359种宠物完整抽取范围、中英文和目录计数。

主角与坐骑使用正式resolveMountDrawPose（骑乘坐标与前景遮挡），在离屏画布合成后按透明边界整体缩放，消除原图留白导致的过小问题。桌面约240px展示区，手机双列；复用原CDN图片，不生成或上传新美术，不读写角色存档。

验证：完整npm run build、JS语法检查、diff检查通过；浏览器确认单个宠物点击只改变对应ID、全换改变四位、图像实际加载与放大、无console error；390px英文模式4个按钮、零列表/搜索框、无横向溢出，英文全换可用。
- 已发布5adf22ccc8a1cf32，远端HTTP/CORS/字节哈希全部核验通过；未提交或推送Git。

### 更正上限为组合可见总高度

- 之前以坐骑整格边长判断两倍上限，会把透明留白和横向宽度计入，误缩小飞毯。现改为人物与坐骑在实际座位位置组合后的可见包围高度，只比较纵向顶部到最底部；上限为原站立角色可见高度两倍。
- prepare_mount_bounds.py从归档WebP逐格读取alpha>16的可见边界，仅补充布局元数据，不改图像。实验室与游戏目录均保存layoutBounds，发布打包保留；原目录组装保留已有边界，更新美术后应重新运行该脚本。配置导入复用当前美术边界。
- 四方向仍共享最严格的整体缩小倍率。飞毯男/女角色四向均恢复原始78px图格；宽而扁的坐骑不再按宽度触发缩小。真正超高时，人物/坐骑/锚点一起缩小。
- 修复发布投影遗漏角色seat/scale覆盖的问题，确保发布前后同样的组合与高度限制。
- 10项定向测试与完整构建通过，覆盖真实飞毯不缩小、全目录可见总高度上限、男女四向尺寸一致、恰好两倍不缩小、超高整体缩小和发布投影一致。浏览器重试仍报nodeRepl.fetch request failed，未完成页面视觉验收。未提交或发布。

### 官网三张可翻换卡牌与自动技能预览

- 卡牌区默认呈现三张扇形卡牌，点击任意一张随机翻换为同系另一张并自动播放真实技能特效；切换流派重新发三张卡并自动播放，支持连续点击、键盘焦点及中英文。
- 官网目录从五张固定卡扩展为184种有实际美术的独立技能（按基础特效去重），沿用游戏的卡牌绘制和技能演出；随机由rng_core实例提供。异步加载按版本隔离，避免快速翻牌后旧资源覆盖新卡。
- 数值按卡牌实际参数显示，有直接伤害/治疗才展示对应数值。手机采用三张卡在上、演出在下，无横向溢出。
- 验证：Vite构建成功；release/runtime_data/pet_mount_ui共9项测试通过。浏览器验证五系翻换、自动播放、中英文与390px布局，未见控制台错误。
- 已发布498b044faaa5fef4，远端入口与资源HTTP/CORS/字节核验通过；未提交或推送Git。

## 五岛特色建筑素材（2026-09-25）

- 使用内置 image_gen 生成哈奇岛、火鸟岛、寒冰岛、沙漠岛、幽暗岛五张透明图集，每张四组：船坞、船只、建筑群、特色塔楼。最终提示词保存在 data/adventure/building-art-plan.json；生成源哈希、裁剪、尺寸及实际 CDN 地址记录于 building-art.json。
- 经人工检查调整源图格间裁剪，避免相邻素材碎片。复用 prepare_environment_art 的 WebP 编码流程，先尝试无损，五张最终均为 768×768、186662–198248 字节，保留 RGBA。源图没有原版 assets_manifest 条目；世界地图仅作风格参考。
- adventure_buildings_core 按固定地图几何布置每岛10组陆上建筑、1座船坞、2艘船；避开道路、居民、地标、出生点及遭遇。世界对象索引和建筑碰撞复用现有实现，树木局部让位，按脚底纵坐标排序；角色被遮挡时半透明，船只有轻微浮动并遵循减少动态效果设置。营地和副本不添加。
- 素材上传 Keepwork CDN 并核验 HTTP 200、SHA-256、CORS *；用户明确选择不复制 digitalhuman-resource 镜像。未发布游戏页面，未提交或推送。
- 定向12项通过，npm run build通过；全量544项中542通过。其余两项为未涉及的 data/mount-demo/mounts.json 缺失及 quest-catalog 来源哈希漂移。浏览器工具返回连接失败，尚未进行浏览器视觉验收，不能宣称已完成实机场景检查。
- 预览入口 tests/fixtures/island-buildings.html，支持五岛与港口/建筑群/塔楼切换，默认CDN，?assets=local可核验离线副本。不读取或修改玩家存档。

### 官网坐骑侧面与完整画布

- 坐骑默认使用左侧面；主角、坐骑与前景遮挡保持同一朝向。
- 修复固定900px合成画布裁掉高坐骑骑手顶部的问题：按主角和坐骑完整绘制矩形计算离屏画布，再读取可见边界。展示容器允许溢出，骑乘画布可横向超出站位。
- Vite构建通过；浏览器用seed=23切换到金羽陆行鸟，确认侧面主角头部与坐骑完整，控制台无错误。
- 已发布ed7044f837ec2020，远端核验通过；未提交或推送Git。

### 树木与地面装饰阴影增强

- 树下柔和投影透明度从0.22提高到0.30，略收紧范围；树根接触阴影从0.24提高到0.28并适当加宽，使树木更贴地。
- 地形瓦片中的deco新增随尺寸缩放的小范围柔和阴影（透明度0.17），比树木和建筑更轻；睡莲、浮叶和沙纹不添加地面投影。沿用静态瓦片缓存，不增加逐帧阴影绘制。
- 验证：terrain_decorations既有3项测试通过，npm run build通过。浏览器预览被工具ERR_BLOCKED_BY_CLIENT阻断，本轮尚未完成视觉验收。
- 修改保留在本地，未提交、推送或发布。

## 暗影神驹正面比例修正
- 正面骑手/坐骑比例从 0.5487890625 调整为 0.30，放大正面坐骑，与侧面马身比例接近；同步实验室、游戏目录和 pose-overrides，保留其他朝向与座位参数。
- 验证：坐骑比例与坐骑功能定向测试共 7 项通过。

## 坐骑列表详情与拖动骑乘

- 坐骑卡片点击改为共享 ItemDetails 嵌套详情，展示物品信息、骑乘预览与坐骑属性；详情提供骑上/下骑，战斗中可查看但不能操作。
- 复用伙伴指针拖动与横向滚动手势，拖到上方任一卡位直接骑乘；重复拖当前坐骑不下马，不修改宠物编队。同步英文提示。
- 验证：4项详情/搜索/战斗限制/落点/指针拖动及取消交互回归通过，npm run build 通过。全量当次543/545通过，失败为 mount_pose 缺少 data/mount-demo/mounts.json、quest-catalog 依赖哈希不一致。随后其他工作区坐骑预览布局发生变化，原有图层非负坐标测试复测失败；未修改其实现或放宽断言。
- 浏览器验收页被 ERR_BLOCKED_BY_CLIENT 拦截，桌面/390px视觉验收未完成。修改保留本地，未提交或推送。

### 宠物窗口坐骑预览采用场景比例，允许向上溢出

- 移除组合图按半个卡位宽度再次压缩、以及后补scale(1.8)的做法。预览直接调用场景布局，以桌面120px角色图格为基准，手机72px/横屏64px等比适配；只在可见组合高度超出角色原高两倍时应用共享限制。
- 组合DOM按人物/坐骑alpha边界定位和底部对齐；图层保留完整图格及遮挡多边形，容器不裁切。卡位、主角按钮和预览均允许overflow；按实际超高量给整排平台预留顶部空间，避免滚动窗口上沿裁掉人物。
- 9项定向测试通过，覆盖全坐骑/男女/本地及CDN图层与场景实际人物尺寸一致、四向限高和当前拖放交互。完整构建通过。浏览器连接此前持续不可用，本轮未完成桌面/手机视觉验收。未提交或发布。

### 官网骑乘以角色尺寸为基准，默认朝右

- 默认朝向改为right，继续复用场景resolveMountDrawPose的角色/坐骑比例、座位与前景遮挡。
- 去掉按组合最大宽高缩进480px方框的二次缩放。画布仅移除透明留白，显示比例由固定角色图格尺寸决定（桌面120px、较窄110px、手机90px），宽坐骑不再连带缩小人物。
- 使用独立站位占位与绝对定位画布，允许组合溢出；不对展示边界裁切。保留骑乘本身必需的前景遮挡。
- 构建与diff检查通过；浏览器seed=90的造梦灵狐验证朝右、人物放大、尾巴完整且可超出站位。
- 已发布06090617545d3676，远端核验通过；未提交或推送Git。

### 官网伙伴数量简化

- 删除66款已接入骑乘计数，中英文仅显示70款坐骑与359种战宠。
- 构建前置校验发现岛屿遭遇派生配置过期，运行现有prepare:island-encounters刷新后构建通过。
- 已发布cd96440b888bb010，远端核验通过；未提交或推送Git。

### 宠物窗口骑乘主角朝向队伍内侧

- 根据heroSlot选择完整骑乘组合朝向：第1/2卡位朝右，第3/4卡位朝左。人物、坐骑、座位和遮挡一起使用对应方向，拖动或键盘换位后随现有重绘更新；场景朝向不受影响。
- 5项宠物UI测试通过，逐项覆盖全部可骑坐骑、男女角色、本地/CDN和四个卡位的图格朝向、尺寸及换位。完整构建通过；未提交或发布，未进行浏览器视觉复核。

### 官网丢牌与发牌动画

- 点击卡牌先用360ms向上滑出并略微旋转，再将新卡用460ms从下方发入、轻微回弹落定，完成后自动播放真实技能特效。
- 离场时并行预加载新卡美术，避免发入占位图；抽牌期间阻止重复点击，切换流派/语言沿用版本校验防止旧动画覆盖新状态。加载失败恢复原牌以便重试。
- 简化动态效果使用短淡入淡出；保留键盘焦点和aria-busy状态。
- JS语法、Vite构建通过；浏览器确认离场动画、连续抽牌后恢复可点、动画中切系无错乱，控制台无错误。
- 已发布773f88b2ca031e68，远端核验通过；未提交或推送Git。

### 官网小队主角与战宠视觉比例

- 主角与坐骑整体放大约三分之一：角色图格桌面160px、较窄145px、手机115px，保持场景骑乘相对比例及朝右方向。
- 战宠视觉缩为原来的70%，以底部中心缩放，保持站位、姓名与底盘对齐。
- 构建通过；浏览器检查飞毯主角与三位战宠，主角明显突出，战宠不再高过主角。
- 已发布8b0c44d8f82983c9，远端核验通过；未提交或推送Git。

## 宠物头顶状态条、休息饱食与四阶段进化路径

- 宠物窗口平台和收藏卡片增加红色生命、黄色饱食两条，含数值无障碍说明；照料计时同步刷新头顶条。仅宠物UI显示，不增加场景HUD。上阵宠物显示等级与形态。
- 默认养成页与形态页共用四阶段进化路径，显示1/10/25/40级门槛、当前形态和未解锁黑色剪影。
- 未在formation中的收藏宠物不消耗饱食/口粮，新增BalanceParams.adventure.restingHungerPerMinute默认0.5，在线离线恢复、封顶100、最多24小时。战斗暂停；主角同位支援仍属上阵。界面照料说明同步。
- 31项相关测试通过；全量551项中549通过，原有两项失败仍为缺少data/mount-demo/mounts.json和quest-catalog来源哈希不符。微调头顶条锚点后7项UI复测通过；完整构建通过。浏览器连接仍报nodeRepl.fetch request failed，桌面/手机视觉验收未完成。未提交或发布。

### 宠物窗口主角与坐骑置于宠物前方

- 将pet-hero-figure层级设为2，宠物pet-stand保持1，平台为0；人物与坐骑组合整体显示在宠物前面，不再由DOM顺序让宠物盖住骑乘形象。
- 纯显示层级调整，完整构建通过；本轮未完成浏览器视觉复核。未提交或发布。

### 官网初始卡牌居中

- 初次发牌及切换流派时，将默认预览卡放在中间并激活，两侧随机卡保持向外倾斜，避免左卡被初始激活后摆正造成扇形不对称。
- 构建通过；浏览器确认初始中间卡激活，切换寒冰后同样保持中间抬起、两侧展开。
- 已发布2c7449f63e681058，远端核验通过；未提交或推送Git。

## 船坞道路与船长站位（2026-09-25）

- 五岛传送点新增60宽短路连接船坞侧面的陆地路口，依岸线选择左右侧，检查路肩、河流、建筑碰撞并清开沿途树木。传送点位置保持原有坐标。
- 法斯特船长移至路口，与船坞完整图像边界保留人物宽度及空隙。优先保留原居民实例（含其他岛屿的原编号），去掉多余临时船长；缺少原居民时保留补齐机制。地图与居民源JSON不修改。
- 更新无存档预览页，展示传送点、连接道路、船坞和船长。浏览器连接恢复后已检查五岛CDN预览及本地模式，画布读取通过，站位无重叠。
- 19项建筑/世界地图/居民测试通过，覆盖原目录及无目录两种路径、可通行性、寻路、交谈、原数据不变；npm run build通过。全量545/547通过，仍为已有坐骑演示文件缺失和任务目录哈希漂移两项失败。未提交、推送或发布。

## 传送点随船长移动（2026-09-25）

- 按用户要求，五岛运行时传送点与船坞旁法斯特船长使用同一坐标；既有道路从原路口连接到船长脚下，保留原传送目的地和交互逻辑。原地图配置不改。
- 19项建筑、居民、世界地图测试通过，新增同位断言及原路口至新传送点通行验证。寒冰岛浏览器隔离预览确认光圈在船长脚下；预览人物裁剪透明留白后按脚底对齐。
- npx vite build通过；npm run build的地图检查通过，但被岛屿怪物配置过期门禁阻断，本轮未改动或重新生成怪物配置。未提交、推送或发布。

## 战斗即时弃牌（2026-09-25）

- 冒险战斗普通手牌向下拖动至少56px后松手、右键或点击弃牌按钮，立即从手牌移除；弃牌列表只追加，不再提供撤销或重新选择已弃卡牌。剩余卡数与返回玩家卡计数同步扣除。
- 鼠标、触笔和触摸均支持下拖；保留触屏横向浏览与上拖施法。取消手势不弃牌，松手后的合成点击不会误选相邻卡牌。宠物卡与符文仍不支持弃牌。
- 弃牌记录随原有出牌/跳过决定交给引擎，保持回合补牌与确定性重演流程；没有修改战斗公式。
- 59项定向测试通过；Edge无存档隔离预览验证右键和鼠标下拖后手牌从4张变为2张、无撤销入口，390px图片失败兜底布局通过。Vite打包通过。全量检查另有已有坐骑数据缺失、任务目录哈希漂移；npm run build前置检查因岛屿怪物配置过期中止，未改写这些无关数据。
- 最终全量重跑550/555通过：失败位于岛屿遭遇（3项）、坐骑数据缺失和任务目录哈希。测试期间观察到岛屿相关源码被其他工作更新；此次未修改这些模块。弃牌定向测试通过。


## 2026-09-25：恢复四岛原始法阵组合

原始 monster-catalog.json 的 placements 数据未丢失；此前 package_island_encounters.mjs 将其去重为每模板一个单怪入口，丢失了运行时组合关系。现在逐法阵导出火鸟岛33、寒冰岛37、沙漠岛29、幽暗岛14处，共113处；其中多怪法阵分别为2、0、7、14处。保留重复怪物、XML空卡位、来源及法阵属性；额外15个子场景任务目标仍保留单怪入口。道路位置是二维改编，未恢复原3D地形坐标。怪物外观继续使用现有绑定，既有卡牌替代规则不变。

旧单怪ID保留为legacyOnly，只供正在进行的旧战斗读档/结算；地图和任务导航不再显示这些入口。新法阵使用独立ID，复用多怪战斗与检查点验证，按全体怪物结算经验、仙豆、任务击败/掉落。野外多怪胜利不写副本通关状态，仍可重复挑战。

验证：8项岛屿定向测试通过（逐法阵对照、混合队伍重演、经验/仙豆、重复挑战、旧单怪存档、道路可达性与任务来源），npm run build通过。全量558项中556通过，另有坐骑测试缺少data/mount-demo/mounts.json和monster-catalog记录的questCatalogSha256与当前任务目录不一致两项失败，未改这些无关数据。首次并行构建写quest-journal.json失败，单独重试成功。浏览器只读预览被工具以ERR_BLOCKED_BY_CLIENT拦截，桌面/手机视觉验收未完成。未提交、推送、发布或读写真实玩家存档。

## 2026-09-25：修复奖励特效导致场景泛绿

交任务、获得经验或升级时，drawRewardEffect 调用未导入的 fill，抛出 ReferenceError，跳过 Canvas restore。动画循环已提前预约下一帧，因此后续场景继承青绿色 shadowColor、shadowBlur 与渐隐 globalAlpha，界面 DOM 按钮不受影响。补齐 locale_runtime 的 fill 导入；奖励绘制和世界坐标绘制各用 try/finally 配对恢复，异常仍向外报告。

新增真实绘制函数回归覆盖经验、升级、物品、普通/减少动画、起始/淡出多帧，以及文字绘制抛错后的状态恢复。奖励定向9项通过，npm run build通过，diff检查通过。全量560项中558通过；仍有此前记录的 mount_pose 缺失坐骑数据和 quest-catalog 哈希漂移两项失败。浏览器工具连接报 nodeRepl.fetch request failed，未完成实际页面视觉复验。未提交、推送、发布或操作真实存档。

## NPC 商店购买确认

- NPC 商品列表的购买按钮先打开共享 ItemDetails / DetailDialog，显示物品说明、拥有数、本次购买数量和实际兑换价格；点击“确认购买”才分发 npc-purchase。
- 取消、关闭及 Esc 不修改角色，关闭后恢复购买按钮焦点；提交前重新校验资格，并防止同一确认重复提交。导师学习流程保持原行为。新增数量文案英文翻译。
- 新增 tests/npc_purchase_ui.test.mjs，使用安卓婆婆真实目录验证预览不扣款、取消/Esc 不扣款、确认单次提交、余额变化重新校验。该测试及 12 项 NPC 回归通过；npm run build 通过。
- 全量测试 558/560 通过，失败位于 mount_pose.test.mjs 与 quest-catalog.test.mjs 的目录哈希检查，未涉及本次修改文件。浏览器工具连接返回 nodeRepl.fetch request failed，桌面/390px 视觉验收未完成。

## 唯一装备不再重复堆叠

- 补齐章节、强化目录、商城候选和NPC目录的原版 maxCount/maxCopiesInStack 字段及导出/发布投影。
- 唯一装备在同步、读档、原服导入和战斗结算时归一；保留穿戴GUID、最高强化和开孔，多余实例的镶嵌宝石退回。旧战斗检查点延后迁移。非唯一装备独立强化逻辑保留。
- 原服导入提示合并结果，并按实际保留实例统计。
- 装备/宝石/导入26项定向回归通过，另加旧存档迁移回归后装备模块11项通过。完整测试562/564通过；已有坐骑测试缺少data/mount-demo/mounts.json、怪物目录依赖哈希不匹配两项失败。npm run build通过。本轮未做浏览器视觉验证；未提交、推送或发布。


### 场景敌人数量文案

按用户要求，名称后的数量仅在同一法阵有多只怪物时显示；单只隐藏“· 1只”，保留名称和已有待迁移提示。node --check语法检查通过。


### 场景法阵标签英文翻译恢复

怪物名称、数量、待迁移状态分别走tr，避免整句拼接后无法命中名称词典；单只继续隐藏数量。补齐四岛48个缺失怪物名及2/3/4只和状态英文条目，109个不同怪物名称已验证全部可译。新增岛屿怪物名称到locale-scan范围。8项本地化测试与语法检查通过；扫描仍有161条其他静态文案缺译，未宣称全站翻译完整。本轮未做浏览器视觉验收。

## 战斗头顶状态（2026-09-25）

- 对照原版 script/apps/Aries/Combat/ObjectManager.lua RefreshBuffs（L1837 起）的 charm / ward / overtime / miniaura 分类，新增原生 Canvas 二维状态徽标，显示在我方、宠物和敌方头顶。护盾使用盾形，减益红色，相同效果合并并显示层数；分行排列、边缘约束、倒下隐藏。
- 头顶和角色卡片共用 battleStatusEffects，保留悬停说明，补齐吸收盾（含 id=0）容量、小光环和未收录说明的回退文本。只修改表现层，不修改公式或存档；状态仍沿用既有回合结算结果，并未新增逐施法事件状态重放。
- 增加只读 tests/fixtures/battle-status.html 和效果失效、叠层、窄画布、死亡隐藏回归测试。
- 验证：6项定向测试通过；npm run build通过；全量567项中565通过、2失败（mount_pose.test.mjs 加载失败、quest-catalog.test.mjs 来源哈希不一致，均非本次状态改动文件）。git diff --check通过。
- 浏览器预览返回 net::ERR_BLOCKED_BY_CLIENT，未完成桌面/390px视觉验收，不宣称其通过。未提交、未推送。

## 沙漠风沙可见性

- 沙尘参照降雪改为原生 Canvas 绘制：48–148 像素弧形风带，暖白细线叠加棕色轮廓，每条带三个深浅沙粒；大小、速度、摆动及阵风强弱错开，避免淡黄色图集融入沙地。
- 保留世界镜头偏移、区域约 0.9 秒渐变、减少动态效果关闭及每层最多 64 组预算；不新增美术资源或修改地图天气配置。
- 验证：天气/地图定向测试 9/9，通过粒子预算与关闭检查；npm run build 通过。浏览器独立内存预览核验南部绿洲和北部沙丘，沙地/道路上的风带清晰且角色名可读，桌面预览约 60 帧/秒；未做手机实机验收。
- 全量 npm test 为 565/567，通过范围外失败为 mount_pose.test.mjs 和 quest-catalog 的依赖哈希检查；未改动相关实现。本轮未提交、推送或发布。

### 头顶状态改为纯图案（用户修正）

- 按用户要求移除头顶所有文字及数字角标。叠层逐个重复图标，卡片悬停详情继续保留文字。
- 查看原版 ObjectManager.lua L1504–1655 状态模型映射，以及本机 Texture/Aries/Item 下 22109_Fire_FireDamageBlade、22148_Ice_IceDamageTrap、22157_Ice_IceGreatShield 的缩略图；用原生 Canvas 重绘圆环术、三角陷阱、学系盾面。属于参考原版图案的二维适配，不是直接导出原模型。治疗使用心形、眩晕使用绕环星形、隐身使用划线眼睛、反射使用双向箭头。
- 无新增位图、外部依赖或 CDN 地址。7项定向测试通过，覆盖所有状态不得调用文字绘制、重复图标、失效状态与死亡隐藏。构建通过。浏览器工具连接失败，未完成视觉复核。未提交、未推送。

### 持续效果显示剩余回合（用户补充）

- DoT / HoT 每条效果独立读取 ticks.length，与 combat_unit_core.js 的剩余回合口径一致，在图标右下角显示数字；其他状态继续纯图案，不显示名称或叠层数字。
- 角色卡片详情显示“持续伤害 / 持续治疗 · 剩余N回合”；耗尽的序列不显示。修正预览页为真实 ticks 结构。
- 7项定向测试通过，调用引擎 popDoT / popHoT 验证不同剩余回合各自递减并在耗尽后消失。构建通过。未修改战斗公式或存档；沿用已有回合结算展示时序。未做浏览器视觉验收，未提交或推送。

### 同类盾合并计数（用户补充）

- 头顶同一护盾定义合并为一个图标，数量大于1时右下角显示层数；消耗后数量减少，剩1层隐藏数量。不同护盾ID、普通/吸收盾不混合，持续盾不同剩余时长保留独立组。
- 仅头顶合并，角色卡片详情仍保留每层说明及吸收容量。DoT/HoT继续独立显示剩余回合，术类保持原样。
- 8项定向测试通过，覆盖四层合为一个图标、消耗更新、不同盾不误合并、DoT回合数保持独立；构建通过。未进行浏览器视觉验收。未提交或推送。

### 头顶状态的悬停 / 点击说明

- Canvas 状态绘制返回真实图标命中范围，按实际缩放同步到透明 DOM 按钮。鼠标悬停、键盘聚焦及点击/触屏均可查看角色名、效果说明、合并数量、持续回合；吸收盾分层容量保留在说明中。
- 点击固定说明，再点图标、空白处或 Escape 收起。查看状态不会触发画布的施法目标回调。状态消失或角色倒下时移除按钮并隐藏说明，重新渲染时清理监听。
- 使用共享纸色 / 金边颜色，说明位置限制在战斗区域内。9项定向测试通过，包含悬停、点击固定/关闭、焦点、Esc、空白关闭、更新、过期及清理；构建通过。浏览器真实指针/触屏视觉验收未完成。未提交或推送。

### 中译英首批118条补漏

- 按用户确认的盘点范围，仅补入英文词典118个唯一键：新手主线37段对白与4个回复按钮，其他界面35条，钓鱼界面42条。沿用已有居民、物品与界面译名；已有5063个词典条目逐条校验保持原值，新词典共5181个键。
- 14个新手任务的69条唯一标题、说明、对白、按钮均有非空英文；本批118条没有重复键、空译文或残留中文。未扩展翻译其余目录候选、原版归档剧情或动态模板，未修改游戏源码。
- 本地化与开场剧情12项定向测试通过。现有扫描缺项由172降至54：53个带括号居民称谓（显示时去括号已有译文）和1条开发调试说明，不属于本批遗漏。
- 在独立Playwright浏览器运行 verify_en_locale.mjs：设置、HUD、岛屿/世界地图、签到抽查通过，未出现脚本错误；语言名称保留原文。没有逐一进行14段任务流程和全部钓鱼状态的视觉验收，剧情完整性另由真实词典键覆盖校验。
- npm test：570项中568通过、2失败。mount_pose.test.mjs 缺少 data/mount-demo/mounts.json；quest-catalog.test.mjs 的 monster-catalog.questCatalogSha256 与任务目录实际哈希不一致。两项失败路径不读取英文词典，未在本次翻译任务中修改相关数据。
- 未提交、推送或发布。盘点与验证输出位于 .cache/locale-audit-20260925/。

### 场景宠物始终陪伴

- 场景统一通过 `selectCompanionId` 选择同卡位宠物，否则选与主角卡位序号距离最近的已上阵宠物；等距取较小卡位。全空时优先背包中已有的抱抱龙，再选其他已拥有宠物。无宠物旧档使用绿色抱抱龙的展示兜底，不写背包或战斗编队。
- 复用原有绕行、停留、追赶与传送复位动画，绘制所选宠物的真实成长图集。语言入口、邀请和会话标题与场景伙伴名称一致。
- 宠物与语言22项定向测试通过；构建通过。全量测试结果见下方补记。未进行本轮真实浏览器或麦克风验收，未提交、推送或发布。
- 全量复核：572项中563通过、9失败；7项为现有语言设置新增 model/showChinese/voiceType 导致存档深比较不一致，另2项为 mount-demo/mounts.json 缺失及怪物目录的任务目录哈希不符。场景缩放与拾取回归通过，完整输出在 `.cache/companion-tests.log`。

## 营地NPC双语剧本聊天与模型音色设置

- 参考HelloLearner learner-runtime的350ms按住/点击区分，新增稳定DOM聊天视图，保留头像、左右气泡、中文开关、提示和录音状态。手机390px验证默认双语、提示展开与完整三轮。
- 新增150句型作者源与900情境模板，生成27位NPC、81个三轮故事；按原描述和createWorld实际摆放记录角色/地标依据。22位地图已启用，5位优秀学员原停用，仅保留配置和预览。
- 实际麦克风转录才可推进；样例本地匹配，变体通过AI语境/句型/原文证据核对。无效结果、服务失败、取消及迟到请求不发奖励。三轮完成后按原课程组共享额度，故事记录与奖励一同持久化。中途退出本轮从头练，不保存原始音频和完整转录。
- 设置从SDK读取对话模型和音色，可试听；model传aiChat.chat，voiceType传speechRTC.createSession。旧档默认双语、默认模型和音色；修正新角色默认值与旧档规范化一致性。
- 测试：28项定向通过；完整579项中577项通过，2项已有失败（mount-demo/mounts.json缺失、任务目录源哈希不一致）。生产构建通过；浏览器桌面/390px查看立绘和双语布局，模拟录音逐轮推进，最终只发10奇豆，控制台无错误。
- 真人ASR、自由表达线上判定、所选音色英文发音仍待试玩；模拟文本不作为真人录音证据。未提交、推送或发布。保留工作区其他既有和并行修改。
- 补充浏览器核验：844×390横屏，设置从线上SDK获取7个模型/10个音色，隔离角色保存keepwork-lite与阳光男声；未改变真实角色偏好。录音按钮小高度样式修正为不换行。

### 普通NPC对白双语与点读

- 剧本聊天每轮默认展开完整回答提示、句型、中英示例和“听示范”；可主动收起，进入下一轮重新展示，避免玩家不知道应答方向。显示示例仍记录为提示练习，通关继续要求麦克风回答。

- 学习模式下，普通NPC描述及连续剧情对白默认显示目标语言和母语，尊重隐藏中文设置；缺译文保留原文。台词可逐条点击朗读，复用Keepwork语音和所选音色，切换及关闭会取消播放，不触发默认任务按钮。
- 28项对白、剧本与语言冒险定向测试通过，生产构建通过。隔离开场对白页面在1280×720和390×844检查中英排版通过；回调测试覆盖语言选择、点击不推进、取消旧播放及关闭取消。未进行真人音质试听。本次未提交、推送或发布。

## 2026-09-25：战斗选牌效果说明

- 选中手牌后，卡面旁显示卡名、学系、目标、魔力消耗、实际规则冷却、基础命中与分段效果；桌面并排、390px手机说明区域可滚动。重新选择、弃牌、施法动画或战斗结束时随手牌收起。
- 新增纯展示模块 `js/card_description_core.js`，读取当前 resolved 卡牌参数及增益/护盾字典；卡面与详情共用数据。直接伤害保留区间，持续伤害/治疗分回合显示，吸收盾不再写作治疗，X费按每点魔力表示。普通单体持续牌附基础总量；复杂特殊效果尚未完整收录时明确提示，不宣称全面覆盖。
- 冷却展示改读引擎实际使用的 params.cooldown，修正物品表与施法参数不一致的显示；护盾不再绘制治疗十字类型图标。战斗公式、存档和资源未修改。
- 验证：卡牌说明/渲染/无图回退/手势17项定向测试通过，npm run build通过。浏览器真实renderBattle内存验收页 `tests/fixtures/card-details.html` 已检查桌面与390×844、正常图/无图、键盘Enter选牌、重新选择、弃牌与施法中隐藏；不读写玩家存档。未执行完整战斗动画验收。
- 全量测试仍有两项无关失败：mount_pose缺少data/mount-demo/mounts.json；quest-catalog的目录依赖哈希不一致。
- 修改保留在main本地工作区，未提交或推送。

### 宠物营养餐直达与拖动撤阵

- 宠物详情的“购买营养餐”直接打开商城“补给 → 喂养材料”；控制器接收子分类并重置搜索、收藏、等级、学系和分页，保证旧筛选不会遮住营养餐。
- 删除宠物详情的四个上阵卡位按钮及“休息”按钮。上方宠物可拖到下方列表、列表工具栏或统计行撤阵；悬停时列表高亮。拖回其他法阵位仍换位，取消、列表外松手及战斗中拖动不撤阵。主角和坐骑不会被当成宠物撤下；键盘聚焦上方宠物可用 Delete/Backspace 撤阵。
- 不修改宠物收藏和主角卡位；沿用已有场景跟随兜底。
- 36项宠物/坐骑/跟随定向测试通过，完整构建通过。隔离内存角色浏览器验收1280×900及390×844：购买跳转、选中子分类、旧搜索清空、详情无编队按钮、实际指针拖回列表、无误开详情及页面横向溢出均通过，无脚本错误；未写真实角色存档。手机尺寸用指针模拟，未在真实触屏设备验收。
- 验收脚本及截图位于 `.cache/verify-pet-interaction.mjs` 与 `.cache/pet-{shop,rest}-{1280,390}.png`。未提交、推送或发布。
- 全量测试586项：584通过，2项失败仍为 `data/mount-demo/mounts.json` 缺失和怪物目录记录的任务目录哈希不一致；本次未改相关数据。

- 聊天窗口删除重复的‘魔法营地 · 情境对话’栏；共享关闭按钮固定在窗口右上角，手机角色栏预留按钮空间。浏览器390×844及桌面排版核验通过，未遮挡角色名或标题。

### 宠物已解锁形态自由切换

- 新增纯核心 `petAppearanceStage` 与 `pet-appearance` 动作；保存可选 `appearanceStage`，读档验证整数和解锁范围。旧档无字段时按实际等级显示，不改战斗规格及历史检查点结构。
- 进化路径改为可点击/键盘操作的形态按钮，两页共用。未解锁形态禁用；选中后保留详情窗口、刷新大图与选中态并恢复焦点。收藏、跟随和己方战斗渲染读取外观偏好；属性、卡包、成长和怪物外观继续按真实等级。
- 35项定向测试通过，覆盖选择/读档、未解锁及非法值、卡包与战斗规格不变、战斗检查点兼容、按钮禁用和选中状态。隔离内存角色浏览器1280×900/390×844通过：40级切幼年、切页后Enter选择青年、关闭后列表同步、1级仍禁用3个阶段，无脚本错误、无页面横向溢出；未使用真实存档。
- 浏览器脚本与截图：`.cache/verify-pet-appearance.mjs`、`.cache/pet-appearance-{1280,390}.png`。未提交、推送或发布。
- 全量591项：589通过，2项失败为既有坐骑演示数据缺失及怪物/任务目录哈希不一致。完整构建通过。

## 2026-09-25：棱镜与吸收等效果释义、英文文案

- 棱镜描述从ward的prism_from/prism_to读取真实方向，解释改变目标受到的伤害、匹配时消耗、使用场景、转换后的抗性以及后放先处理顺序；四种棱镜对照processDamageAgainstWards验证。原始卡表预览通过inferTargetKind与战斗目标推断保持一致。
- 吸收盾增加容量/溢出/剩余容量说明及600承受200后剩400的示例；减伤盾、陷阱、增益/减益术与眩晕吸收按效果字段解释，避免仅显示简短标签。
- describeCard接受只读translate回调，以中文命名占位符模板先翻译再填值，core不引用浏览器locale模块。冒险卡面、详情、选牌可访问性标签和悬浮标题接入；en.txt补齐模板和效果标签并将展示模块加入locale扫描白名单。不改变战斗数值与公式。
- 19项定向测试通过，包含四种棱镜的真实触发/消耗、吸收跨次扣减及溢出、701张kids卡牌生成英文无中文/占位符残留。浏览器桌面/390×844检查中英切换、棱镜方向、长说明滚动与选牌目标，使用内存验收页，不改玩家存档。
- 本轮完整构建通过。全量测试588/591通过，失败项为未改动的dialogue_ui测试桩缺少append、mount_pose缺少演示数据、quest-catalog依赖哈希不一致；未扩大修改这些并行工作区域。locale扫描仍有全局其他缺键，但card_description_core本轮文案无缺键。

### 对白朗读图标与AI词义映射

- 目标语言末尾增加喇叭，母语译文末尾增加词义映射入口。固定keepwork-lite生成双语分段，严格保留原句；对应词同色，近似词邻近色并用虚线，词性区分色系。取消、失败重试与同窗口去重已接入。
- 14项定向测试通过，生产构建通过。隔离页面模拟映射在桌面/390px核验；真实请求因未登录被阻止，未验证线上模型映射质量。未提交或发布。

### 对应词独立配色及TTS缓存

- 细化主谓宾及不同名词的分组提示，按组分散色相；中文映射词组增加0.09em两侧间距，原文不加空格。模拟映射浏览器可见检查通过。
- 核对SDK：AudioEngine已有独立内存/IndexedDB机制，但游戏的SpeechRTC没有接入。SDK新增synthesizeCached，100条/20MiB内存、500条/100MiB IndexedDB、30天闲置过期，LRU淘汰；键覆盖文字及生效的音色/语速/模型/情绪等参数。只缓存完整合成音频，不缓存ASR录音。
- 游戏自动检测新接口；现有CDN旧SDK使用100条/20MiB临时内存兼容缓存，发布新版SDK后自动启用持久层。没有上传SDK或修改CDN路径。
- 游戏15项定向测试通过、构建通过；SDK缓存测试及原语音协议回归通过、TypeScript检查通过。真实浏览器验证IndexedDB跨实例读取、500条淘汰及清空通过。模拟合成检验同句少调用一次，不代表真人音质验收。

### 主角与坐骑的全向移动特效

- 保留原四方向身体/坐骑图集，新增 `adventure_motion_effects.js`：碰撞处理后的世界位移决定角度，独立魔法飘带全向偏转；沿实际轨迹留下火系余烬、冰晶、风暴电弧、生命叶片、死亡幽焰、左右脚印及轻尘。
- 纯表现成长强度：等级贡献50%、已穿戴GUID实例强化贡献30%、已镶嵌宝石数量贡献20%，各项按50级/累计80强化/12颗宝石归一后总强度封顶。强化和宝石不会读取背包内未穿戴实例。强度提高粒子大小、密度、寿命，达到0.3产生淡残影；不改变任何战斗公式、存档或资源。
- 会员读取现有运行时状态，ready且isVip且未过期时显示前景金色魔法星与星形拖尾；不伪造会员档位。身体图集中已烘焙的装备仍随四方向，新魔法配件独立按真实角度渲染。
- 粒子采用独立rng实例、按行程采样，最多160个；停步消退，传送/切图/上下坐骑/长时间中断清理；标题、战斗、隐藏、捕鱼、传送过程及prefers-reduced-motion不生成。坐骑残影省略姓名边界计算。
- 验证：新增5项定向测试通过（斜向/停止/确定性/容量/传送切图/减少动态/装备成长/会员/帧率），完整npm test 600项通过598项；既有失败为data/mount-demo/mounts.json缺失与quest-catalog依赖哈希不一致。完整生产构建通过。
- 浏览器：隔离内存预览 `tests/fixtures/motion-effects.html`，实际Canvas桌面与390px检查、五系选择、CDN骑乘及会员开关；不写玩家存档。既有本地离线坐骑资源路径无效，骑乘验收使用默认CDN。真实账号会员未另行登录验证。未提交、推送或发布。

### 延长移动脚印留存

- 脚印寿命从0.95秒延长到3.5秒：前1.2秒保持清晰，随后2.3秒逐渐淡出；步行和骑乘共用。元素粒子仍为0.6–1.18秒，形成粒子先散、脚印后退的顺序。
- 更新已有停步消退回归，验证1.3秒时只剩脚印、3.7秒时清空；5项移动特效测试通过。未提交或发布。

### Flash精简映射与500条持久缓存

- 词义映射改用keepwork-flash，明确reasoning:false，返回纯文本词语(#RGB)，不再要求模型生成冗长JSON结构。解析器容忍空白差异但禁止改写原文，保留安全纯文本上色。增加映射中/已映射/重试状态。
- 新增独立500条内存与IndexedDB最近使用缓存，同句在途请求合并；支持Web Locks时跨同源标签去重，单个视图取消不会中断其他等待者。失败不入缓存。
- 19项定向测试通过；浏览器实测模拟生成并发一次、IndexedDB跨实例复用、500条淘汰及精简格式上色通过。未实测登录后的真实模型速度与映射质量。未提交或发布。

### 蓝色魔法星16帧独立图集

- 核对原版Pet/main.lua：kids会员附件模型为character/v5/11vip/MagicStar/MagicStar.x，通过AddAttachment(asset,4,1)挂载。下载并按原清单MD5核验DDS纹理，解码归档为WebP，完整源条目另存。
- 使用内置imagegen参考原纹理蓝色星体/暖黄色中心光重绘4×4、16帧转身浮动循环；非原生模型动画导出。第二次清理候选未采用，最终保留第一版。
- 最终assets/adventure/effects/magic-star-blue-16.webp：768×768、每格192×192、139278字节、质量95、alpha保留；通过16格非空和可见主体边距校验。SHA-256、帧率10fps、1.6秒循环、来源及alpha边界阈值32记录在art-references/magic-star/atlas.json。压缩脚本scripts/prepare_magic_star.py可复现。
- tests/fixtures/magic-star.html提供自身旋转与头顶绕行预览、暂停及原WebP下载。浏览器核验Canvas逐格绘制及透明背景合成，不读写玩家存档。未上传CDN、未接入运行时会员效果、未提交或推送。


### Personal workspace缓存与钓鱼按需加载

- 按用户新约定，角色目录、历史、检查点、学习档案和钓鱼镜像统一开启服务器cache读写；savePageData保留显式等待sync与读取核验，角色冲突及账号检查保留。profile使用SDK缓存，避免每次同步强制刷新。
- 无钓鱼纪录不创建或读取fishing/records.json；纪录不变的普通保存跳过镜像，新增纪录/移除有纪录角色时更新。镜像revision表示最近一次纪录变更版本。
- fishing.json从启动包分离，首次进入钓鱼加载，合并并发并允许失败重试；6.6KB物品元数据保留，避免旧存档、背包和商店依赖完整钓鱼配置。导出脚本同步生成物品清单。
- 38项云存储、角色、加载器及打包定向测试通过，npm run build通过；完整608项中606项通过，2项无关失败：mount-demo/mounts.json缺失，以及quest-catalog依赖哈希不一致。
- 浏览器源码模式完成访客创建及场景启动；HTTP记录确认启动仅请求fishing-items.json，不请求fishing.json；首次点击海面、自动走到岸边后请求完整配置并显示钓鱼渔具及精力。未使用真实账号验证线上pageCache读写。未提交、推送或发布。

### 魔法星改为正面软弹待机

- 按用户反馈，使用内置imagegen将整圈翻转改为始终正面的16帧轻微扭动、压扁回弹、浮动；保留蓝色星体与黄色中心光。
- 新文件magic-star-blue-idle-16.webp为768×768、4×4、128252字节，旧版保留；打包器新增--idle，独立来源和哈希记录于atlas-idle.json，完整提示词归档。
- 原预览页已切换新版，浏览器检查正面姿态、透明背景及头顶绕行；16格主体边距、非空、尺寸与预算打包校验通过。未修改游戏运行时、未上传/提交/推送。


### 用户数据分层：小型云端状态、物品收藏、战斗背包与本地临时状态

- 云端roles入口升级为schemaVersion 2：当前小型状态/分文件引用，items保存收藏及装备实例，battle保存数量账本/配装/坐骑/阵容/出战副本，records保存交易流水/奖励记录/钓鱼榜。不可变分文件只在内容变化时新增，先核验分文件后更新入口；历史清单不再复制大收藏。未变化文件会话内复用。
- 本机IndexedDB保存血量、饥饿、位置、护理计时、精力、签到在线时长及未结束战斗。缺失按满格恢复；已领取标记、物品数量等核心数据仍云存。核心指纹忽略这些临时变化和游玩时间，同角色选择不再触发同步；战斗过程仅本地，结束后同步核心结果。
- 兼容旧全量云/本地存档，迁移旧战斗到本机；保留账号/角色隔离、跨设备冲突、失败不发布入口。相同云端核心状态重连时保留更新的本地运行状态和随机序列revision。
- 350宠物/1000流水投影压力测试、物品数量变化复用收藏、分文件失败/并发冲突与迁移缓存回归通过。完整npm test：622项，620通过；2项既有失败为mount-demo/mounts.json缺失及quest-catalog依赖哈希不一致。npm run build通过。打包器排除工作区新增的独立hero-preview.json配置，避免误并入游戏数据包。
- 浏览器隔离fixture验证真实IndexedDB跨实例恢复血量/饥饿、无云端dirty、战斗确定性重演。主游戏新访客创建、进入场景及刷新恢复通过；最终旧本地存档首次重连战斗迁移补测36项通过。未使用真实账号写入或迁移云数据，未提交、推送或发布。结构详见docs/user-storage.md。

### 按确认稿重做角色配色提示词

- 按用户确认稿实现：text/translation为已翻译对应内容，固定角色/词性色板，句法角色优先，两组完整JSON示例；同义同色、近似邻近色。模型改回keepwork-lite，维持reasoning:false。
- 解析JSON内颜色标记，仍校验原句字符并恢复空白。缓存键更新为role-colors-json-v3，旧Flash协议结果不会复用；500条IndexedDB及在途去重保留。
- 20项定向测试及生产构建通过；包含新JSON协议、确认示例和缓存回归。未实测真实模型语义标色质量，未提交或发布。

### 魔法星改为VIP 1～16级静态图集

- 按最新要求停用序列帧方案，内置imagegen生成同一正面蓝星的16级外观，依次增加光泽、银边、金边与宝石；不映射或修改原版会员等级规则。
- 新文件assets/adventure/effects/magic-star-vip-levels-16.webp为320×320、4×4、每格80×80，31,908字节，严格小于32,000字节。编码质量80、透明alpha保留，16格主体非空且未触边，SHA-256及格号映射见atlas-vip-levels.json。完整提示词见vip-levels-prompt.txt，prepare_magic_star.py --levels可重现压缩。
- 当前预览页切换为16级选择，固定读取选中格，仅用代码做±4度左右摇摆和±3.5%缩放；支持暂停、减少动态效果。浏览器核验VIP1/16切换、透明合成及暂停通过。未上传CDN、未接入游戏、未提交或推送。

### 打开对白自动恢复词义映射

- 打开双语对白先执行仅缓存查询，命中即恢复颜色和已映射状态，不需要再次点击；无缓存或损坏缓存不触发AI请求。关闭窗口后的迟到缓存结果不会更新已关闭视图。
- 12项缓存与对白定向测试通过，覆盖自动上色、零生成调用及关闭保护。

### 同词类差异色阶

- 词义映射提示词扩展为12类固定色系，每类明确前三个色阶，后续允许同色系六位色微调。主宾语只覆盖核心词，形容词/副词/连词/介词/限定词/数词分别标注。加入He is tall and thin完整双语例子，tall与thin分别使用#067/#089。双语对应优先，明确一对多允许同色。
- 缓存协议更新role-shades-json-v4，隔离旧版少色结果，保留500条缓存和自动恢复。12项缓存/对白回归通过。实际分词及配色仍由模型按提示词判断。

### 提示词版本与缓存失效

- 新增DIALOGUE_MAPPING_PROMPT_VERSION（当前5），提示词与缓存身份统一引用。缓存身份同时包含完整规则正文，忘记升版本但修改了提示词也会自动失效，内存/IndexedDB/在途请求及跨标签锁均共用身份。旧结果不再命中，后续按500条上限自然淘汰。
- 12项缓存与对白测试通过。

### 魔法星绕行与追赶接入

- 新增纯表现运动模块adventure_star_motion_core.js：世界坐标弹簧跟随、椭圆绕行、周期性轻快换位、落后加速追赶、小幅摇摆/缩放。停步继续绕行，传送/换图/长间隔重置，减少动态时固定相对头顶位置。
- adventure_renderer接入正式会员状态，按既有magicStarStatus等级选择图格（现有规则上限10，不创造11–16级权益）；步行锚定头部，骑乘使用骑手姿态，前后层分别绘制。替换旧静态金色跟随星，已有脚下星形粒子保留。
- 图集已使用既有upload-deploy-cdn-files技能上传至https://cdn.keepwork.com/haqi/adventure/magic-star/20260925/magic-star-vip-levels-16.webp，31,908字节，HTTP/CORS/远端SHA-256核验通过。data/adventure/magic-star-art.json登记来源及地址，图片按需加载，构建不携带WebP。用户选择仅上传CDN，不复制digitalhuman-resource。
- 预览magic-star.html使用同一运动模块与游戏人物图片，点击移动、自动跑动、等级切换及暂停；内存场景motion-effects.html复用正式渲染器，支持坐骑。没有读写真实存档或登录真实会员。
- 定向9项通过；完整636项中634通过，2项仍为已有mount-demo缺文件和quest-catalog依赖哈希不一致；生产构建通过。未提交推送或发布游戏构建。

### 魔法星 UI 美术统一

- 魔法星详情徽章改用场景伙伴同一套 VIP 1–16 级蓝色 WebP 图集，通过 `drawMagicStarIcon` 共享裁剪与等级选择；移除旧金色 SVG 和图标上的数字，等级仍由旁边标题明确显示。
- 未激活状态使用基础星灰色样式；复用既有资源加载器、CDN/离线选择与异步重绘，无新增图片。文字入口保持原有交互。
- 验证：生产构建通过；运动与跟随定向 9 项测试通过。浏览器工具连接失败，未完成本次 UI 截图复核。

### 主角头身分层独立预览

- 新增`HaqiHeroPreview.html`与独立`HeroRenderer`，覆盖原整身/新分层对照、66种骑乘、无坐骑、骑手站姿、UI缩略图及锚点检查。正式游戏入口、原角色WebP和坐骑目录未替换。
- 根据用户迭代，头部改为男女各一套4×4十六方向，步行骑乘共用，女生统一棕色双马尾。头图不含脖子；身体清头区域内补短脖子，再由头部覆盖。新增“只看身体与脖子”检查开关。
- 六张身体无损WebP保留原画布与身体坐标；步行副本只额外清空不使用的树木/建筑/另一性别行，不重排主角格位。六套身体的遮罩外像素完全一致。头图清除孤立alpha噪点，避免包围盒失真造成悬空。
- 8张最终资源最大182,462字节，全部通过既有Keepwork上传器发布到`keepwork/haqi/hero-preview/20260925/b6b6501d03cf/`，远端SHA-256/CORS全部通过。用户选择仅CDN，不同步digitalhuman-resource。
- 定向11项通过，包括66坐骑×男女×四向×静止/移动共1056组与既有姿态的严格比较、头部替换不影响身体、部分资源失败整身回退、独立视觉RNG。全量本次运行634项中632通过：已有mount-demo/mounts.json缺失与quest-catalog依赖哈希不一致各1项失败。正式构建通过。
- 浏览器核验本地/CDN、男女步行、鸵鸟坐姿、飞毯站姿、前/侧/背方向、短脖子开关、UI缩略图及390px布局；未读写真实存档。生成图16方向的精细美术效果仍待用户验收。使用说明见`docs/hero-preview.md`。

### 战斗卡牌说明改为上下折叠面板

- 选中卡牌的效果说明由左右并排改为上下结构：折叠态在卡面上方仅显示"查看详情"胶囊；点击展开为带"卡牌说明 / 收起"标题栏的说明框（卡牌名、meta 行、效果行、备注）。原生details/summary实现，展开状态存localStorage（haqi.battle.card-detail-open），默认折叠，刷新与下场战斗保持。
- en.txt补充"卡牌说明 / 收起"两条文案；"查看详情"沿用既有词条。布局适配650px宽与500px高视口；折叠态外层不拦截法阵点击。
- 验证：npm test既有2项无关失败不变（mount-demo缺文件、quest-catalog哈希），其余通过；Vite浏览器实测折叠默认态、展开、收起、localStorage='open'与控制台无新增报错。

### 卡牌说明面板微调

- 悬停说明框不再触发手牌 hover 上浮：hand-focused 模式下 .card-select 的 hover transform 置为 none。
- 展开态移除"卡牌说明"标题行，"收起"改为右上角小字链接（绝对定位），标题 h3 预留右内边距；折叠态"查看详情"胶囊不变。
- en.txt 移除不再使用的"卡牌说明"词条。定向17项测试通过，浏览器回归确认无卡牌跳动、切换与持久化正常。

## 冒险战斗地面行动指针

依据原版 arena_server.lua L82/L5224-5232 的 movearrow 顺序与 ObjectManager.lua AnimateArrow L2277-2349 的正向线性旋转，在 Haqi.html 每次施法、失误、跳过或捕获前加入200ms表现步骤。原生Canvas金色箭头贴合法阵椭圆，位于角色下方，跟随实际出手者；伤害/治疗期间保持方向，回合间保留最后方向，战斗结束隐藏。减少动态效果时直接转到目标。指针不写引擎事件、存档或随机状态，不改战斗公式；这是二维重绘，并非原模型导出。

验证：指针及角色动画6项通过；完整npm test为638项/636通过，失败分别为mount_pose缺少data/mount-demo/mounts.json和quest-catalog已有依赖哈希不符，均不涉及本次文件。npm run build通过。只读tests/fixtures/battle-pointer.html浏览器核验玩家及三怪连续施法，桌面和390px宽画布指针可见；非完整手机交互验收。改动保留main本地，未提交或发布。

### 营地语言按钮修复右侧滚动条

- 语言学习开启后页面右缘出现意外垂直滚动条。根因：`.learning-launch`（抱抱龙 · 和我聊聊）按钮挂在 body 上，adventure_storybook.css 的 `.storybook-ui :is(.primary,.secondary){position:relative}`（特异性 0,2,0）压过 language_adventure.css 的 `.learning-launch{position:fixed}`（0,1,0），按钮以相对定位坠到 100dvh 高的 #game 之后，文档高出视口约 50%。
- 修复：language_adventure.css 新增 `.storybook-ui .learning-launch{position:fixed}`，同特异性且该文件在 Haqi.html 中加载更晚而胜出；按钮其余样式仍沿用 storybook 纸面皮肤。
- CDP 验证（737×1001 视口、语言学习开启、营地世界）：按钮 computed position 恢复 fixed、位于 top:110/right:16 设计位置，documentElement.scrollHeight === clientHeight，截图右缘无滚动条轨道色像素。仅 CSS 表现层修复，未改引擎与存档；未提交或推送。
